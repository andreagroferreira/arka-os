# Security review: JEV Decisions Layer, PR1

- **Date:** 2026-09-23
- **Branch:** `feature/jev-decisions-1`
- **Reviewer:** Bruno (security-eng, dev squad), requested by Paulo
- **Scope:** `core/decisions/{privacy,shadow,cache,backoff,transport,client}.py`, reading `engine.py`, `site.py`, `telemetry.py`, `core/egress/*` and `core/evals/sanitizer.py` for context. Follow-up from QA: `core/governance/leak_scanner.py` (`.jsonl` coverage).
- **References:** ADR `docs/adr/2026-07-31-egress-policy.md`, ADR `docs/adr/2026-09-23-jev-decisions-layer.md`, campaign plan ("Architecture" and "Counter-arguments" sections)
- **Frameworks:** STRIDE (per finding), OWASP Top 10:2025, CWE
- **Verdict:** APPROVED-WITH-FIXES. Both major findings of the first pass are fixed and covered by tests. The first pass left four actions for the owners of `engine.py`, `site.py` and the ADR (see "Actions outside my scope"); all four are now closed. Quality Gate round 1 rejected the PR on three majors and two minors in this lane; they are fixed below (findings 14-18).

Every piece of evidence below comes from execution: probes in a temporary directory with a fake HOME and fictional client names, local HTTP servers, and mutation runs. The operator's redaction config was never read out or printed.

## Findings

| # | Finding | Sev. | STRIDE / OWASP / CWE | Evidence (command → result) | Fix or recommendation | Owner |
|---|---|---|---|---|---|---|
| 1 | **The OpenRouter Bearer key followed a redirect to another host.** urllib turns a 301/302/303 answer to a POST into a GET and copies every non-content header to the host named in `Location`, `Authorization` included, even on a downgrade to `http://`. That host's response was then parsed as the decision. | **major** (low likelihood, since it needs a redirect from OpenRouter's side; high impact, since it is a paid key) | I + T / A02:2025 Security Misconfiguration / CWE-522 | Probe with 2 local servers (origin 302 → another port): `REDIRECT followed; B saw auth: True /steal`. Source: `HTTPRedirectHandler.redirect_request` copies `req.headers`. | **Fixed** in `client.py`: `Authorization` travels as an *unredirected header* (urllib never copies those), and a response whose final URL differs from the request is refused with `reason=redirected`. urllib already drops the body (the state) on the GET. 307/308 still surface as `http-307/308`. I kept the `urllib.request.urlopen` patch seam that the engine, replay and shadow tests rely on, so I did not use `build_opener(_NoRedirect)` as `layers_kb.py:216` does. The effect is the same. | Bruno (done) |
| 2 | **The 96k cap ran BEFORE redaction and the secret check.** An identifier or secret cut in half no longer matched its pattern, and the surviving part left in clear text. | **major** (rare today, since prompts are short; routine once later PRs send diffs and transcripts) | I / A06:2025 Insecure Design / CWE-200 | Probe with a fictional two-word client name straddling the cut: `CLIENT-SPLIT tail: '… Zorblax…[truncated]'` (the first word of the name left in clear). | **Fixed** in `privacy.py`: normalise home → run the checks on the whole text (bounded at `MAX_SCAN_CHARS=1M`, 17 ms measured for 1 MB) → cap last. If the 1M bound cuts the text, the last 4096 chars are dropped as well (`_TAIL_GUARD`), so no token split at that boundary reaches the send. | Bruno (done) |
| 3 | **A missing redaction config turned the JEV off for every npm user** (item 2 of the request; decision below). | major (product availability) | D / A10:2025 Mishandling of Exceptional Conditions | `evaluate` with `{"clients": []}` → `False ['redaction-config-missing']`; with no file → the same; with a fictional list → `True [] … [CLIENT-2] … [CLIENT-1]`. So (a) **no**: the sanitizer raises on the empty list, and (b) seeding `{"clients": []}` in the installer **does not help**. | **Implemented (c)** in `privacy.py`. See "Item 2 decision". | Bruno (done); ADR and engine: Paulo/Gabriel |
| 4 | With `redactClients: false` the state left **without an egress audit line**, which breaks the egress ADR invariant ("no audit, no egress"). | minor | R / A09:2025 Logging & Alerting Failures | Reading plus a test: with `redact=False`, `evaluate` never runs, and before this fix no audit line was written. | **Fixed**: `_unredacted` refuses secrets and writes an audit line with `override=redact-disabled`. If the audit write fails, the result is `egress-denied:audit-unavailable`. | Bruno (done) |
| 5 | **8 orphaned spools turned shadow mode off for good.** The sweep only ran in the worker's `finally`, and a killed worker (SIGKILL, reboot) left its spool behind. With 8 orphans, `spawn_shadow` returned False on every turn, with no live worker left to sweep. | minor | D / A10:2025 | New test: 8 spools with an mtime older than 1 h → without the fix, `spawn_shadow` returns `False`. | **Fixed**: `spawn_shadow` calls `sweep_stale()` before counting. | Bruno (done) |
| 6 | **A planted or corrupt `backoff.json` with a distant `until` blocked the JEV indefinitely**, and stopped a real trip from overwriting it (`current > until`). | minor | D / A08:2025 Software or Data Integrity Failures | New test with `until=1e12`. | **Fixed**: `MAX_BLOCK_S=3600`. `blocked()` ignores deadlines more than 1 h out, and `trip()` caps its seconds at 1 h and overwrites an invalid deadline. | Bruno (done) |
| 7 | Backoff shows up in telemetry, but **without its cause**: after a 401, `/arka decisions` shows `http-401 — 1` and then `backoff — N` for an hour, without saying why. Rotating the key does not reopen the breaker either (up to 1 h blocked with a good key). | minor | R / A09:2025 | Probe in a temporary HOME: turn 1 `http-401`, turn 2 `backoff`, `net called turn2: False`. CLI `telemetry_cli today` → `http-401 — 1`, `backoff — 1`, exit 0. | **Recommendation** (engine): `reason=f"backoff:{blocked()}"`. In `trip`, store a key fingerprint (sha256[:8]) and make `blocked()` ignore the state when the current key differs. | Gabriel (engine) / Bruno (backoff, in a later PR) |
| 8 | **`~/.arkaos/egress/audit.jsonl` has no rotation.** Every `evaluate` fsyncs one line, cache hits included, because `prepare_state` runs before the cache. The degraded path (item 2) writes 2 lines. | minor | D / A09:2025 | `grep -n "rotate\|max_bytes" core/egress/audit.py` → exit 1 (no rotation). Measured line: 299 bytes. | **Recommendation**: `rotate_if_oversized` (`core/shared/telemetry_rotate.py`, 10 MB), keeping N generations, since the audit is evidence. Expected growth: ~300 turns/day ≈ 90 KB/day ≈ 33 MB/year in PR1. With the command sites of later PRs (~2000 calls/day) it reaches ≈ 220 MB/year. | owner of `core/egress` (Carlos/Paulo) |
| 9 | `decisions.jsonl` is created with 0644 (readable by other users on the machine). It stores `state_sha16`, an unsalted sha256 of the state: for short prompts ("yes", "continue") it works as a confirm-a-guess oracle. The cache file names (unsalted sha256 of the redacted state) have the same issue, but their directories are 0700. | minor | I / A04:2025 Cryptographic Failures / CWE-759 | `stat -f %Lp …/t.jsonl` → `644`. | **Recommendation** (telemetry.py): create the file with 0600, as `core/egress/audit.py` does. Use an HMAC keyed with the per-install audit salt (`load_or_create_salt`) for `state_sha16`. | Gabriel (telemetry) |
| 10 | The shadow spool stores the state **before** `prepare_state` (unredacted, secrets included if present) for a few seconds. | info (accepted by design) | I | Existing test: files 0600, spool 0700; `main` refuses paths outside the spool (exit 2), including a symlink pointing outside (new test); sweep > 1 h; cap 8; `Popen(start_new_session=True)`, stdio DEVNULL; `PYTHONPATH` derived from `__file__` (`paths.repo_root`); argv = `[sys.executable, -m, core.decisions.shadow, <uuid>.json]` (a new test proves `session_id` and state never reach argv). | Accepted. Redacting before spooling (evaluate + fsync inside the hook) costs more than it protects: this is the user's own data, in 0600 files that live for one turn. | — |
| 11 | The request said `DecisionUnavailable.detail` is cut at 300 chars, but **the code has no such cut**. Nothing is exposed: no path reads the error body. `detail` is `HTTPError.reason` (the status phrase), `str(URLError.reason)` or the exception name. | info | I | Probe: local server answering 401 with a body that echoes `Authorization` → `detail: 'Unauthorized'`, key absent from `str(e)`, `detail` and the formatted traceback. `repr(Transport)` has no key. A new test pins this. | No change. If the error body is ever read, cut it and run it through `secret_labels` before storing it. | — |
| 12 | Checks that passed: every send path goes through `prepare_state` (`grep post_decision(\|urlopen` → a single `urlopen` in `client.py`, called only by `engine._fetch`, after `prepare_state`; `replay.py` and the shadow worker enter through `run_sync` → `_fetch`); `data_collection=deny` in every payload (fixed `body_extras` in `resolve_transport`, checked on the sent body); `timeout` always passed (`call_args.kwargs == {"timeout": 1.5}`); `redact=False` still refuses secrets; home paths normalised (absolute, case-insensitive, and `~/`); the cache stores only responses (new end-to-end test through `run_sync`: no cache file holds the state marker, all are 0600), cache keys validated as sha256 hex by regex (traversal rejected), corruption = miss. | — | — | See "Verification". | — | — |
| 13 | **The release leak scanner never read `.jsonl` files**, so the preflight never scanned the replay corpora (`config/decisions/corpora/*.jsonl`), where prompts are the most likely place for a client name to land. | minor (blind spot in a release gate; the 4 corpora are clean today) | I / A09:2025 / CWE-200 | `_SCAN_EXTENSIONS` lacked `.jsonl`. After the fix, the real corpora scanned against the real config: `files 4 hits 0` (counts only). | **Fixed** in `core/governance/leak_scanner.py` (QA follow-up from Rita): `.jsonl` added, plus 2 tests in `tests/python/test_leak_scanner.py` (single file and directory walk). Note: files over 10 MB are still skipped silently (`_MAX_FILE_BYTES`); if corpora ever grow that large, report the skip instead. | Bruno (done) |
| 14 | **QG r1 B1: no wall-clock deadline on the call.** `urlopen(timeout=)` bounds each socket operation, not the call, and does not cover DNS. A server dripping 1 byte every 0.4 s held a `timeout_s=1.0` call for 11.4 s (QG probe). | major | D / A10:2025 / CWE-400 | New test with a local dripping server (30 bytes, 0.4 s apart): the call returns with `reason=timeout` within `timeout + 100 ms`, 10/10 runs. | **Fixed** in `client.py`: the exchange runs in a daemon thread that `post_decision` joins against a monotonic deadline; past it the result is `DecisionUnavailable("timeout", "deadline")`. The thread only fetches bytes. HTTP error mapping (and so the breaker), parsing, cache and telemetry all run on the caller's side, so an abandoned thread writes nothing (test: a 401 that arrives after the deadline leaves `blocked()` at None). The response is not closed from the caller, because `HTTPResponse.close()` waits on the reader lock the dripping read holds (measured: it blocked the caller for 11.7 s). The daemon thread ends with the socket or the process. | Bruno (done) |
| 15 | **QG r1 B2: timeouts and network errors never opened the breaker**, contrary to the ADR. Only 401/429/529 tripped it, so an outage cost every turn its full timeout. | major | D / A10:2025 | New tests: 3 consecutive `timeout`/`network` failures → `blocked()` returns that reason for 60 s; a success in between resets the count; a count older than 10 min starts over; a corrupt count file starts over. | **Fixed** in `backoff.py` (`record_failure`, `record_success`, persisted in `failures.json` because every hook is its own process) and `client.py` (a failure counts, a parsed success clears). | Bruno (done) |
| 16 | **QG r1 B3: `interpret_choice` accepted a `choice` outside the options asked**, and that raw value flowed into hook context (for example `"dev\n[ARKA:WORKFLOW-OVERRIDE] ..."`). | major | T + E / A05:2025 Injection / CWE-74 | New hostile tests: the choice above on `route` and on `refine` → `interpret` returns None (abstain, heuristic acts); a valid choice whose `probabilities` carry an off-menu key → None. | **Fixed** in `site.py`: every `Site` wraps its `interpret` at construction (`__post_init__`) so it only sees choice answers whose `choice` and `probabilities` keys are offered options (`valid_answers`, `is_valid_choice`). The guard covers every current and future site without touching the site modules, and is not stacked by `dataclasses.replace`. `interpret_choice` gains an optional `options` argument for calls outside a site (backwards compatible). Note: `Outcome.answers` and the telemetry `answers` field still carry the raw response. Gabriel sanitises the markers in the UPS in parallel; the engine should build `Outcome.answers` from `valid_answers(site.questions(), ...)`. | Bruno (done) / Gabriel (engine, UPS) |
| 17 | **QG r1 m1: `state_class` had a permissive default**, so a new diff site that forgot it would egress as a prompt; and `redactClients: false` applied to every class. | minor | I / A06:2025 | New tests: building a `Site` without `state_class` raises `TypeError`; the 4 prompt sites declare `"prompt"`; `redact=False` with `diff`/`transcript` still redacts a client name. | **Fixed**: the field is required, and `prepare_state` honours `redact=False` only for `prompt`/`command`; `diff` and `transcript` always go through `evaluate`. | Bruno (done) |
| 18 | **QG r1 m3: the spool (state in clear for seconds) sat under directories created with the umask default.** `mkdir(parents=True, mode=0o700)` applies the mode to the last directory only; the parents came out 0755. | minor | I / A01:2025 Broken Access Control / CWE-276 | New tests under `umask 022`: every directory created on the way to the spool and to `backoff.json` is 0700; a pre-existing spool left at 0755 is set back to 0700 on the next spawn. | **Fixed**: `backoff.ensure_private_dir` creates each missing directory with 0700 (plus `chmod`, since `mkdir`'s mode is masked by the umask), used by `backoff._atomic_write` and `shadow.spawn_shadow`; the spool is always set to 0700; `backoff.json` gets an explicit `fchmod(0600)`. The `fchmod` has no killing mutation because `mkstemp` already creates 0600; it is kept as an explicit guarantee. **Closed** for `cache.py` too: `cache._atomic_write` now uses `ensure_private_dir`, so every directory created up to the entry directory is 0700 (test under `umask 022`: `test_created_directory_chain_is_private` in `test_decisions_cache.py`). | Bruno (done) |

## Item 2 decision (missing redaction config)

**Fact (from execution):** `core.evals.sanitizer.sanitize_text` raises `SanitizerConfigMissing` whenever `load_redaction_patterns` returns `()`. That happens with no file, with `{"clients": []}` **and also** with a corrupt, unreadable or wrongly shaped file. Seeding an empty list in the installer does not help.

**Policy implemented (`privacy.py`):**

1. If `evaluate` denies with **exactly** `[redaction-config-missing]`, **and** the config is *truly* absent (no file, or a file that is exactly `{"clients": []}`), **and** the state class is `prompt` or `command`: the state leaves, with secrets refused (`secret_labels`) and the home normalised.
2. The override is written to the egress audit (`allowed: true`, `override: "redaction-config-missing:<class>"`). **If the audit write fails, nothing is sent.**
3. It is recorded **once per session** in telemetry (`site="egress-notice"`, `reason="redaction-config-missing"`, `fallback_used=false`), guarded by an `O_EXCL` marker under `<cache>/notices/`.
4. **Still fail-closed:** the `diff` and `transcript` classes and any unknown class; a corrupt, unreadable or wrongly shaped file (the operator has a list we cannot read, and degrading would send their client names in clear); a dangling symlink; and `redaction-failed`.

**Threat model (STRIDE, Information Disclosure):** the redaction list protects the confidentiality of *third parties*, the operator's clients. An npm user without a list has no identifier to protect, so fail-closed protects nothing there and turns the feature off. The state of a prompt or command is the user's own text, sent with their own key to the provider they configured, with `data_collection=deny`. The high-impact classes that apply to every user (secrets, home paths) are still enforced. Diffs and transcripts carry third-party material (client code, tool output), so they stay fail-closed.

**Follow-up recommendation:** the installer should not seed `{"clients": []}`. It would only change the denial reason, and under this policy an explicit `[]` is already treated as absent. Onboarding (`/arka setup`) should ask for the list when the user turns on the diff or transcript sites.

## Actions outside my scope (all four closed)

| Action | Why | Owner |
|---|---|---|
| `engine._fetch`: `prepare_state(state, redact=redact, state_class=strictest_state_class(c.site.state_class for c in calls), session_id=session_id)` | Without `session_id`, "once per session" becomes "once per install". Without `state_class`, everything counts as `prompt`. **This blocks any PR that registers a diff or transcript site**; PR1 is safe because it only has prompt sites. | Gabriel |
| `tests/python/test_decisions_engine.py::test_egress_denial_is_a_fallback` now fails: it encodes the old behaviour (missing config → denial on a prompt site). Fix validated by execution on a temporary copy: replace `.unlink()` with `.write_text("{nope", encoding="utf-8")` (a corrupt config stays fail-closed) → `1 passed`. | This is the intended consequence of the item 2 decision. | Gabriel |
| Update ADR `2026-09-23-jev-decisions-layer.md` (the "Consequences" bullet on the new egress path, originally "fail-closed on a missing redaction list") with the per-class policy. | The ADR contradicted the code. Done: now lines 244-264 of the ADR. | Paulo |
| Field `Site.state_class` in `site.py`. First pass: defaulted to `"prompt"`. After QG round 1 (m1): **required, no default** (`field(kw_only=True)`), and the 4 prompt sites in `sites/prompt.py` declare `state_class="prompt"`. I touched only those four `state_class=` lines in that file. | Heads-up for Gabriel, who edits the refine instructions in `sites/prompt.py` in parallel. | Gabriel |

## Mutation proof (each new test has a mutation that kills it)

Scratchpad script: apply the mutation, run the test file, restore the original file. The restore was verified: the suite passes again.

| Mutation | Tests that kill it |
|---|---|
| M1 remove `and _config_absent()` | `test_unreadable_config_is_not_degradable[×4]`, `test_dangling_config_symlink_is_not_absent`, `test_config_dir_unreadable_is_not_absent` |
| M2 cap before the checks | `test_cap_never_splits_a_client_name`, `test_cap_never_ships_a_secret_prefix`, `test_scan_cut_drops_the_straddling_tail` |
| M3 `diff` in `DEGRADABLE` | `test_missing_config_diff_transcript_unknown_fail_closed[diff]` |
| M4 degraded path without the secret refusal | `test_missing_config_still_refuses_secrets` |
| M5 ignore the audit result | `test_missing_config_prompt_and_command_still_leave[×2]`, `test_missing_config_without_audit_does_not_leave`, `test_audit_path_failure_does_not_leave` |
| M6 marker without `O_EXCL` | `test_missing_config_noted_once_per_session` |
| M7 no `_TAIL_GUARD` | `test_scan_cut_drops_the_straddling_tail` |
| M8 `Authorization` as a normal header | `test_redirect_never_carries_the_key_nor_its_answer`, `test_key_is_an_unredirected_header` |
| M9 no final-URL check | `test_redirect_never_carries_the_key_nor_its_answer` |
| M10 no sweep on spawn | `test_stale_orphans_do_not_block_shadow_forever` |
| M11 no cap in `blocked()` | `test_planted_far_future_deadline_neither_blocks_nor_sticks` |
| M12 no cap in `trip()` | `test_trip_is_capped_at_an_hour` |
| (M13 `_unredacted` without the audit) | `test_redaction_disabled_is_still_audited` (by construction: it is the only audit write when `redact=False`) |
| M14 remove `.jsonl` from `_SCAN_EXTENSIONS` | `TestJsonlCorpora::test_jsonl_file_is_scanned`, `TestJsonlCorpora::test_jsonl_found_when_walking_a_directory` (run: `2 failed`) |
| R1 (B1) call `_send` directly, without the deadline | `test_dripping_server_is_cut_at_the_deadline`, `test_abandoned_call_never_trips_the_breaker_late`, and every HTTP-mapping test in the file |
| R2 (B1) map HTTP errors (trip the breaker) inside the worker thread | `test_abandoned_call_never_trips_the_breaker_late` |
| R3 (B2) drop `record_failure` in `post_decision` | `test_three_timeouts_open_the_breaker_and_success_resets` |
| R4 (B2) drop `record_success` in `post_decision` | `test_three_timeouts_open_the_breaker_and_success_resets` |
| R5 (B2) no `trip` at the failure threshold | `test_three_transport_failures_trip_and_success_resets` |
| R6 (B3) no `interpret` guard in `Site.__post_init__` | `test_hostile_route_choice_abstains`, `test_off_menu_probability_keys_abstain`, `test_hostile_refine_gap_abstains`, `test_guard_is_not_stacked_by_replace` |
| R7 (B3) ignore `probabilities` keys | `test_off_menu_probability_keys_abstain` |
| R8 (m1) default `state_class="prompt"` | `test_state_class_is_required` |
| R9 (m1) honour `redact=False` for every class | `test_redaction_disabled_never_applies_to_diff_or_transcript[diff, transcript]` |
| R10 (m3) `mkdir(parents=True)` for `backoff.json` | `test_created_directory_chain_is_private` |
| R11 (m3) `mkdir(parents=True)` for the spool | `test_spool_chain_is_private` |
| R12 (m3) no `chmod` of a pre-existing loose spool | `test_spool_chain_is_private` |
| R13 (m3) `mkdir(parents=True)` in `cache._atomic_write` | `test_created_directory_chain_is_private` (`test_decisions_cache.py`) |

Regression tests without a dedicated mutation: `test_full_path_never_writes_the_state_to_disk` (cache; the property already held, and the test pins it end to end), `test_error_body_is_never_read_into_the_exception`, `test_worker_refuses_symlink_escaping_spool`, `test_popen_argv_has_no_caller_input`, `test_state_file_is_private`, `test_fictional_clients_redacted_in_structured_state`, `test_empty_client_list_is_treated_as_missing`, `test_client_list_present_writes_no_notice`, `test_strictest_state_class`.

## Verification (first pass; superseded by "Status at merge")

- `~/.arkaos/bin/arka-py -m pytest tests/python/test_decisions_*.py -q --cov=core.decisions --cov-fail-under=80` → **exit 1**: 211 passed, 1 failed. The only failure is `test_decisions_engine.py::test_egress_denial_is_a_fallback`, which is expected and has an owner (see above). Total coverage 98.47% (privacy 100%, client 100%, transport 100%, site 100%, shadow 97%, cache 93%, backoff 91%).
- The 6 test files in my scope (`privacy, client, shadow, backoff, cache, transport`) → **exit 0**, all passing.
- `tests/python/test_ups_decisions.py` → 27 passed.
- `~/.arkaos/bin/arka-py -m pytest tests/python/test_leak_scanner*.py -q` → **exit 0**, 17 passed.
- `~/.arkaos/bin/arka-py -m ruff check core/decisions <the 5 changed test files>` → **exit 0** ("All checks passed!"). In this shell `ruff` only exists inside the venv. `ruff` on `leak_scanner.py` and `test_leak_scanner.py` reports 2 fixable findings (UP035, I001) that already exist at `HEAD` (checked with `git show HEAD:<file>`); they are not from this change.
- `mypy` on the touched modules → 0 errors in `core/decisions/` (the 257 errors reported come from other packages).
- Redirect test run 30 times in a row → 30/30 passed. An earlier version failed intermittently on a socket RST, because the server closed the connection with the body unread; fixed in the test itself.

## Status at merge (2026-09-23)

Numbers after the Quality Gate round 1 fixes, all from runs on this branch:

- `~/.arkaos/bin/arka-py -m pytest tests/python/test_decisions_*.py -q --cov=core.decisions --cov-fail-under=80` → **exit 0**, 243 passed. Total coverage 98.63% (client 100%, privacy 100%, site 100%, shadow 97%, backoff 95%, cache 93%). The engine test that failed at the first pass (`test_egress_denial_is_a_fallback`) was updated by its owner and passes.
- `~/.arkaos/bin/arka-py -m ruff check core/decisions tests/python/test_decisions_*.py` → **exit 0**.
- `mypy core/decisions` → 0 errors in `core/decisions/`.
- `tests/python/test_ups_decisions.py` → 35 passed.
- `tests/python/test_decisions_client.py` 10 runs in a row → 10/10 passed (the dripping-server test included).
- Mutations: M1-M14 (first pass and QA follow-up) and R1-R13 (QG round 1) each killed by at least one test.
- Open recommendations (not blocking, owners in the findings table): 7 (key fingerprint only; the cause already rides as `backoff:<cause>`, `engine.py:198-200`), 8 (egress audit rotation), 16 (off-menu text on non-choice or unasked answers still reaches `decisions.jsonl`; QG round 2 m4, carried to PR2). Finding 9 is fixed (`telemetry.py:97-99` creates the file with the 0600 mode set at `telemetry.py:46`; `telemetry.py:81-93` computes `state_sha16` as an HMAC-SHA256 keyed by the per-install salt). Finding 18 is closed.


## PR2 (2026-09-23)

- **Branch:** `feature/jev-decisions-2` (PR1 merged in `6e4ad3eb`)
- **Reviewer:** Bruno (security-eng), lane C, dispatched by Paulo
- **Scope:** `core/egress/{audit,policy,credentials}.py` (changed or new) and, after Quality Gate round 1, `core/decisions/privacy.py` (raw-leaf scan, finding 23); read-only review of `core/decisions/backoff.py`, `core/decisions/transport.py`, `core/decisions/client.py` and the new `core/decisions/sites/command.py` (work in progress in the `core/decisions` lane, not committed yet)
- **Frameworks:** STRIDE, OWASP Top 10:2025, CWE, NIST CSF 2.0 PR.DS (data security) and DE.AE (audit evidence)
- **Verdict for this lane:** APPROVED-WITH-FIXES. The two majors of the first pass (19, 20) and the carried finding 7 are fixed: R-P1, R-C1 and R-B1 landed in the `core/decisions` lane and their strict-xfail markers were removed with them. Quality Gate round 1 rejected PR2 on two blockers in this lane (findings 23, 24); round 2 on a blocker present since round 1 (finding 25, a secret glued to `;`, `)` or `,`) and a major (finding 26, httpie basic auth); round 3 on a major (finding 27, credentials holding `[ ] { }`, even single-quoted); round 4 on a major (finding 29, an apostrophe in the prose flipping the quote reading) and on finding 28 omitting a residual class. All six are fixed; the round-4 fixes also closed an escaped-quote leak found while fixing (finding 30) and a userinfo `@` leak (finding 31). Round 5 rejected on a major introduced by the finding 29 fix: the scan went quadratic on a JSON-serialised paste (finding 32, fixed, together with two older quadratic regex windows found in the same sweep; a 203 KB serialised paste now scans in 0.10 s, down from 15.62 s). Finding 19 is **Fixed on the probed forms**: 57 forms x 14 suffixes x 3 paths (2394/2394 refused), 60 reviewer rows (47 reviewer shapes verbatim plus 13 round-4 rows) x 3 paths (180/180 refused) and 84 benign forms x 3 paths (252/252 sent). Regex detection cannot prove absence; finding 28 states the residual. The probe is a parametrized test in the repository, not a scratchpad script.

Every piece of evidence comes from execution in a temporary HOME with fictional client names (`acmecorp`, `betacorp`). Secret values in tests are built at runtime from synthetic fragments. The operator's redaction config was never read.

### Findings

| # | Finding | Sev. | STRIDE / OWASP / CWE | Evidence (command → result) | Fix or recommendation | Owner |
|---|---|---|---|---|---|---|
| 8 | `audit.jsonl` had no rotation (PR1, carried). | minor | D / A09:2025 | `test_oversized_file_rotates_exactly_once`: file over the cap → one `.1`, prefill intact, the next line in a fresh file. | **Fixed** in `audit.py`: `rotate_if_oversized(path, AUDIT_MAX_BYTES)` before each append, same flock (`audit.jsonl.rotlock`) and `.1` generation as the telemetry. The cap is a 10 MB constant, not the `ARKA_TELEMETRY_MAX_BYTES` knob: a cap of 1 byte would overwrite `.1` on every write and erase the evidence. Rotation is never a gate: a helper that raises is suppressed, and if the file is still over the cap after the attempt the line is written in place with `"rotation": "failed"`. The fail-closed rule for the write itself is unchanged (`test_rotation_failure_still_fails_closed_when_write_fails`). | Bruno (done) |
| 19 | **Command-line credentials without a vendor prefix left in clear.** `secret_labels` only knows prefixed keys (`sk-ant-`, `AKIA`, `xoxb-` ...). Probe on `prepare_state({"command": ...}, state_class="command")`: `export API_TOKEN=<opaque>`, `curl -H 'Authorization: Bearer <opaque>'`, `mysql -p<password>`, `https://user:<password>@host`, `PGPASSWORD=<opaque> psql` → **all five SENT**, with `redact=True` and with `redact=False`. | **major** | I / A04:2025 Cryptographic Failures (exposure of credentials) / CWE-200, CWE-522 | Probe before the fix: 10/10 sent. After: `redact=True` 5/5 `egress-denied:secret`; `redact=False` still 5/5 sent (see R-P1). | **Fixed on the probed forms** (findings 23-27; the probe and its limits are in the table below and in finding 28). On the policy path: new `core/egress/credentials.py` detects credentials by context (secret-named assignment, `--password`/`--token`/`--api-key` flag, `Authorization`/`X-Api-Key` header, `curl -u user:pass`, `mysql -p…`/`sshpass -p`, URL userinfo), and rejects shell expansions, placeholders and, for assignments, paths, URLs and calls. What the detectors reject, exactly (since round 3): `$` or a backtick in a bare or double-quoted value (substitution: the text names the secret); a single-quoted value only when it is nothing but `$NAME`, `${NAME}` or `$(…)`; for bare assignment and flag values, a code shape (`name[…]` or `name{…}` running to the end of the value, a call `fn(`); paths, URLs, placeholders and values that do not look like a token. `[ ] { }` are not rejected. History: up to round 2, `_REFERENCE` rejected ANY value holding `$(){}[]<>,;` or a backtick (the cause of finding 25); round 2's `_is_expansion` still rejected a bare value holding `[ ] { }` and applied that bare rule to fragments inside single quotes (the cause of finding 27). A value that starts inside quotes runs to the closing quote; a bare value ends at whitespace, quotes and `; & \| ( ) < > ,` and backtick. `policy.evaluate` now uses `egress_secret_labels` (prefixes + context). Labels only reach the audit, never values (`test_policy_denies_a_bearer_header_and_audits_no_value`). **Fixed (R-P1)** for the two paths that skip the policy (`redactClients:false` and the missing-config degrade): `privacy.py:45` imports `egress_secret_labels` and `_refuse_secrets` calls it at `privacy.py:200`. | Bruno (policy) / `core/decisions` lane (R-P1), both done |
| 20 | **`command_state` caps at 4000 chars BEFORE the privacy checks**: the PR1 finding 2 (M2) shape again, one layer up. A client name or a key straddling the cut ships as a fragment that no longer matches its pattern. | **major** | I / A06:2025 Insecure Design / CWE-200 | `prepare_state(command_state("echo " + "a"*3991 + " acmecorp"))` → SENT, tail `'… acm'`; same with a Slack-shaped key → SENT, tail `'… xox'`. With the full string: the name is redacted, the key is denied. | **Fixed (R-C1)**: `command_state` cuts at the last whitespace before the limit and drops the token the cut would split (`sites/command.py:31-37`). Pinned by `test_command_cap_never_ships_a_split_client_name` and `test_command_cap_never_ships_a_secret_prefix`, now plain tests. | `core/decisions` lane, done |
| 7 | After a 401 the breaker stays open for up to 1 h even when the operator rotates the key (PR1, carried). `backoff:{cause}` (`engine.py:206-210`) fixes the observability half only; recovery is unchanged. | minor | D / A09:2025 | Reading `client.py:146` (`trip("http-401", 3600)`) and `backoff.blocked()`: no key input. | **Fixed (R-B1)**: `transport.key_fingerprint()` (`transport.py:61`); a 401 trip stores it (`backoff.py:116`); `blocked()` ignores a block earned by another key (`backoff.py:97`), and `trip()` keeps a later deadline only when the same key set it (`backoff.py:111`, `_set_by_other_key` at `backoff.py:121-123`). Pinned by `test_rotating_the_key_reopens_a_401_breaker` and `test_non_401_trips_are_not_key_bound` (`test_decisions_backoff.py`, 15 passed). | `core/decisions` lane, done |
| 16 | Off-menu text on non-choice or unasked answers reaching `decisions.jsonl` (PR1 QG r2 m4, carried). | minor | T / A05:2025 / CWE-74 | Working tree of the `core/decisions` lane: `site.clean_answer` drops `choice` on non-choice questions and `score` on non-score ones; `engine.valid_site_answers` feeds `Outcome.answers`, which is what telemetry records (`engine.py:368`). Not committed yet, so not verified here by test. | **In progress** in the `core/decisions` lane. To verify at the Quality Gate with a hostile non-choice answer that carries a `choice` string. | `core/decisions` lane |
| 21 | Audit retention is one kept generation: about 20 MB, about a month at the PR2 rate (~2000 calls a day, ~300 bytes a line). Older lines are dropped. | info | R / A09:2025 | Arithmetic from finding 8's measured line size. | Accepted for PR2: the lines carry only digests and hashed labels, so their evidence value is correlation within a recent window. If a longer window is ever required, add N generations under the same lock in `telemetry_rotate`, not a second mechanism. | — |
| 22 | With `redactClients: false`, a command naming a client leaves with the name in clear (probe: `cd <home>/Work/acmecorp-api` → sent as is). | info | I / — | Probe above, `redact=False` row. | By design (operator's explicit choice, audited as `override=redact-disabled`). Secrets are still refused once R-P1 lands. No change. | — |
| 23 | **QG r1 B1 (blocker): quoted credentials hidden by JSON escaping.** `prepare_state` ran `json.dumps` before the secret checks, so `API_TOKEN="v"` became `API_TOKEN=\"v\"` and the value patterns captured only the backslash. `export API_TOKEN="…"`, `PGPASSWORD="…" psql`, `curl -s -u "bob:…"` and `tool --password "…"` were labelled on the raw text and `[]` on the serialised text, so they were SENT on all three paths. Reproduced by Marta and Francisca. | **blocker** | I / A04:2025 / CWE-200, CWE-522 | Probe (temp HOME, the 4 commands, `redact=True`, `redact=False`, missing config): after the fix, 12/12 REFUSED. | **Fixed** in `privacy.py`: `prepare_state` first calls `_refuse_leaf_secrets(state)` (`privacy.py:72`), which runs `egress_secret_labels` on every raw string of the state before serialisation: dict keys and values, list items, and `str()` of any non-JSON leaf, exactly what `json.dumps(default=str)` would ship (`privacy.py:154-195`). The walk is iterative (a 5000-deep list does not overflow the stack) and bounded by `MAX_SCAN_CHARS`. The scan of the serialised text stays as the second layer. The four commands are rows in `SECRET_COMMANDS`, run on the three paths. | Bruno, done |
| 24 | **QG r1 B2 (blocker): gaps in the credential patterns.** (a) `_ASSIGNMENT` required a character before the secret word, so a bare `TOKEN=`, `PASSWORD=`, `KEY=`, `SECRET=` or `export token=` passed; (b) no query-string detector (`?token=`, `&sig=` …); (c) `mysql -p'pw'` and `-p"pw"` passed; (d) `DB_PASS=`/`PASS=` (the word list had no `PASS`), `docker login -p`, `redis-cli -a` and `aws configure set aws_secret_access_key <v>` passed. | **blocker** | I / A04:2025 / CWE-522 | Same probe, 19 commands × 3 paths: after the fix, 57/57 REFUSED. | **Fixed** in `credentials.py`: (a) the name takes zero or more characters before the word, with the pointer-suffix guard kept; (b) `_QUERY` for `access_token`, `refresh_token`, `id_token`, `client_secret`, `api_key`, `token`, `key`, `password`, `passwd`, `pwd`, `secret`, `sig`, `signature`, `auth`; (c) the password flags take a quoted value; (d) the word list has `PASS` and `PWD`, and `_PASSWORD_FLAGS` covers `docker`/`podman`/`nerdctl login -p` and `redis-cli -a`; `_CLI_SETTING` covers `aws configure set` for the secret key and the session token. The widening brought one false positive (`COMPASS_URL=http://…`); URL values and the `_URL`/`_URI`/`_HOST`/`_ENDPOINT` suffixes are now excluded, since credentials inside a URL are the userinfo and query detectors' job. One regression row per case, and 19 rows of ordinary commands pin the false-positive side. | Bruno, done |
| 25 | **QG r2 B3 (blocker): a secret glued to `;`, `)` or `,` left in clear on all three paths.** Reproduced by Francisca and Marta: `export API_TOKEN=abc123XYZ99; ./deploy`, `export TOKEN=…; ./d`, `mysql -uroot -p…;`, `(export API_TOKEN=…)`, `PGPASSWORD=…,psql`, `curl ?token=…;`, `sshpass -p …;ssh h`, `docker login -p …;`, `Authorization: Bearer …;`. Present since round 1 and missed by every reviewer, me included: my probes never glued a separator to a value. | **blocker** | I / A04:2025 / CWE-200, CWE-522 | Probe after round 2: 44 leak forms x 14 suffixes x 3 paths → 1848/1848 REFUSED; re-run inside the round-3 set (table below). | **Fixed** in `credentials.py`, two independent changes. (1) Every bare value group ends at the shell metacharacters (`_STOP`: whitespace, quotes, `; & \| ( ) < > ,`, backtick) in `_VALUE`, `_AUTH_HEADER`, `_KEY_HEADER`, `_BASIC_FLAGS`, `_QUERY` (atomic groups, so the regex cannot back off a character). (2) `_REFERENCE` is replaced by a quote-aware rule (refined again in round 3, finding 27): a `;` or `,` inside quotes is data, not a reference, so `export TOKEN="ab;cd…"` is caught too. **Deliberate deviation for `_USERINFO`:** it is bounded by `:` and `@`, not by shell syntax, so the terminator class there would make a password containing `;` or `)` leak; mutation U below proves it (43 tests fail when the proposed class is applied to it). Userinfo keeps its class and relies on (2). A call guard (`fn(x)`, `cfg[k]` after a bare value is code) and "space is not a symbol" in the opacity rule keep the widening off code and prose. | Bruno, done |
| 26 | **QG r2 M1 (major): httpie basic auth not detected.** `http --auth bob:pw` and `http -a bob:pw` (also `https`, `xh`, `xhs`) were sent on all three paths, while `curl -u`, the same shape, was caught. | major | I / A04:2025 / CWE-522 | Probe: the 3 httpie forms x 14 suffixes x 3 paths → 126/126 REFUSED. | **Fixed**: `_BASIC_FLAGS` gains a pattern for `-a`/`--auth` (`=` or space) only after `http`, `https`, `xh` or `xhs`. Negative rows: `ls -a /tmp`, `git commit -a -m …`, `http GET …` without auth, `http -a $USER:$PASS …`, `http --auth-type=bearer …`. | Bruno, done |
| 27 | **QG r3 M2 (major): credentials holding `[ ] { }` left in clear, even single-quoted.** Reproduced by Francisca and Marta: `curl -u 'admin:Xk9]pL2vQ' …` SENT (without the `]`: REFUSED), `-H 'Authorization: Basic Xk9{pL2vQ}'`, `'…?api_key=Xk9]pL2vQ'`, `git clone 'https://bob:Xk9]pL2vQ@…'`, `http -a 'bob:Xk9]pL2vQ'`, and the bare `API_TOKEN=abc123XYZ99}`, `export API_TOKEN=abc[123]XYZ`. Cause: the sub-token detectors captured a fragment inside the quoted word, and the round-2 rule treated `[ ] { }` in a bare value as expansion. This contradicted the docstring and finding 19, which said a single-quoted value never counts as an expansion. | major | I / A04:2025 / CWE-522 | Probe: the 5 M2 forms, the 2 bare forms and 6 more bracket/quote forms x 14 suffixes x 3 paths, plus the reviewers' 47 shapes x 3 paths → all REFUSED (table below). | **Fixed** in `credentials.py`: (1) `[ ] { }` are no longer expansion markers anywhere; only `$` and backtick are, and only outside single quotes (`_EXPANSION`). (2) `_quote_context` reads the quotes of the whole text once, as a POSIX shell does, and a sub-token value that starts inside quotes runs to the closing quote. So `'admin:ab;cd…'` and `'admin:Pa$$w0rd…'` are caught, which neither round-2 rule caught. Inside double quotes the extension stops at whitespace, quotes and backslashes, so the JSON-serialised layer does not run a value into the rest of the command. (3) The bracket rule is now only a code guard for bare assignment and flag values, and it is narrower than the one prescribed (`name[…]`/`name{…}` running to the end of the value). A blanket `[ ] { }` guard on bare assignments would have kept Marta's `API_TOKEN=abc123XYZ99}` and `abc[123]XYZ` leaking. (4) A single-quoted value that is nothing but a reference (`'Bearer $TOKEN'`) is still a reference. (5) Header values must look like a token, so `grep "Authorization: Bearer" logs/` and `grep -n "X-Api-Key: missing header"` stay benign (the probe found both as false positives while the fix was in progress). The guard in (3) has a leak side: a bare value that is a secret shaped like code is SENT; finding 28 lists it as a residual class. The whole-text quote reading in (2) was itself defeated by an unmatched apostrophe earlier in the text (`can't`), so `'admin:Pa$$w0rd…'` after one was SENT until round 4; that is finding 29, **fixed**. | Bruno, done |
| 28 | **Residual: regex detection cannot prove absence.** Every independent probe so far found one more class (round 1: quoting under JSON; round 2: glued separators, httpie; round 3: brackets and quote context; round 4: an apostrophe flipping the quote reading, escaped quotes found while fixing it, and an unencoded `@` in userinfo (finding 31); round 5: a scan-cost class, the N1 fix made the scan quadratic (finding 32)). The residual is bounded by the probe set, listed in `tests/python/test_egress_credentials.py`: `LEAK_FORMS` (57), `SUFFIXES` (14), `REVIEWER_LEAKS` (60), `BENIGN_FORMS` (84), `PATHS` (3). Known and accepted inside that bound: (a) a password shorter than 6 characters; (b) a password with spaces in an unquoted or double-quoted `-u`; (c) credential syntaxes of tools without a detector (a new CLI flag, a config file echoed into a command); (d) **the unquoted code-shape class (leak side of finding 27(3)):** a bare assignment or flag value shaped `name[…]` or `name{…}` running to the end of the value is read as code and SENT. Executed on this tree, all SENT: `API_TOKEN=tok9{a}`, `API_TOKEN=abcdef9[1]`, `API_TOKEN=abc123[XYZ99`, `SECRET=f00[bar]`, `TOKEN=Abc9{x}`, `token=secret9[0]`, `--password Hunter2[x]`, `--token=Qx9{abc}`; `API_TOKEN=x9[a]` is SENT too, but by (a) first (5 characters). Quoted, code-shaped values are refused (`export API_TOKEN='x9[ab]'`, `--token "Qx9{abc}"`, both in the probe). The guard exists because diffs are full of `token = tokens2[0]` and `cfg_key = cfg[k1]` (15 false refusals when it is removed, mutation Mf); (e) escaping nested deeper than 8 levels: `_escape_layers` stops at 8 un-escapes (a work bound, see finding 30); the probe goes to 3; (f) on the benign side, a false refusal: `echo it's; curl -H "Authorization: Bearer $TOKEN" x` is refused, because the whole-text reading puts `$TOKEN` inside the apostrophe's quote and a refusal wins when either reading says literal (finding 29); its sibling `I can't: echo "$HOME" && http -a "$U:$P" x.io` is refused for the same reason (`['basic-auth flag']`, executed on this tree); (g) the work bounds of finding 32: a password flag more than 512 characters after its tool's name (`mysql … -p`, `docker login … -p`, `redis-cli … -a`, `http … -a`) is not read, and a quoted value is read to 256 characters past its regex match (a longer value is still refused: its head decides the length and placeholder tests, and since PR2 round-6 m2 a value cut at the bound gets no reference exemption; rows in `test_bounded_reads_keep_long_values` and the three m2/m3 rows of `test_egress_credentials.py`). N1 (finding 29) is fixed, not accepted. | residual | I / A04:2025 | The probe table below; the test file is the list. The (d) rows were run through `egress_secret_labels` on this tree: 9/9 `[]`. | Accepted for PR2. Every new class goes into the probe set before its fix, and the probe runs in the suite. (d) and both shapes of (f) go to PR5; a candidate for (d), not yet tried, is to read a code shape as code only when the name before the bracket also appears elsewhere in the text (`core/decisions` hardening). | Bruno (security-eng), 2026-09-23; (d), (f): PR5 |
| 29 | **QG r4 N1 (major): an unmatched apostrophe earlier in the text flipped the whole-text quote reading, so a single-quoted value holding `$` or a backtick was read as a variable reference and SENT.** Reproduced by Paulo: `egress_secret_labels("I can't log in with curl -u 'admin:Summer$2026x' https://api.example.com, why?")` → `[]` (with "cannot": `['basic-auth flag']`); also `[]`: `it's: http -a 'bob:Pa$$w0rd9' x.io`, `can't auth: curl -H 'Authorization: Bearer ab$c9Zq9k7k7' x`. SENT on the redacted, redaction-disabled and config-missing paths. Prompt states are English prose full of apostrophes. | major | I / A04:2025 / CWE-522 | After the fix, the 3 shapes → `['basic-auth flag']`, `['basic-auth flag']`, `['auth header']`; in `REVIEWER_LEAKS`, 9/9 refused (3 shapes x 3 paths), plus one row in `test_decisions_privacy_command.py` x 3 paths. | **Fixed** in `credentials.py` (Francisca's proposal, deny-leaning): for the sub-token detectors, `_readings` returns the whole-text reading AND a local one (`_local`), and a refusal wins when EITHER is literal. The local reading takes the nearest quote before the value on its line; if that quote opens a word (line start, whitespace or `=` before it) and the same quote character appears later on the line, the value is re-read under that quote and runs to the closing quote (`_extend`). Round 4 did this by re-reading the rest of the line (`_quote_context(text[q:le])`), which finding 32 replaced with bisects on the quote and newline marks. The either-rule is required, not a choice: `echo don't -H "Authorization: Bearer ab$c…" x'` is one the shell really single-quotes from the apostrophe, so there the local `"…"` reading is the wrong one (row in the probe; mutation R6). m4 (`echo it's; curl -H "Authorization: Bearer $TOKEN" x`) is **not** closed: the local reading says reference, the whole-text one says literal, and the either-rule refuses. It stays on record as a benign-side residual (finding 28(f)). | Bruno, done |
| 30 | **Found this round (major): a string state holding escaped quotes (`\"`) was SENT, raw and serialised.** Francisca's round-4 note said such a string is scanned only by the serialised layer. Executed, neither layer caught it: `prepare_state({"command": 'export API_TOKEN=\"<opaque>\"'})` SENT with `redact=True` and `redact=False`; same for `curl -u \"bob:<pw>\" x` and a JSON document carried as a string. The realistic shape is nested shell quoting: `ssh host "export API_TOKEN=\"<opaque>\" && ./run"`, `bash -c "curl -u \"bob:<pw>\" x"`, `bash -c "mysql -p\"<pw>\" app"`, all `[]` before the fix. Cause: read as written, the value is a lone backslash (under 6 characters). | major | I / A04:2025 / CWE-522 | After: the 4 escaped shapes (`ssh host "export API_TOKEN=\"…\" && ./run"`, `bash -c "curl -u \"bob:…\" x"`, `bash -c "mysql -p\"…\" app"`, `export API_TOKEN=\"…\"`) + a 2-level `bash -c "ssh h \"export API_TOKEN=\\\"…\\\"\""` in `REVIEWER_LEAKS` (15/15 refused over 3 paths); `test_every_escape_layer_is_scanned[0..3]` (0 to 7 backslashes before the quote) at the detector; one row in `test_decisions_privacy_command.py` x 3 paths. | **Fixed** in `credentials.py`: `credential_labels` scans every escape layer (`_escape_layers`: the text, then successive un-escapes of `\"`, `\'` and `\\` until nothing changes, at most 8), and a label on any layer refuses. The benign side holds: `ssh h "export API_TOKEN=\"\$API_TOKEN\""` is sent (a `\$` is not un-escaped, and the value stays a reference). | Bruno, done |
| 31 | **QG r4 m3 (minor): an unencoded `@` inside a URL userinfo password was SENT.** `git clone 'https://bob:p@ss{w0}rd@g.io/r'` → `[]`. RFC 3986 §3.2.1 wants a literal `@` in userinfo percent-encoded, but a typed `p@ss` is still the password, and what leaves is the secret whatever the RFC says. | minor | I / A04:2025 / CWE-522 | After: that shape and the bare `git clone https://bob:p@ssw0rd9@g.io/r` → `['url userinfo']`, 6/6 refused over 3 paths. | **Fixed** (my call, rather than listing it): `_USERINFO` takes the password up to the LAST `@` of the authority (the class stops at `/ ? #`, where the authority ends). Inside quotes the extension would stop at the first `@`; `_extend` skips its stops inside the span the regex already matched (the `floor`), so the password runs to the last `@`; beyond the floor it still applies the double-quote stops (whitespace, quotes, backslash), which keeps `grep "Authorization: Bearer" logs/` benign on the serialised layer (mutations R7, R8). | Bruno, done |
| 32 | **QG r5 B1 (major): the credential scan was quadratic on a JSON-serialised paste, introduced by the finding 29 fix.** `_local` did O(line) work for every sub-token match: two `rfind` back to the line start, the slice `text[start:le]` and a pure-Python `_quote_context(text[q:le])` over the rest of the line. `privacy._serialise` runs `json.dumps` on the state, so newlines become `\n` and the serialised text is ONE line of up to `MAX_SCAN_CHARS` (1,000,000). `engine._fetch` runs `prepare_state` synchronously before the POST, so `timeout_ms` does not bound it: one large paste blows `ARKA_UPS_BUDGET_MS` (6000) and can pass the 20 s UserPromptSubmit ceiling. Reproduced by Marta, Francisca and Paulo with N lines of `curl -sS -H "Authorization: Bearer $TOKEN" "https://api.example.com/v1/items/{i}?page=1" \| jq .`, serialised: 49 KB 0.93 s, 98 KB 3.70 s, 198 KB 14.58 s (multiline 0.06 s); adversarial `('a' "-u x:y" )*n` 109 s at 195 KB. The same sweep found two older quadratic windows, not from round 5: the tool-argument window `[^\n|;&]*?` of the password-flag and httpie patterns (every `mysql`, `http`, `redis-cli`, `docker login` scanned the rest of the one-line text: 100 KB of `mysql x ` 6.0 s, `http x ` 8.1 s) and the userinfo scheme `\b[a-z][a-z0-9+.\-]*` (a start at every letter after `.` or `-`: 100 KB of `a.` 13.8 s, of `--a-` 6.9 s, the shape of a url-safe base64 blob). | major | D / A06:2025 / CWE-407 | Timings on this machine, `credential_labels` (before → after): serialised paste 50 KB 0.97 → 0.02 s, 101 KB 3.91 → 0.05 s, 203 KB 15.62 → 0.10 s; adversarial 100 KB raw 26.95 → 0.04 s, 195 KB raw 109 (Marta) → 0.07 s; `mysql x ` 100 KB 6.01 → 0.08 s; `a.` 100 KB 13.76 → 0.04 s; at the 1 MB cap: paste 0.50 s, `a.` 0.39 s, `mysql x ` 0.86 s, adversarial 0.68 s. `test_serialised_many_match_paste_scans_in_linear_time` (200 KB ≤ 1.0 s and ≤ 3x the 100 KB time + 0.05 s; round-5 code: 14.57 s, after 0.10 s) and `test_adversarial_text_scans_under_a_second` (8 shapes, raw and serialised). Equivalence: a differential fuzz of the round-5 module against this one, 400,000 random shapes from 35 credential-syntax tokens (raw and serialised), 0 label differences. | **Fixed** in `credentials.py`. (1) `_scan` indexes each layer once: the quote context plus the sorted positions of `'`, `"` and the newline; `_local` finds the nearest quote, the line bounds and the closing quote by bisect (O(log n)), with no slice and no re-read. (2) `_extend` no longer walks characters: in single quotes only `'` ends the value, in double quotes the stops (whitespace, quotes, backslash) are also where the quoted run can end, so the end is one stop-character search, read to at most 256 characters past the regex match (`_VALUE_CAP`). (3) The tool-argument window is `[^\n|;&]{0,512}?` (`_ARGS`). (4) The userinfo scheme is `[a-z][a-z0-9+.\-]{0,62}` without a word boundary, so a longer scheme matches on its tail (deny side). The bounds are residual 28(g). N1 and finding 30 stay closed: the 3 N1 shapes and the 3 nested-quoting shapes of finding 30 are refused raw and serialised; mutations R1 and R6 still kill (table below). | Bruno (security-eng), 2026-09-23 |

Blast radius of finding 19's fix: `policy.evaluate` is also the NotebookLM choke point (`core/kb/nlm_client.py`). A note that contains a literal credential is now denied there too. That is the deny direction, the finding kind is `secret` with a named label, and it is allowlistable per exact label like every other secret. `test_egress_policy.py`, `test_notebooklm_chokepoint.py` and `test_leak_scanner*.py` pass unchanged.

### Changes recommended to the other lanes (all three landed)

**R-P1** (`core/decisions/privacy.py`, 2 lines). `_refuse_secrets` must use the same vocabulary as the policy:

```python
from core.egress.credentials import egress_secret_labels   # replaces the secret_labels import
...
    if egress_secret_labels(text):                          # in _refuse_secrets
```

Landed at `privacy.py:45` and `privacy.py:200`. Before it landed, applying the swap through a pytest plugin flipped the 10 R-P1 xfails to XPASS(strict) while `test_decisions_privacy.py` still passed; the markers were removed with the fix.

**R-C1** (`core/decisions/sites/command.py`, `command_state`). Never cut inside a token; drop the token the cut would split:

```python
_TRUNCATED = " [truncated]"

def command_state(command: str) -> dict[str, Any]:
    if len(command) <= MAX_COMMAND_CHARS:
        return {"command": command}
    head = command[: MAX_COMMAND_CHARS - len(_TRUNCATED)]
    cut = max(head.rfind(" "), head.rfind("\n"), head.rfind("\t"))
    return {"command": (head[:cut] if cut > 0 else "") + _TRUNCATED}
```

Landed at `sites/command.py:21` and `:31-37` (the constant as shipped is `" [truncated]"`). Before it landed, patching this function in through a plugin flipped both R-C1 xfails to XPASS(strict); the markers were removed with the fix.

**R-B1** (`core/decisions/transport.py` + `backoff.py`, 9 lines). Tie a 401 trip to the key that earned it:

```python
# transport.py
def key_fingerprint() -> str:
    return hashlib.sha256(_resolve_key().encode("utf-8")).hexdigest()[:8]

# backoff.py — trip(): add the fingerprint for 401 only
    payload = {"reason": reason, "until": until}
    if reason == "http-401":
        payload["key"] = key_fingerprint()   # lazy import inside trip()
# backoff.py — blocked(): a 401 block earned by another key is not ours
    if state.get("key") and state["key"] != key_fingerprint():
        return None
# backoff.py — trip(): only keep a later deadline set by the SAME key
    if ... and until < current <= ref + MAX_BLOCK_S and _read().get("key") in (None, payload.get("key")):
```

The last line matters: without it, a 401 on the new key cannot land while the old key's longer block exists, so every turn pays a failing call for up to an hour. Eight hex chars of a SHA-256 of a high-entropy API key do not help anyone recover it (unlike the low-entropy client names, which need the salted HMAC). Landed at `transport.py:61` and `backoff.py:97, 111-123`, with the suggested tests (`test_rotating_the_key_reopens_a_401_breaker`, `test_non_401_trips_are_not_key_bound`).

### Mutation proof (PR2)

Scratchpad script: copy the file aside, apply the mutation, run the named test files, restore by copy. The suite passes again after every restore.

| Mutation | Tests that kill it |
|---|---|
| A1 no rotation call in `record` | `test_oversized_file_rotates_exactly_once`, `test_rotated_and_fresh_files_stay_private`, `test_rotation_crash_does_not_deny_egress`, `test_refused_rename_does_not_deny_egress`, `test_concurrent_processes_lose_no_line[×3]` |
| A2 rotation call outside `suppress` | `test_rotation_crash_does_not_deny_egress` (the raise flips the ALLOW to denied) |
| A3 no post-rotation size check | `test_refused_rename_does_not_deny_egress`, `test_rotation_crash_does_not_deny_egress` |
| A4 unlocked rotator (size check, sleep, `os.replace`, no flock) | `test_concurrent_processes_lose_no_line[×3]`, 5 runs out of 5 (four writers released together by a start file) |
| C1 policy back to `secret_labels` | `test_redacted_path_refuses_command_secrets[×5]`, `test_secret_at_the_end_of_a_long_command_is_still_refused`, `test_policy_denies_a_bearer_header_and_audits_no_value` |
| C2 no reference filter | `test_references_placeholders_and_pointers_pass` (5 rows) |
| C3 no placeholder filter | `test_references_placeholders_and_pointers_pass[psql postgres://app:password@…]` |
| C4 no pointer-suffix filter | `test_references_placeholders_and_pointers_pass` (`SECRET_NAME`, `API_TOKEN_CMD` rows; the first run survived because every pointer row was also a path, and the two rows were added for this) |
| C5 no path filter | `test_references_placeholders_and_pointers_pass[GOOGLE_APPLICATION_CREDENTIALS=…]` |
| C6 no opacity rule | `test_references_placeholders_and_pointers_pass` (`SORT_KEY=created_at`, `api_key = settings.API_KEY`) |
| C7 no auth-header detector | 2 positive rows, `test_redacted_path_refuses_command_secrets[curl …]`, `test_policy_denies_a_bearer_header_and_audits_no_value` |
| C8 no URL-userinfo detector | 2 positive rows, `test_redacted_path_refuses_command_secrets[git …]` |
| C9 no password-flag detector | 2 positive rows, `test_redacted_path_refuses_command_secrets[mysql …]` |
| C10 no assignment detector | 3 positive rows, `test_redacted_path_refuses_command_secrets[export …, PGPASSWORD …]`, `test_secret_at_the_end_of_a_long_command_is_still_refused` |
| C11 no credential-flag detector | 2 positive rows |
| C12 no basic-auth detector | 1 positive row |
| P1 (R-P1 applied, before it landed) | the 10 R-P1 xfails turned into XPASS(strict) failures |
| P2 (R-C1 applied, before it landed) | the 2 R-C1 xfails turned into XPASS(strict) failures |
| B1 remove `_refuse_leaf_secrets(state)` (scan only the serialised text again) | 17 tests: the 4 quoted rows × 3 paths, `test_raw_leaves_are_scanned_before_serialisation`, `test_nested_leaves_and_keys_are_scanned[×2]`, `test_deep_state_does_not_blow_the_stack` |
| B1k dict keys not walked | `test_nested_leaves_and_keys_are_scanned[state2]` |
| B2a a character required before the secret word | 6 rows: `TOKEN=`, `PASSWORD=`, `KEY=`, `SECRET=`, `export token=`, `PASS=` |
| B2b no query detector | 7 query-string rows |
| B2c password flags without the quoted form | `mysql -p'…'`, `mysql -p"…"` |
| B2d `PASS`/`PWD` dropped from the word list | `DB_PASS=`, `PASS=`, `MYSQL_PWD=` |
| B2d no `docker login -p` | the `docker login` row |
| B2d no `redis-cli -a` | the `redis-cli` row |
| B2d no `aws configure set` detector | the 2 `aws configure set` rows |
| G1 URL values not excluded | `export API_KEY_DOCS=https://…` |
| G2 `_URL`/`_URI`/`_HOST`/`_ENDPOINT` suffixes dropped | `export TOKEN_URL=auth-v2.example.org` |

Round 2 (findings 25, 26). Each value-class mutation reverts ONE pattern to its old class and leaves the others fixed. The counts are failing probe tests for that pattern's own forms, per separator:

| Mutation | `;` | `)` | `,` | Total failing tests |
|---|---|---|---|---|
| V `_VALUE` back to `[^\s'"]+` (assignment, flag, password flags, aws) | 57 | 57 | 57 | 350 (including the call-guard rows) |
| AH `_AUTH_HEADER` back to the old class | 3 | 3 | 3 | 18 |
| KH `_KEY_HEADER` back to the old class | 3 | 3 | 3 | 18 |
| BF `_BASIC_FLAGS` (curl `-u`, httpie) back to the old class | 12 | 12 | 12 | 72 |
| Q `_QUERY` back to `[^&\s'"#]+` | 3 | 3 | 3 | 15 |
| U the terminator class applied to `_USERINFO` (the proposed fix) | 6 | 3 | 3 | 43 |
| E old `_REFERENCE` semantics restored in `_literal` | 12 | 6 | 6 | 86 |
| H no httpie pattern | 18 | 9 | 9 | 129 |
| G no call/subscript guard after a bare value | — | — | — | 4 (`api_key = compute_key2(x)` as negative row and benign probe) |
| S space counted as a symbol in the opacity rule | — | — | — | 4 (`SORT_KEY="created at"`) |

For V, AH, KH, BF and Q the kills come from the separator-then-`$` suffixes (`;$next`, `)$(next)`, `,${next}`). With the quote-aware expansion rule in place, a swallowed separator on its own no longer throws the value away; the swallowed `$` does, and only the terminator class stops it. For E, U and H the plain separators kill as well. The `;` column for U, E and H also counts the spaced ` ; next` control.

Round 3 (finding 27). Each mutation changes one line of `credentials.py`; the counts are failing tests in `test_egress_credentials.py` and `test_decisions_privacy_command.py` (leak side = a secret sent; benign side = a false refusal):

| Mutation | Leak side | Benign side | Example of a killing test |
|---|---|---|---|
| Ma `[ ] { }` restored as expansion markers (the round-2 rule in the sub-token detectors) | 194 | 0 | `API_TOKEN=abc123XYZ99}`, `curl -u 'admin:Xk9]pL2vQ'` |
| Mb bare-value rule applied inside single quotes | 128 | 0 | `curl -u 'admin:Pa$$w0rd…'` |
| Mc quote context ignored (every value read as bare) | 129 | 0 | `basic-single-dollar`, `basic-single-separator` |
| Md no extension to the closing quote for sub-tokens | 43 | 0 | `curl -u 'admin:ab;cd…'` |
| Me no whole-reference rule in single quotes | 0 | 4 | `curl -H 'Authorization: Bearer $TOKEN'` |
| Mf no code-shape guard | 0 | 15 | `cfg_key = cfg[k1]`, `token = tokens2[0]` |
| Mg double-quote extension without its whitespace/quote/backslash stops | 0 | 12 | `docker run -u 1000:1000 image && echo done` (serialised layer) |
| Mj header values need not look like a token | 0 | 4 | `grep -n "X-Api-Key: missing header" app.log` |
| V round-2 `_VALUE` class restored (B3, re-checked) | 460 | 11 | the separator suffixes |
| H httpie pattern removed (M1, re-checked) | 199 | 0 | `http --auth bob:…` |

Round 4 (findings 29-31, QG r4 m2). Same method: one line of `credentials.py` changed, the two test files run, restore by copy, `shasum` equal before and after (`d3850bb4…`). Counts are failing tests:

| Mutation | Leak side | Benign side | Example of a killing test |
|---|---|---|---|
| R1 local reading removed (`local = None`) | 12 | 0 | `I can't log in with curl -u 'admin:Summer$2026x' …` |
| R2 code-shape guard applied to quoted values too (QG r4 m2; survived round 4 with 3005 passed) | 6 | 0 | `export API_TOKEN='x9[ab]'`, `--token "Qx9{abc}"` |
| R3 `_USERINFO` back to the first `@` | 6 | 0 | `git clone 'https://bob:p@ss{w0}rd@g.io/r'` |
| R4 escape layers off | 18 | 0 | `ssh host "export API_TOKEN=\"…\" && ./run"` |
| R5a escape layers: 1 | 2 | 0 | `test_every_escape_layer_is_scanned[2]`, `[3]` |
| R5b escape layers: 2 | 1 | 0 | `test_every_escape_layer_is_scanned[3]` |
| R6 local reading only (whole-text reading dropped when a local one exists) | 3 | 0 | `echo don't -H "Authorization: Bearer ab$c…" x'` |
| R7 `extend` stops applied inside the regex's own match (no `floor`) | 3 | 0 | `git clone 'https://bob:p@ss{w0}rd@g.io/r'` |
| R8 `floor` overrides the double-quote stops | 0 | 3 | `grep "Authorization: Bearer" logs/` (serialised layer) |
| R9 word-opening requirement dropped | 0 | 3 | `it's Bob's: curl -u bob:$PASS 'x'` |
| R10 closes-later requirement dropped | 0 | 3 | `echo x'y 'curl -u bob:$PASS z` |
| R11 local reading skipped for the headers only | 3 | 0 | `can't auth: curl -H 'Authorization: Bearer ab$c9Zq9k7k7' x` |

m2's two rows: `export API_TOKEN='x9[a]'` as prescribed is 5 characters, under `_MIN_VALUE`, and was SENT before and after this change (not "refused today"); the row uses `'x9[ab]'`. No mutation kills an escape depth of 3 against 8: 8 is a work bound (each layer halves a run of backslashes), not a detector, and finding 28(e) states it. One check I wrote this round, `local[start - q] == _NONE`, could never be true (`q` is the nearest quote before the value, so the value is inside it), and I removed it.

Two guards I tried while fixing (a backslash stop in bare values, scheme words such as `Bearer` as placeholders) survived their mutations, because the double-quote stops and the header token rule already cover them. I removed them rather than keep code no test can kill.

The concurrency test with the real helper: 10 runs out of 10 green.

Round 5 (finding 32). Same method, restore by copy, `shasum` equal before and after (`975205e9…`). Counts are failing tests in `test_egress_credentials.py` and `test_decisions_privacy_command.py`:

| Mutation | Failing | Example of a killing test |
|---|---|---|
| Whole fix reverted (round-5 `credentials.py`, run on the timed tests) | 4 of 4 run | `test_serialised_many_match_paste_scans_in_linear_time` (14.57 s against 1.0 s); `[quotes]` 26.95 s, `[mysql]` 5.98 s, `[dotted]` 13.80 s |
| B1a tool-argument window unbounded again | 4 | `test_adversarial_text_scans_under_a_second[mysql]`, `[httpie]`, `[redis]`, `[docker]` |
| B1b userinfo scheme back to `\b[a-z][…]*` | 2 | `test_adversarial_text_scans_under_a_second[dotted]`, `[dashed]` |
| B1c scheme bounded but word boundary kept | 1 | `test_bounded_reads_keep_long_values` (an 80-character scheme) |
| B1d value read without its cap | 1 | `test_adversarial_text_scans_under_a_second[one-quoted-span]` |
| R1 local reading removed (re-run) | 13 (leak side) | the 9 N1 rows, 3 privacy rows, `test_bounded_reads_keep_long_values` (a long single-quoted value after `can't`) |
| R6 local reading only (re-run) | 3 (leak side) | `echo don't -H "Authorization: Bearer ab$c…" x'` |
| R7 no `floor` (re-run) | 3 (leak side) | `git clone 'https://bob:p@ss{w0}rd@g.io/r'` |
| R8 `floor` overrides the double-quote stops (re-run) | 3 (benign side) | `grep "Authorization: Bearer" logs/` |
| R9 word-opening requirement dropped (re-run) | 3 (benign side) | `it's Bob's: curl -u bob:$PASS 'x'` |
| R10 closes-later requirement dropped (re-run) | 3 (benign side) | `echo x'y 'curl -u bob:$PASS z` |

B1d first survived at 100 KB: a literal value short-circuits `any()`, so the far-quote span cost only one read. The killing shape uses reference values (`-u a:$(x)`), which never short-circuit, at 300 KB. Timings re-measured on the PR3 tree (cap `start + floor + _VALUE_CAP`), each at a named size: without the cap, 90 KB in 0.72 s (quadratic); with the cap, 90 KB in 0.017 s and 300 KB in 0.058 s. Round 5 had recorded 0.69 s at 90 KB without the cap and 0.04 s with it, without the size of the second timing, so that figure is replaced rather than repeated.

### Verification (PR2, this lane)

Re-run after Quality Gate round 5 (venv `~/.arkaos/venv/bin`):

- `pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py -q` → **exit 0**, 3207 passed (round 4: 3194; +13 timed and bounded-read tests). The timed tests, 5 runs in a row: 5/5 green, about 4 s each run.
- `ruff check core/egress` → **exit 0**.
- `mypy core/egress core/decisions/privacy.py --follow-imports=silent` → **exit 0**. Without the flag: exit 1, 51 errors, all in imported modules outside this lane (`core/governance/harness_scanner.py` 14, `core/runtime/*` 31, others 6); 0 in `core/egress` or `privacy.py`.
- `uvx codespell core/ tests/ docs/security/` → **exit 0**.
- `~/.arkaos/bin/arka-py -m core.governance.evidence_checks . --checks security-grep,lint` → **exit 0**, both PASS.
- Probe: `pytest tests/python/test_egress_credentials.py -k test_probe` → **exit 0**, 2826 passed (2394 + 180 + 252).
- Functions ≤ 30 lines: `credentials.py` max 27 (`_quote_context`), `audit.py` max 27 (`load_or_create_salt`; `record` is now 22, with the rotation stamp moved into `_stamped`, QG r4 m5), the two test files max 16 (`test_egress_credentials.py`: `_send`; `test_decisions_privacy_command.py`: 11, `test_raw_leaves_are_scanned_before_serialisation`), measured with `ast` on the PR2 merge `4f92bbd8`.

Round 3, kept for the record:

- `~/.arkaos/bin/arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py -q` → **exit 0**, 3127 passed.
- `~/.arkaos/bin/arka-py -m ruff check core/egress core/decisions/privacy.py` → **exit 0**.
- `uvx codespell core/ tests/ docs/security/` → **exit 0**.
- `~/.arkaos/bin/arka-py -m core.governance.evidence_checks . --checks security-grep` → **exit 0**, PASS; the synthetic fixture lines carry `arka:sec-ok(hardcoded-password)`.
- `~/.arkaos/bin/arka-py -m mypy core/egress core/decisions/privacy.py --follow-imports=silent` → **exit 0**.
- Probe: `pytest tests/python/test_egress_credentials.py -k test_probe` → **exit 0**, 2769 passed (2394 form x suffix x path cases + 141 reviewer cases + 234 benign cases).
- Wider regression (`test_decisions_*`, `test_ups_decisions`, `test_notebooklm_chokepoint`, `test_leak_scanner*`, `test_egress_policy`, `test_telemetry_rotate`) → 763 passed.

### Probe: 57 forms x 14 suffixes x 3 paths (after Quality Gate round 4)

Source: `tests/python/test_egress_credentials.py` (`LEAK_FORMS`, `SUFFIXES`, `REVIEWER_LEAKS`, `BENIGN_FORMS`, `PATHS`; tests `test_probe_leak_form_is_refused`, `test_probe_reviewer_leak_is_refused`, `test_probe_benign_form_is_sent`). Each case calls `prepare_state({"command": …}, state_class="command")` in a temporary HOME on three paths: `redact=True` with a redaction config, `redactClients:false`, and no config.

What the set contains:
- my round-1 probe;
- every shape named in the round-2 and round-3 findings;
- every POSITIVE and NEGATIVE row of the credential tests;
- verbatim, the reviewers' scratchpad probes: `probe3.py` (13 leak, 10 benign, 11 bracket shapes), `probe4.py` (7) and `probe_r2.py` (16), 47 leak shapes in all;
- round 4: Paulo's 3 N1 shapes verbatim, the 2 m2 rows, the 2 m3 rows, 5 escaped-quote rows and the either-rule row (13 more rows: 47 reviewer shapes verbatim plus 13 round-4 rows, 60 in all), and 6 benign rows for the local reading and the escape layers.

The Quality Gate notes mention 37 leak and 25 benign shapes for Marta's round-3 probe, but those files hold fewer. Every shape they hold is here.

Leak forms, refused / cases (each cell = forms x 3 paths; `∅` = no suffix):

| Detector (forms) | `∅` | `;next` | `)next` | `,next` | `&&next` | `\|next` | `>out.txt` | ` ; next` | `;$next` | `)$(next)` | `,${next}` | `&&$next` | `\|$next` | `>$out` | Refused |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| assign (18) | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 54/54 | 756/756 |
| flag (3) | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 126/126 |
| authhdr (3) | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 9/9 | 126/126 |
| keyhdr (1) | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 42/42 |
| basic (5) | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 15/15 | 210/210 |
| httpie (4) | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 168/168 |
| pwflag (8) | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 336/336 |
| aws (2) | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 | 84/84 |
| userinfo (4) | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 168/168 |
| query (8) | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 24/24 | 336/336 |
| vendor (1) | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 3/3 | 42/42 |
| **all (57)** | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | 171/171 | **2394/2394** |

Reviewer rows (47 reviewer shapes verbatim plus 13 round-4 rows, own separators, not crossed with the suffixes): 60/60 refused on each of the three paths (180/180).

Benign forms: 84/84 sent on each of the three paths (252/252), 0 false refusals among them (finding 28(f) is a false refusal outside the set). The round-3 additions are `curl -u 'bob:$PASS'`, `cfg_key = cfg[k1]`, `key = f(x)`, `TOKEN=${TOK}`, `export API_TOKEN=$(vault read x)`, the header-name greps, and `docker run -u 1000:1000 image && echo done`.

Earlier rounds, kept for the record: after round 2, the same suite gave 2364 passed and the probe 1848/1848 on its 44 forms; round 3 showed those forms did not cover brackets or quote context (finding 27). After round 1, `~/.arkaos/bin/arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py -q` gave 241 passed, and the round-1 probe gave 69/69 REFUSED on the forms it probed; round 2 showed that those forms did not cover a value glued to a separator (finding 25). Unchanged since round 1: `tests/python/test_decisions_backoff.py` → 15 passed (R-B1); regression `test_egress_policy.py`, `test_notebooklm_chokepoint.py`, `test_telemetry_rotate.py`, `test_decisions_privacy.py`, `test_leak_scanner*.py` → 307 passed.

## PR3 — diff class consumers (2026-09-24)

- **Branch:** `feature/jev-decisions-3` (PR2 merged in `4f92bbd8`)
- **Reviewer:** Bruno (security-eng), security lane, dispatched by Paulo
- **Scope:** the new consumers that send `diff` and `prompt` states: `core/governance/qg_prescreen.py`, `core/governance/slop_check.py`, `core/governance/jev_advisory.py`, `core/decisions/sites/quality.py` (`diff_state`, `prose_state`), `core/workflow/frontend_gate.py::_jev_ui_scope` (`ui_state`), `core/hooks/stop.py` with `sites/governance.py::stop_state`; the egress layer they go through (`core/decisions/privacy.py`, `core/egress/credentials.py`, `core/egress/audit.py`)
- **Frameworks:** STRIDE, OWASP Top 10:2025, CWE, NIST CSF 2.0 PR.DS (data security) and DE.AE (audit evidence)
- **Method:** every property is proven on the captured request body (`urlopen` mocked, the fake-transport pattern of `test_decisions_engine.py`), in a temporary HOME with a fictional client (`zorblax`) and runtime-built secret values. The operator's redaction config was never read. The proofs are `tests/python/test_egress_pr3_consumers.py` (95 tests) and, for findings 38 and 39, the rows of `tests/python/test_egress_credentials.py` named in the table.
- **Verdict for this lane:** APPROVED-WITH-FIXES. Five findings, all fixed in this PR: three majors (33, 34, 36), one of minor-to-major reach (35) and one minor (37). Quality Gate round 1 then found three more in this lane, all fixed in this PR: one blocker (38) and two majors (39, 40). Quality Gate round 2 found one more blocker (41) and two minors (m7, m8), all fixed in this PR. Quality Gate round 3 found one more blocker (42) and three minors (m10, m11, m12), all fixed in this PR; fixing 42 turned up a quadratic scan (43, minor), fixed as well. Quality Gate round 4 found one more blocker (44, a quoted name holding a dot) and a major (45, three Ruby forms), both fixed in this PR, with minors m13 and m14 and the review's own :541 note; after round 4 the operator decided on a structural answer to the recurring class, the catch-all layer (46), added in this PR.

### What held (no finding)

| Property asked | Evidence (test in `test_egress_pr3_consumers.py`) |
|---|---|
| `diff` fail-closed without `~/.arkaos/redaction-clients.json`: privacy refuses, both CLIs print `skipped reason=redaction-config-missing`, zero `urlopen` | `test_prescreen_cli_without_the_list_prints_the_skip_and_sends_nothing` (`qg_prescreen.main`), `test_evidence_cli_prints_the_slop_skip_without_the_list` (`evidence_checks.main --checks slop-score`), `test_slop_section_without_the_list_skips_and_sends_nothing`, `test_privacy_refuses_diff_and_transcript_without_the_list_and_audits_it[×2]` |
| With a list, client names redacted in the body and in the chunk header | `test_client_names_in_a_diff_are_redacted_in_the_body_and_the_header` (`billing/zorblax_invoices.py` → header `# file: billing/[CLIENT-1]_invoices.py part 1/1`) |
| Home paths normalised, in the diff and the header | `test_home_paths_in_a_diff_and_a_header_are_normalised`, `test_tilde_paths_in_a_diff_are_normalised` |
| Secrets in added lines (`API_TOKEN=…`, quoted, `curl -u user:pw`, `Authorization: Bearer …`) refused on the raw leaves AND on the serialised text | `test_secrets_in_added_lines_never_reach_the_wire[×4]`, `test_secrets_in_changed_prose_never_reach_the_wire[×4]`, `test_the_serialised_text_layer_refuses_on_its_own[×4]` (leaf scan patched out, the text layer still refuses) |
| The Stop body is `response`, `user_message`, `tool_uses` only; a transcript with 50 tool results contributes the count and nothing else | `test_the_stop_body_carries_three_fields_and_nothing_of_the_transcript`: 50 `tool_use` + 50 `tool_result` blocks (string and list content), each carrying a canary, the client name and a secret; the captured state is exactly the three fields with `tool_uses: 50`, and none of the three markers is in the body. `_extract_text` never reads a `tool_result` block, so a result cannot pass for the operator's message. |
| A `.ts` payload with a secret literal is refused before the POST | `test_a_ts_file_embedding_a_secret_is_refused_before_the_post[×2]` (with and without the list; it FAILED on this tree before finding 33), `test_a_ts_payload_with_a_camel_case_key_is_refused_by_the_ui_site` |
| Every refusal on the audit trail; PR2 rotation still applies | `test_a_client_residual_refusal_is_audited`, the finding 37 rows, `test_a_privacy_refusal_rotates_an_oversized_audit_like_any_line` (audit over 10 MB → `.1`, the deny line lands in a fresh file; killed by `rotate_if_oversized` patched to a no-op) |

Fixed, QG r1 M3: `ui_state` was class `command`, so on the default install (`{"clients": []}`) a non-UI `.ts`/`.js` Write sent source code with client identifiers in clear, while the same text as a diff was refused. ui-in-ts is now class `diff` (ADR PR3 Decision 2: content classes are fail-closed regardless of the hook that carries them): without a list, `prepare_state` refuses it before the network (`reason=egress-denied:redaction-config-missing`, the gate keeps the regex's WARN-only answer), and with a list the client names are redacted in the code sent (`test_frontend_gate_jev.py::test_no_redaction_list_never_reaches_the_network`, `::test_client_names_are_redacted_in_the_sent_code`; mutant `state_class="command"` killed).

### Findings

| # | Finding | Severity | STRIDE / OWASP / CWE | Reproduction | Fix | Residual |
|---|---|---|---|---|---|---|
| 33 | **Credentials written in SOURCE-CODE syntax left in clear.** The PR2 detectors read shell syntax; the PR3 states are code. Probe on `egress_secret_labels` before the fix, all `[]`: `API_TOKEN: "<opaque>",` (TS object), `apiKey: '<opaque>'`, `` clientSecret: `<opaque>` ``, `{"api_key": "<opaque>"}`, `{"x-api-key": "<opaque>"}`, `db_password: <opaque>` (YAML), `'api_key' => '<opaque>'` (PHP), `API_KEY: str = "<opaque>"`, `const apiKey: string = "<opaque>"`, `{'Authorization': 'Bearer <opaque>'}`, `{"Authorization": "Bearer <opaque>"}`, `` Authorization: `Bearer <opaque>` ``, docker `config.json` `"auth": "<opaque>"`. End to end: `test_a_ts_file_embedding_a_secret_is_refused_before_the_post` FAILED on this tree, `urlopen` called once with the token in the body, with and without the list. | **major** | I / A04:2025 / CWE-200, CWE-798 | The 13 shapes above as `test_source_code_credentials_never_reach_the_wire[×13]`, through `run_prescreen` on a real git repo. | **Fixed** in `credentials.py`: (1) `_ASSIGNMENT` reads an optional type annotation (`_ANNOTATION`, bounded `{0,48}?`); a typed number (`max_context_tokens: int = 200_000`) is a setting. (2) New `_KEY_VALUE` detector (`key: v`, `"key": v`, `key => v`; key read atomically, judged by its WORDS: `token`, `secret`, `password`, … anywhere, `key` only after a qualifier such as `api`, `private`, `master`, `client`; the last word must not be a pointer such as `id`, `url`, `path`). Values: quoted, backtick (a `${…}` template is an expansion) or bare; a bare value that is a member access (`process.env.X`), a number or an UPPER_SNAKE name is code; a value with whitespace is prose; placeholders (`{api_key}`, `example`, `your_…`) pass. (3) `_AUTH_HEADER` accepts a quoted key and an opening quote or backtick. False-positive sweep: every line of the 2999 tracked source, doc and config files of this repository (540,045 lines) through the old and the new detector (540,383 lines at the last run): 10 lines newly refused, 8 of them test fixtures that hold fake credentials on purpose (`"GRAPHIFY_TOKEN": "tok-123"`, `"OPENAI_API_KEY": "0123…"`) and 2 of them lines of this section that quote such shapes; 0 elsewhere; 0 lines lost a label. 19 benign code rows in `test_ordinary_code_still_reaches_the_wire`. Cost: linear, 1 MB of `"a": "b", ` serialised in 0.71 s CPU, the worst of 9 adversarial shapes; three are timed rows of `test_adversarial_text_scans_under_a_second`. | (a) a key/value passphrase with spaces (`password: "correct horse battery"`) reads as prose; (b) a multi-line value (YAML `password: >` block, a value on the line after its key); (c) `key` without a qualifier word (`key: "<opaque>"`); (d) a bare numeric value (`pin: 482913`); (e) `.netrc`'s `password <value>` (space-separated, no detector; see 36 for the read path). |
| 34 | **A PEM private key cut from its BEGIN line left in clear.** `secret_labels` keys on `-----BEGIN … PRIVATE KEY-----`. The prescreen cuts a diff into chunks on line boundaries; a key block that straddles a cut leaves chunk 2 with 24 base64 lines and the END line, no BEGIN. Probe: 2 chunks, chunk 1 `DENIED`, chunk 2 `SENT` with the key body. | major (reach: any diff over one chunk holding a key; a 2.9k-char RSA block straddles about 1 cut in 8 of its placements) | I / A04:2025 / CWE-321, CWE-200 | `test_a_private_key_split_across_chunks_never_reaches_the_wire` | **Fixed** in `credentials.py`: `_PEM_TAIL` (`-----END … PRIVATE KEY-----`) is its own label (`private key block`). The largest PEM key (RSA-16384, about 12.7k chars) fits in one 23.5k chunk body, so a block spans at most two chunks, and each holds BEGIN or END. | A key body with neither marker (pasted base64 without armour). |
| 35 | **The chunker split a token across two requests.** `_split_lines` hard-cut a line longer than a chunk (`line[:limit]`), and `chunk_diff` then cut every chunk to `limit` after adding the header (`chunk[:limit]`), which bit whenever a file name took more than the fixed 512-char reserve. Either cut can fall inside a token; each half then matches no pattern. Probe on a minified line: `API_TOKEN=q` in part 1 and the other 15 characters of the value in part 2, both `SENT`; `zorb` + `lax` (the client name), both `SENT`. Minified bundles, lock files and JSON fixtures carry such lines (`dashboard/package-lock.json` is untracked on this branch). | minor to major (needs a line over 23.5k chars, or a path over about 480 chars, with the token on the cut; reassembly is trivial for the receiver) | I / A04:2025 / CWE-200 | `test_a_token_straddling_the_chunk_cut_never_leaves_in_halves[×2]`, `test_a_long_file_name_never_cuts_a_chunk_after_the_fact` | **Fixed** in `qg_prescreen.py` (another lane's file, changed for this leak only): a line longer than a chunk keeps its head, cut on whitespace, plus ` [long line cut]`; the tail is dropped, not sent. `chunk_diff` measures the header on the real name (upper bound on the part numbers) and no longer cuts after the fact; a name that leaves under half a chunk sends nothing. `test_qg_prescreen.py::test_chunks_cut_between_lines_and_a_huge_line_hard_splits` pinned the leaking behaviour (the whole diff rebuilt from the parts); it is now `…_keeps_only_its_head`. | A quoted multi-word value cut at a space inside it ships its first words; the dropped tail loses advisory context (the prescreen never gates). |
| 36 | **The prescreen and the slop section read files outside the project and sent them.** `_untracked_diff` and `slop_check.prose_text` read `project_dir / name`, and a changed-file name is an input: `--changed-files ../x` or `/abs/x`, and an untracked symlink, which `git ls-files --others` lists on its own (no flag needed). Probe: `notes.txt -> <tmp>/netrc` (untracked) and `../netrc`: the file's content (`machine … password CANARY-OUT-77`) was in the diff, and the credential detector does not know the `.netrc` shape. `~/.docker/config.json`, `~/.kube/config` and `~/.netrc` are the obvious targets. | **major** | I / A01:2025 Broken Access Control / CWE-22, CWE-59 | `test_the_prescreen_never_sends_a_file_outside_the_project[dotdot, absolute, symlink]`, `test_an_untracked_symlink_git_lists_is_not_followed` (changed files derived from git), `test_the_slop_section_never_scores_a_file_outside_the_project[../netrc.txt, notes.txt, .git/config.txt]`: the canary is in no captured body. | **Fixed**: `jev_advisory.readable_inside(project_dir, name)` resolves the path (symlinks followed) and accepts it only inside the project and outside `.git`; `_untracked_diff` and `prose_text` read through it. The files are another lane's; the change is the read path only. | A hard link inside the project to a file outside it (not detectable from the path). Tracked symlinks are unaffected: `git diff` shows the link text, a path, which the home normalisation covers. |
| 37 | **Refusals by the privacy layer left no audit line.** `_refuse_leaf_secrets` (raw leaves) and `_refuse_secrets` (serialised text on the degraded and `redactClients:false` paths) raise before `evaluate`, so a secret refused there, the common case since PR2 finding 23, was the one denial the egress audit never saw. Probe: `prepare_state(diff_state("+API_TOKEN=…"), …)` → `egress-denied:secret`, audit file absent. | minor | R / A09:2025 Security Logging and Alerting Failures | `test_a_leaf_secret_refusal_is_audited_without_the_value[×4]` (FAILED on this tree), `test_a_text_layer_refusal_on_the_degraded_path_is_audited` | **Fixed** in `privacy.py`: `_audit_refusal` writes the deny line `evaluate` would have written (`allowed: false`, `destination`, salted label hashes, `layer: "privacy"`), best effort: the refusal never depends on the line landing. The leaf line carries no payload digest, because an unsalted sha256 of one short leaf would confirm a guessed secret; the text-layer line carries the digest of the whole serialised text, as `evaluate` does. No value and no client name in either line (asserted). | — |
| 38 | **QG r1 B1: a `$` inside a source-code string was read as a shell expansion, and the credential left in clear.** `_kv_value` mapped `"` to `_DOUBLE`, and `_literal` rejects `$` outside single quotes, a rule written for shell words. JSON, JS, YAML and Python substitute nothing inside quotes. Reproduced by Marta: `egress_secret_labels('{"password": "Tr0ub4dor$3xYz"}') == []` (without the `$`: `credential key-value`); YAML `password: "P@ss$w0rd!2024"` → `[]`; end to end, a diff-class `prepare_state` SENT both. Missed in my finding 33 fix: I carried the shell rule into the code detector and tested no value holding `$`. The same rule hid `password = "…$…"` and `API_KEY: str = "…$…"` (assignment) and `{"Authorization": "Bearer …$…"}` (header), all `[]` before. Found while fixing: the key/value detector resumed its scan after a quoted value, never inside it, so on the serialised layer `{"diff": "+password: v"}` read as the single pair `diff` and the pair the leaf carries was never seen (the leaf scan caught it; the text layer on its own did not). | **blocker** | I / A04:2025 / CWE-200, CWE-798 | `test_egress_credentials.py::test_a_dollar_inside_a_source_code_string_is_data[×11]` (JSON, JS, YAML double, single and bare, PHP, template with and without a slot, Python, typed, quoted header key; each raw and inside `json.dumps({"diff": …})`), `::test_a_whole_reference_in_source_code_is_still_a_reference[×13]`; end to end `test_egress_pr3_consumers.py::test_source_code_credentials_never_reach_the_wire[json-dollar, yaml-dollar, js-dollar, py-dollar]` through `run_prescreen`. After: `{"password": "Tr0ub4dor$3xYz"}` and YAML `password: "P@ss$w0rd!2024"` → `['credential key-value']`. | **Fixed** in `credentials.py`: a new quoting `_CODE` (nothing substituted, as in single quotes; only a whole reference is exempt). Key/value values are always `_CODE`; a backtick template drops its `${…}` slots and is judged on the rest; a bare value that starts with `$` is a variable (`'password' => $pw`). An assignment is `_CODE` when written as code: a type annotation, or anything but a bare `=` between name and value (`password = "v"`). The auth header is `_CODE` in its code forms (a quoted key, a value opened by a quote, a call argument), not when a backtick opens the value. `_WHOLE_REFERENCE` also exempts a dotted `${env.X}`. `_key_value_hit` resumes inside a quoted value. Sweep: see finding 39. | (a) `TOKEN="a$b"` with no spaces around `=` keeps the shell reading; the Python keyword argument `f(password="a$b")` this residual named was a real leak, closed by finding 41; (b) PHP double quotes DO interpolate, so `"prefix$var"` under a secret key is now refused (a false refusal); (c) the shell forms stay references by design (`-H "Authorization: Bearer $T"`). |
| 39 | **QG r1 M1: the auth header written as a call argument left in clear.** `_AUTH_HEADER` took only `:` or `=>` after `authorization`. Reproduced by Marta: `headers.set("Authorization", "Bearer q8Zr2mXv7LpT0wKd")` (JS) and `req.Header.Set("Authorization", "Bearer …")` (Go) → `[]`; also `[]` before: Java `setRequestProperty("Authorization", "Bearer …")` and `headers["Authorization"] = "Bearer …"`. Missed in finding 33: I probed object and JSON forms only, no call form. | major | I / A04:2025 / CWE-200, CWE-798 | `test_egress_credentials.py::test_an_auth_header_as_a_call_argument_is_refused[×11]` (JS `Headers.set`, Go, Java, JS fetch, `new Headers([[…]])`, Python requests, a `$` in the token, no scheme word, G2's `Authorization: Token token=…`, `headers[…] =`, a constant key; raw and serialised), `::test_a_header_built_from_a_variable_is_not_a_secret[×11]`; end to end `test_source_code_credentials_never_reach_the_wire[js-headers-set, go-header-set, java-request-property, bearer-only]`. | **Fixed** in `credentials.py`: after a QUOTED key `_AUTH_HEADER` also accepts a quote, a comma and a quote (``["'`]\s*,\s*["'`]``, conditional group `(?(kq)…)`, so an unquoted `authorization, x` is not read). New `_QUOTED_BEARER`: a quote, the word `bearer`, `token` or `basic`, whitespace, then 16 or more of `[A-Za-z0-9._~+/=-]` (atomic, linear) then a quote, whatever key carries it; its alphabet has no `$`, so `"Bearer $TOKEN"` never matches. The scheme word alone is never the value (lookahead): the sweep showed `'headers.set("Authorization", "Bearer " + token)'` as a quoted line read `Bearer` as the value and ran on to the closing quote; `Token token=v` still reads `token=v`. False-positive sweep, round-1 module against this one, on every line of the 3184 tracked and untracked text files (599,630 lines): 4 lines newly refused, 3 of them the new leak fixtures of `test_egress_pr3_consumers.py` (lines 299-301) and 1 the docstring of `credentials.py` that quotes `{"password": "Tr0ub4dor$3xYz"}`; 0 elsewhere. 6 lines lost a label, none of them a credential: `bin/arka-providers:89` (a prompt text), `credentials.py:7` and review lines 134 and 394 (placeholders in prose), and two fixture lines of `test_egress_credentials.py` (163, 629) read as whole source lines. Cost: the 11 timed adversarial rows and the serialised-paste test stay green. | A token joined at run time (`"Bearer " + "<literal>"`); a letters-only token of 16 to 19 characters (not token-like: read as prose); a header value held in a variable. |
| 40 | **QG r1 M2: `.git` was refused by case, and the file system matches without it.** `readable_inside` tested `".git" in parts`; `Path.resolve` keeps the case as written and APFS (and NTFS) are case-insensitive. Reproduced by Marta: `readable_inside(repo, ".GIT/config")` returned an existing path, and `qg_prescreen.file_diff` built a diff of `.git/config` holding a userinfo URL (refused there by the userinfo detector, but a remote URL, a hook or any `config` line that is no credential shape would ship). Missed in finding 36: my rows used `.git/…` only. | major | I / A01:2025 Broken Access Control / CWE-178, CWE-22 | `test_qg_prescreen.py::test_git_metadata_is_never_sent_in_any_case[.git, .GIT, .Git]` (a canary that is no credential shape in `.git/config`, absent from every body), `test_egress_pr3_consumers.py::test_the_slop_section_never_scores_a_file_outside_the_project[.GIT/config.txt, .Git/config.txt]`, `::test_git_metadata_is_refused_in_any_case[.GIT/config, .Git/config, src/../.GIT/hooks/x]`, `::test_a_nested_git_dir_in_another_case_is_refused`, `::test_the_root_git_dir_is_refused_by_identity_under_another_spelling` (skips on a case-sensitive file system), `::test_an_ordinary_file_is_still_readable`. After: `.GIT/config`, `.Git/config` → `None`. | **Fixed** in `jev_advisory.py`: the part test ignores case (`part.casefold() == ".git"`), and `_under_git_dir` compares the target and every directory above it inside the project with `root/.git` by `os.path.samefile`, which catches any other spelling the file system maps to it; an `OSError` there refuses. | A hard link inside the project to a file under `.git` (the same limit as finding 36); a nested repository's `.git` under a spelling other than case (the inode check knows only `root/.git`). |
| 41 | **QG r2 B1: prefixed string literals, keyword arguments and Go assignments left in clear.** Reproduced by Marta with a redaction list present, all SENT by `prepare_state(diff_state(...), state_class="diff")` with labels `[]`: `conn = psycopg2.connect(host=h, password="Tr0ub4dor$3xYz")`, `SECRET_KEY = b"<opaque>"`, `token = f"<opaque>"`, `password := "<opaque>"`; my re-probe added `var password string = "…$…"`, `db.connect(user="u", password="…$…")`, `Client(api_key="…$…")`, `r"…"`, `u'…'`, `rb"…"`, `{"password": b"…"}` and `f"{prefix}<opaque>"`: 15 shapes, 15 SENT. The control `SECRET_KEY = "<opaque>"` was refused. What I missed, and why: (1) `_VALUE` and `_KV_VALUE` accepted a quote or a bare word only, so a prefix letter was read as a one-character bare value (`b`) and the rest was never looked at; (2) in finding 38 I made an assignment `_CODE` only when an annotation or spaced `=` marked it, and residual 38(a) recorded the keyword argument as a gap instead of closing it; (3) `_ASSIGNMENT` knew `=` alone: neither Go's `:=` nor `var name Type =`. My probe rows for 33 and 38 were Python, JS, TS, JSON, YAML and PHP literals without prefixes, and I tested no call form of an assignment. Master comparison, measured on `git show master:core/egress/credentials.py`: master is `[]` on every one of the 15 shapes too, and labels `psycopg2.connect(password="<opaque without $>")`, which this tree also labels. So this is not a regression against master but a gap that was already there, and PR3 exposes it by sending diffs for the first time. The severity holds. | **blocker** | I / A04:2025 / CWE-200, CWE-798 | `test_egress_credentials.py::test_prefixed_keyword_and_go_string_forms_are_refused[×21]` (each raw and inside `json.dumps({"diff": …})`), `::test_templates_references_and_placeholders_in_string_forms_pass[×21]`; at the state the prescreen sends, `test_egress_pr3_consumers.py::test_string_forms_in_a_diff_are_refused_by_prepare_state[×10]` (the refusal is captured: `DecisionUnavailable`, `egress-denied:secret`) and `::test_string_form_references_in_a_diff_are_sent[×6]` (the state comes back unchanged). Before/after on the 27 probe rows: 19 wrong → 0. | **Fixed** in `credentials.py`: (1) `_STR_PREFIX`, an optional `[rRbBuUfF]{1,2}` before a quote, outside the `value` group, in `_ASSIGNMENT` and `_KEY_VALUE`. An f-string drops its `{…}` slots (`_FSTRING_SLOT`). A template, f-string or backtick, is judged on its longest literal run, and a run ending in `=` or `:` labels its slot (`f"TOKEN={tok}"` is not a value; the sweep found 5 such fixture lines). (2) `_call_argument`: `name="v"` right after `(` or `,` on its line, or with `,` or `)` right after the value, is `_CODE`. (3) `\s*:?=`, plus `var`/`const` followed by a type (`(?(var)…)`, so the typed form needs the keyword and prose such as `the api key value = x` stays out). The separator test now runs from the end of the name to the quote, so an annotation or a prefix makes it something other than a bare `=`. Two terms I first wrote were redundant; the mutants showed it and I removed them. QG r2 m7: `_WHOLE_REFERENCE` accepts `${NAME:-}`, `${NAME:?msg}`, `${NAME:-$OTHER}` and `${NAME:-${OTHER}}` (`_PARAM_DEFAULT`); the key/value scan that resumes inside that string no longer reads `-$OTHER` as a literal (`_BARE_VARIABLE`). A literal default (`${PW:-<opaque>}`) is still refused. QG r2 m8: `%(NAME)s` is a `_KV_PLACEHOLDER`, and a quoted bearer string whose token holds `placeholder`, `example`, `your_`, `_here`, `redacted`, `changeme` or `dummy` is documentation (`_BEARER_PLACEHOLDER`). False-positive sweep, round-2 input module against this one, on every line of the 3184 tracked and untracked text files (599,904 lines; artifact `~/.arkaos/quality-gate/81e2c0d9-…/pr3/bruno-sweep-r2.out`): 18 lines newly refused, 17 of them the new leak fixtures of the two test files and 1 a false refusal I accept: `credentials.py:109`, `_SECRET_WORD = r"(?:KEY|…)"`, a raw-string regex under a secret-named constant (that file was already refused at line 66, so no diff that would otherwise be sent is affected); 0 elsewhere. 9 lines are no longer refused, all of them the new m7/m8 benign fixtures and the `credentials.py` comment that quotes the placeholder, none of them a credential. | (a) the last keyword argument of an exploded call with no trailing comma (`password="…$…"` alone on its line, `)` on the next); black's magic trailing comma makes this rare; (b) a prefixed or keyword value in shell (`TOKEN=b"x$Y"`, `(TOKEN="a$B" cmd)`) is now read as code: a false refusal on the deny side; (c) closed in round 3 (m10): the literal runs of a template are joined, so `f"q8Z{a}r2m{b}Xv7"` is refused; (d) a Go raw string in backticks under `:=` (`` password := `…` ``) is SENT: `_VALUE` takes no backtick, since in shell a backtick is command substitution. Corrected in round 3 (QG r3 m11): this residual also named the Kotlin/Swift `val`/`let` forms, which were refused all along; since round 3 they are pinned as leak rows (`test_egress_credentials.py::test_a_secret_named_in_quotes_is_refused[val password = …, val password: String = …, let password = …, let password: String = …]`, `test_egress_pr3_consumers.py::test_a_secret_named_in_quotes_is_refused_by_prepare_state[kotlin-val, swift-let-typed]`); (e) a raw-string regex constant under a secret-named variable is refused (the one sweep line above). | <!-- arka:sec-ok(hardcoded-password): finding row quotes the refused shapes, not a credential -->
| 42 | **QG r3 B2: a secret named inside a quoted string left in clear.** Reproduced by Francisca, then by Marta and Paulo on this tree, all SENT by `prepare_state({"path": "x.py", "diff": "+…"}, redact=True, state_class="diff")` with a redaction list and labels `[]`: `define('AUTH_KEY', '<salt>');` and `define( 'DB_PASSWORD', '<opaque$>' );` (WordPress `wp-config.php`), `os.environ["API_TOKEN"] = "<opaque>"`, `app.config["SECRET_KEY"] = "<opaque>"` (Flask), `$config['encryption_key'] = '<opaque>';`; also SENT: `os.environ.setdefault("DB_PASSWORD", "…")`, `os.getenv("PW", "…")`, `setPassword("…")`, `auth=("admin", "…")`. My re-probe added `ENV['API_TOKEN'] = '…'` (Ruby), `request.headers["X-Api-Key"] == "…"`, `builder.password("…")`, `withApiKey("…")`, `HTTPBasicAuth(user, "…")` and the .NET `<add key="ApiKey" value="…" />`: 15 shapes, 15 SENT. The control `SECRET_KEY = "<opaque>"` was refused. What I missed, and why: every code detector I wrote for 33, 38 and 41 finds the secret's NAME as an identifier: an assignment target, a key before `:` or `=>`, a keyword argument. None reads a name that is itself a string literal, and a subscript, a positional argument, a setter or a pair never puts the name where those detectors look. My probe rows had the name bare or as a key, never as a quoted argument or subscript. Master comparison, measured on the master copy of `credentials.py` (sha256 `d5ca23a1…`): `[]` on all 15 shapes too. Like 41, a gap already on master that PR3 exposes by sending diffs for the first time; the blocker severity holds. | **blocker** | I / A04:2025 / CWE-200, CWE-798 | `test_egress_credentials.py::test_a_secret_named_in_quotes_is_refused[×27]` (each raw and inside `json.dumps({"diff": …})`; also the m10, m11 and m12 rows), `::test_a_quoted_name_without_a_literal_secret_passes[×16]` (raw and serialised: `os.environ["HOME"]`, `config["timeout"] = "30s"`, `os.environ["GIT_COMMIT"] = "a1b2c3d4e5f6"`, `define('WP_DEBUG', 'false')`, `os.environ.get("API_TOKEN")`, `setPassword(password)`, `auth=(user, pw)`, the wp-config sample salt, `get_secret("prod/db-password-2024")` …), `::test_finding_42_detectors_scan_in_linear_time[×5]`; at the state the prescreen sends, `test_egress_pr3_consumers.py::test_a_secret_named_in_quotes_is_refused_by_prepare_state[×13]` (refusal captured: `DecisionUnavailable`, `egress-denied:secret`), `::test_a_secret_named_in_quotes_is_refused_on_the_serialised_layer[×13]` (leaf scan patched out, the text layer refuses on its own) and `::test_a_quoted_name_without_a_literal_secret_is_sent[×7]` (the state comes back unchanged). Before/after on the 15 shapes: 15 SENT → 15 refused; the 6 benign rows named in the QG criterion: sent before and after. | **Fixed** in `credentials.py`, three detectors, each judged by the existing `_kv_secret`/`_kv_value` (the name by its words, the value as a `_CODE` literal): (1) `_QUOTED_NAME`, from Francisca's prototype: a quoted name, then `] =` (also `==` and `!=`), `,` or `value=`, then a string literal, prefixed or not; the scan resumes at the value's quote, since it may open the next name (`cfg.get("a", "API_TOKEN", "…")`). (2) `_SETTER_CALL`: a call whose first argument is a literal and whose name is a setter verb (`set`, `with`, `use`, `put`) followed by secret words, or the secret word alone (`builder.password("…")`); `get_secret("prod/db-2")` looks a secret up by its name and stays sent. (3) `_AUTH_PAIR`: `auth=(user, "…")`, `auth=[…]`, `auth=HTTPProxyAuth(…)`, `HTTPBasicAuth(…)`, `HTTPDigestAuth(…)`; the user may be a name, the password is a literal. `pw` joins the secret words (`os.getenv("PW", "…")`). False-positive sweep, round-3 input module against this one, on every line of the 3184 tracked and untracked text files (600,216 lines; artifact `~/.arkaos/quality-gate/81e2c0d9-…/pr3/bruno-sweep-r3.out`): 7 lines newly refused, 3 of them rows of this review that quote the shapes, 2 the new leak fixtures of `test_egress_credentials.py` (m10, m12) and 2 test fixtures with key-shaped values (`test_keys.py:67` `set_key("OPENROUTER_API_KEY", "sk-or-1234567890")`, `test_synapse_l25.py:633` `c["auth"] == "Bearer tok-123"`); 0 elsewhere. 3 lines are no longer refused: the new `access_token_here2` benign fixture and the two review lines that quote it, none of them a credential. On Francisca's 83,270 sampled added lines: 2 newly refused (the same two fixtures), 0 lost; her prototype had 1 extra refusal on that sample. | (a) a setter verb outside the four (`resetPassword("…")`, `changePassword("…")`) and a password that is not the first argument of a plain call (`login("admin", "…")`); (b) the shell form `PW=…` (the shell list has `PWD`, not `PW`: as a substring `PW` would match `NPW`, `PWM`); (c) a name built at run time (`env[f"{prefix}_TOKEN"] = "…"`; the concatenated form `env[prefix + "_TOKEN"] = "…"` is refused, by the quoted-name detector on `"_TOKEN"`; since round 4 the catch-all layer refuses the f-string form too, finding 46); (d) a value that is not a literal on the same line (a PHP heredoc, a value on the next line); (e) a userinfo password holding `$` inside a code string (`DATABASE_URL = "postgres://u:…$…@h/db"`): the userinfo detector keeps the shell reading of `$`, and the `_URL` pointer suffix keeps the assignment detector off; (f) a Python triple-quoted literal (`password = """…"""`). (e) and (f) come from Francisca's probe sets; I propose them for PR5 with the 28(f) and 28(g) hardening. |
| 43 | **Found while fixing 42: the assignment name backtracked quadratically.** `(?P<name>[A-Za-z0-9_]*WORD[A-Za-z0-9_]*)` retried the secret word at every occurrence inside one identifier and ran the tail to its end each time. Measured with `"token" * n` (process CPU time): master 10 KB 0.13 s, 20 KB 0.51 s, 40 KB 1.97 s; the round-3 input 0.55 s, 2.23 s, 8.86 s (the annotation group adds a factor of about 4). A state near the 1 MB cap in that shape would stall the hook for many minutes. The trigger is one `[A-Za-z0-9_]` run holding the word many times: no line of the sweep has one, so it takes crafted input. | minor | D / A06:2025 Insecure Design / CWE-1333 | `test_egress_credentials.py::test_adversarial_text_scans_under_a_second[secret-word-run]` (`"token"` × 20,000, raw and serialised) | **Fixed** in `credentials.py`: the name is read atomically, and a lazy lookahead finds the word inside it. The meaning is unchanged, since the name always ran to the end of its identifier (the next character is `:`, `=` or a space). After: 40 KB 0.02 s. A scaling probe of 41 units (20 KB and 80 KB, raw and serialised) through every detector: all linear, the worst 80 KB text 0.094 s. | — | <!-- arka:sec-ok(hardcoded-password): finding rows quote the refused shapes, not a credential -->
| 44 | **QG r4 B1: a quoted name holding a dot left in clear.** Reproduced by Francisca, then by Marta and Paulo, on this tree and on master: `Config::set("services.stripe.secret", "<opaque>")`, `config()->set('services.stripe.secret', '<opaque>')`, `'stripe.secret' => '<opaque>',`, `"db.password": "<opaque>"`, `System.setProperty("javax.net.ssl.trustStorePassword", "<opaque>")` and `conf.set("spark.hadoop.fs.s3a.secret.key", "<opaque>")`: labels `[]`, SENT by `prepare_state` with the value. The controls `"db_password": …` and `'stripe_secret' => …` were refused. Cause: the name alphabet `[A-Za-z0-9_-]+` of `_QUOTED_NAME` and `_KEY_VALUE` had no dot. What I missed: every name in my probes was an identifier; Laravel config keys, Java system properties and dotted JSON keys are names written as strings, and a string may hold a dot. | **blocker** | I / A04:2025 / CWE-200, CWE-798 | `test_egress_credentials.py::test_dotted_names_and_ruby_forms_are_refused[×14]` (raw and inside `json.dumps({"diff": …})`), `::test_dotted_names_and_ruby_forms_without_a_literal_pass[×8]`; at the prescreen state, `test_egress_pr3_consumers.py::test_a_secret_named_in_quotes_is_refused_by_prepare_state` and `…_on_the_serialised_layer` (9 new shapes each: `laravel-config-set`, `laravel-helper-set`, `laravel-config-array`, `json-dotted-key`, `java-system-property`, `spark-conf`, and the three of 45) and `::test_a_quoted_name_without_a_literal_secret_is_sent` (6 new rows: `"db.password.file": "/etc/x/pw"`, `"spring.datasource.password": "${DB_PASSWORD}"`, `"api.key.id": "k1a2b3c4d5"`, `'services.stripe.key' => env('STRIPE_KEY')`, `'app.name' => 'Laravel'`, `ENV.fetch("API_TOKEN") { nil }`). Before/after on the 6 shapes: 6 SENT → 6 refused. | **Fixed** in `credentials.py` from Francisca's prototype: a QUOTED name may hold a dot, in `_QUOTED_NAME` and in `_KEY_VALUE`; a bare key may not (`cfg.token: v` stays read as `token: v`). The prototype's `_KEY_VALUE` form is quadratic: its quote group `(?P<q>["'`]?)` matches empty, an empty match counts as matched for `(?(q)…)`, so a bare `a.a.a…` took the dotted alphabet and was read to its end from every dot (`test_adversarial_text_scans_under_a_second[dotted]`: over 1 s on 100 KB, and the mutant "empty-matching quote group" fails it). The shipped form makes the group optional (`(?P<q>["'`])?`) and the closing quote conditional (`(?(q)(?P=q))`). Sweep: the dotted names add no refusal on the 3,184 files (600,763 lines) or on the 83,270 sampled lines (artifact `bruno-sweep-r4.out`; every newly refused line is the catch-all's, finding 46). | A dotted name in an unquoted shell or properties form is the assignment detector's (`db.password=<opaque>` was refused already). |
| 45 | **QG r4 M1: three Ruby forms left in clear.** Reproduced by Francisca and Marta, on this tree and on master: `ENV["API_TOKEN"] ||= "<opaque>"`, `config[:api_key] = "<opaque>"`, `ENV.fetch("API_TOKEN") { "<opaque>" }` → `[]`, SENT. `_QUOTED_NAME` took `] =`, `==`, `!=` but not `||=`; a symbol subscript has no quoted name; a fetch block puts `) {` between the name and the literal. | **major** | I / A04:2025 / CWE-798 | `test_dotted_names_and_ruby_forms_are_refused` rows `ENV["API_TOKEN"] ||= …`, `config[:api_key] = …`, `config[:api_key] ||= …`, `ENV.fetch("API_TOKEN") { … }`; the prepare_state rows `ruby-or-assign`, `ruby-symbol`, `ruby-fetch-block`; benign `ENV["API_TOKEN"] ||= ENV["FALLBACK_TOKEN"]`, `ENV.fetch("API_TOKEN") { nil }`, `x[:5] = 'abc'` (a slice). Before/after: 3 SENT → 3 refused. | **Fixed** in `credentials.py`, all three detected, none left as a residual: `_QUOTED_NAME` accepts `] ||=` and `) {`; a new `_SYMBOL_SUBSCRIPT` reads `[:name] =`, `||=`, `==`, judged by the same `_kv_secret` and reported as `credential quoted name`. | A fetch block written `do "…" end`, and the other Ruby shapes this row does not list, fall to the catch-all (46), which refuses `ENV.fetch("API_TOKEN") do "<opaque>" end`. |
| 46 | **The catch-all layer (operator decision after round 4: "fixes + camada catch-all").** Five review rounds each found a new SHAPE the precise detectors did not know (the shell forms of 23-31, the code forms of 33, 38, 39, 41, the quoted names of 42, the dotted and Ruby names of 44-45). A precise detector knows the syntax around a secret; code has more syntax than a list can hold. The structural answer is a last line of defence that knows no syntax, modelled on the gitleaks `generic-api-key` rule and GitHub's generic-secret patterns: a keyword near a high-entropy-looking literal. | layer (design finding; closes the class, not one shape) | I / A04:2025 / CWE-200, CWE-798 | `test_egress_credentials.py`: `::test_the_catch_all_refuses_a_shape_no_detector_knows[×16]` (label `credential catch-all` exactly, raw and serialised: `resetPassword("<opaque>")`, `login("admin", "<opaque>")  # password`, `keyring.set_password(…)`, `env[f"{prefix}_TOKEN"] = "<opaque>"`, `password = """<opaque>"""`, `ENV.fetch(…) do … end`, `secrets.put(name, …)`, `set_secret(name=…, value=…)`, `token ||= …`, `auth_header = "Basic " + "<opaque>"`, `String pw = …`, a literal `${X:-<opaque>}` default; since round 5 the bare shapes `ENV API_TOKEN <opaque>`, `<td>api token</td><td><opaque></td>`, `set the admin password to <opaque>` and a 32-character lowercase hex after `ENV API_TOKEN`), `::test_the_catch_all_passes_names_patterns_and_references[×28]` (one row per guard), `::test_the_catch_all_reads_a_serialised_state_by_its_leaves`, `::test_the_catch_all_runs_only_when_no_detector_fired`, `::test_the_catch_all_alone_refuses_the_review_history[×127]` and `::…_misses_only_the_documented_residual` (every precise detector switched off), `::test_the_catch_all_alone_scans_a_megabyte_in_linear_time[×13]` (open and closed literal, raw and serialised: 52 forms timed at the 4x step, and the 12 forms of `escapes`, `digits` and `bare-tokens` also at the 1 MB cap); `test_egress_pr3_consumers.py`: `::test_the_catch_all_refuses_a_shape_no_detector_knows_by_prepare_state[×7]`, `…_on_the_serialised_layer[×7]`, `::test_the_catch_all_sends_names_patterns_and_references[×6]`, `::test_with_no_precise_detector_the_catch_all_still_refuses_the_code_forms[×43]` (no exception left: the bare YAML value is refused since round 5), `::test_the_catch_all_alone_misses_a_pretty_printed_element_value` (residual (g), pinned at the prescreen state). | **Added** to `credentials.py` (`CATCH_ALL_LABEL`, `_catch_all_hit` and helpers). **Rule.** A line (a real newline or a serialised `\n`) is refused when (a) it holds a quoted literal (`'…'`, `"…"`, `` `…` ``, backslash escapes honoured, never opened by an escaped quote) whose value, once a leading `NAME=` or `Name: ` label, an auth scheme (`Bearer`), template and format slots (`${X}`, `{x}`, `{{ x }}`, `%(x)s`, `<x>`) are set aside and a literal `${X:-v}` default kept, has at least 10 characters, letters and a digit or an ASCII symbol, and is none of: a whole reference, a placeholder (`_KV_PLACEHOLDER`, `_PLACEHOLDER`), a path, a URL without userinfo, a path from a variable, a name (letter segments with at most one digit run, joined by separators: `services.stripe.secret`, `SORT_KEY=created_at`), a regular expression, a call or an attribute, a timestamp, text with whitespace or non-ASCII characters; and (b) a secret word (`password`, `passwd`, `passphrase`, `pwd`, `pw`, `secret(s)`, `token`, `auth`, `authorization`, `credential(s)`, `apikey`, `secretkey`, `privatekey`, `accesskey`, `clientsecret`, or `key` after a qualifier) is in a name OUTSIDE that literal, not followed by a pointer word in the same name (`api.key.id`, `token_url`). A JSON state is read by its string leaves, plus each key with its plain value and each array of plain strings, so the JSON quoting of a leaf is never taken for a literal. The layer runs only when no precise detector fired on any escape layer, so it never changes an existing label. **Unquoted (round 5, operator decision "catch-all sem aspas").** The text between the quoted literals is also read by its bare tokens (between whitespace, `<` and `>`; end quotes, `,;.:()[]{}` and a backslash trimmed, a diff's `+`/`-` and a CDATA opener dropped; a `NAME=` or `NAME:` label split off as in a literal). A bare token is judged by the same length, two-class rule and guards, and more strictly: not a declared hash (`sha512-…`, `h1:…`, `sha256:…`), not code glued to a name (`tokens[0].map((line`, a call or subscript; a bundler's `name$1`), no secret word inside the value (`password))->` is code naming one), and the gate the operator named for this case: a digit AND a symbol or both letter cases, which I widened on the leak side to also pass 20+ characters (a 32-character lowercase hex key). The gate is on because the ungated form exploded the cost (sweep below: 111 code and doc lines newly refused against 5). Nothing is read as a bare token INSIDE a quoted literal, which keeps `'${DB_PASSWORD:?set DB_PASSWORD}'` a reference. **Path words (QG r5 m1).** The words of a file-path literal or bare token (`/`, `~`, `./`, `../`, a drive, `$VAR/`, and since round 6b a relative path, finding 48; not a URL) are dropped from the secret-word search: they name the file. Since round 7 (M-B, finding 49) only when the value next to the path, after it or before it, is itself a hex digest of 32+ characters, a declared hash, a path or a hashed asset; a path next to an opaque value, or next to nothing, keeps its words. On Francisca's Laravel boilerplate this sends the 41 hash-manifest lines (`'/…/app/Actions/Auth/X.php' => '<sha256>'`) that round 5 refused; the other 6 lines the layer refuses there are test fixtures holding a literal password or a TOTP seed (`'password' => Hash::make('<literal>')`, `'two_factor_secret' => Crypt::encryptString('<literal>')`), correctly refused. **Thresholds, priced by the sweep** (round-4 input against this tree, 3,184 tracked and untracked files, 600,763 lines; Francisca's 83,270 sampled added lines): minimum length 16 → 2 lines newly refused, and the layer alone catches 44 of the 111 history rows; 12 → 5 lines, 88 rows; **10 (shipped) → 5 lines, 89 rows**; 8 → 11 lines (2 in docs), 90 rows. On the sample, 0 newly refused at every threshold. The 16 of the brief missed every 14-character password fixture of the history (`Tr0ub4dor$3xYz`, `Hunter2Secret!`), hence 10. **The 5 lines newly refused at 10, all test fixtures, none real code, none docs**: `test_egress_credentials.py:687` `F_TOKEN = "q8Zr" + "2mXv7LpT0wKd"` and `test_egress_pr3_consumers.py:40` `TOKEN_VALUE = "q8Zr" + "Vt3LmW9xK2pB7nJ4"` (synthetic secrets, correctly refused), and three fixture lines where one quote of a Python string pairs with a quote of the code inside it (`test_egress_credentials.py:649`, `:713`, `:793`). No line is refused any longer that was refused before. The tuning that got there, each step measured on the sweep: the word must sit outside the literal (a `sha512-…Pw…` integrity hash of `package-lock.json`, 37 lines, and regex literals holding `password`), `pass` alone left out (prose: "tests pass", "pass the result"), code, regex, timestamp and non-ASCII guards (Markdown backticks around `var(--token-name)`, `create($request->all())`), a quoted value inside a literal unwrapped (`'k: "v"'` in fixtures), `Bearer $TOKEN` judged after the scheme goes. **What the layer alone catches of the review history** (`test_the_catch_all_alone_refuses_the_review_history`, every precise detector off): 89 of 111 distinct rows (the quoted shell forms of 23, the N1 and 30 rows, 38, 39, 41, 42, 44, 45), raw and serialised; 39 (11 of 11), 41 (22 of 22), 44 and 45 (14 of 14) in full. At the prescreen state, 30 of the 31 `CODE_LEAKS` and `STRING_FORM_LEAKS` shapes of `test_egress_pr3_consumers.py` (33, 38, 39, 41). **Round 5, with the unquoted reading**: 106 of 127 distinct history rows (the 111 above plus the 16 rows of 47), 19 misses before 47's rows (89 of 111 plus the 3 newly refused below: 92 of 111), 47 adds 2 (both residual (g)), so `CATCH_ALL_MISSES` holds 21 and `HISTORY_REFUSED = 106` pins it; newly refused of the round-4 misses: `password: <opaque>` (the bare value), `can't auth: curl -H 'Authorization: Bearer …' x` and `ssh host "export API_TOKEN=\"…\" && ./run"` (the value is outside every literal once the quotes pair wrongly); 47 itself 14 of 16 (both pretty-printed elements are residual (g)). At the prescreen state all 43 of `CODE_LEAKS`, `STRING_FORM_LEAKS` and the 12 XML and netrc shapes. **Sweep, round 5** (`pr3/bruno-sweep-r5.out`; every line read raw and serialised; baselines the round-5 input and master): shipped (gated) form, against the round-5 input: the repo corpus (3,184 files, 601,165 lines) 7 newly refused, 5 by the bare reading (1 code line, a regex in a vendored test, `/\[composition…\] … id "jwt" "auth"`; 3 doc lines: `$30K/month` twice in the ABM playbook and a wiki link row `[19 · Token Economy](19-Token-Economy.md)`; 1 test comment `_ARGS{0,512}`) and 2 by the netrc rule (the finding-36 canary fixture `machine … password CANARY-OUT-77` and review row 36 quoting it, both correct); Francisca's 83,270 sampled lines 0; her Laravel boilerplate (186,459 lines) 0 newly refused and 41 no longer refused (m1). Apart from those 41 manifest lines, no line refused by the round-5 input is sent on any corpus. Ungated variant (the same tree, `_BARE_GATE` patched off in memory): 50 + 14 + 47 = 111 lines newly refused by the bare reading, nearly all code (`if (!API_SECRET && !API_KEY) {`, `$settings->setApiKey($data['api_key']);`, `/** @var array{…, password: string} $data */`), hence the gate. **Linear**: 10 adversarial units, raw and serialised, open or closed literal, timed in CPU time with the sizes interleaved: 4x the text (25 KB → 100 KB) costs at most 8x + 0.05 s, 2x the text at the 1 MB cap (500 KB → 1 MB) at most 3x + 0.05 s, and one form at the cap (up to 2 MB serialised) at most `_CAP_SLOW = 4.0` CPU seconds (not wall time). Measured worst on this machine in round 4: 0.74 s without coverage, 1.45 s under `--cov=core` (the 2 MB serialised `escapes` form; the tracer roughly doubles pure-Python time, and the former absolute 1 s bound failed there at 1.47 s); worst ratio at the cap 2.31 (linear 2, quadratic 4). Round 5, with the bare reading and the new units `bare-tokens`, `xml-tags`, `netrc-window` (`scratchpad/bruno-r5/timing.py`, the test's forms and discipline): worst 0.81 s without coverage and 1.60 s under coverage (`escapes`), `bare-tokens` 0.65 s and 1.55 s; worst ratio 4.20 at the 4x step (bound 8x + 0.05 s) and 2.05 at the cap (bound 3x + 0.05 s). Since QG r5 m2 the cap step runs on `escapes`, `digits` and `bare-tokens` only and the 4x step on all 13 units: `test_egress_credentials.py` alone under `--cov=core --cov-report=` took 88.8 s and 89.4 s wall (exit 0 twice), 50.1 s without; the cap step costs `bare-tokens` 22.1 s, `escapes` 14.5 s, `digits` 8.6 s under coverage (Francisca's round-5 figures for the old form: 30.7, 19.2, 11.6 s per form, on all ten). The three quadratic drafts fail the 4x step at 16x, rebuilt and rerun against this form (see the mutation table) (the two guards first written with a backtracking run took 1.35 s and 2.04 s on 40 KB, quadratic; a literal opened by an escaped quote 4.3 s on 40 KB). | **Residual of the layer** (round 5; the round-5 text of this cell was wrong on (h), R5-M2). Every row is pinned in `CATCH_ALL_MISSES` (21 of 127 history rows) and refused by the precise detector named: (a) no secret word on the line (14 rows): `curl -u 'admin:…'`, `curl -s -u "bob:…"`, `http -a 'bob:…'` (basic-auth flag), `mysql -p'…'` (password flag), userinfo URLs (url userinfo), and the N1/30 rows `I can't log in with curl -u …`, `it's: http -a …`, `bash -c "curl -u \"…\""`, `bash -c "mysql -p\"…\""`; (b) whitespace inside the literal, a passphrase: `export TOKEN='ab cd …'` (credential assignment); (c) under 10 characters once the slots go: `token = f"q8Z{a}r2m{b}Xv7"` (credential assignment); (d) a quote paired across an apostrophe, so the value sits inside a literal holding whitespace, where no bare token is read: `echo don't -H "Authorization: Bearer …" x'` (auth header); (e) a `{…}` run read as a template slot: `'Authorization: Basic Xk9{…}'` (auth header); (f) a query credential inside a URL literal, the URL guard: `'https://…?api_key=…'` (url query credential); (g) an XML element value on a line of its own (pretty-printed, raw and in a diff): no secret word on that line (credential xml element; also pinned at the prescreen state). Closed in round 5: the former (d) bare value (`password: <opaque>`; YAML `db_password: <opaque>` at the prescreen state), the former (h) XML element value (finding 47: the round-5 text said it was pinned and refused by a precise detector; it was neither), and two rows of the former (e). Below the gate, a bare value of letters with a symbol and no digit, or of one letter case with digits under 20 characters, is sent by the layer: `ENV API_TOKEN abc123def456` (a Dockerfile `ENV` without `=`) reaches no precise detector and is sent (pinned: `test_below_the_gate_a_bare_value_is_sent`). Since round 7 two more rows sit here, both pinned: (h) a standard base64 value opening on `/` reads as a path and is sent by the layer alone (34 of 2000 random 40-character keys on a fixed seed; with 3 read as a name and 2 without a digit, 39 of 2000, `test_random_base64_keys_miss_only_the_documented_residual`); (i) a hex value of 32 or more characters next to a path reads as a hash manifest, so a hex secret keyed by a file path is sent (`test_a_path_keyed_hex_digest_is_the_documented_residual`). A shape in (a)-(i) or below the gate that no precise detector knows is still sent; for a `diff` state the path allowlist (50) now decides first. |
| 47 | **QG r5 M1: XML element text and `.netrc` left in clear by every layer.** Reproduced by Francisca, Paulo and Marta, on this tree and on master: `<password><opaque></password>`, `<Password>`, `<db:password>`, `<secret>`, `<apiKey>`, `<ApiToken>`, `<property name="hibernate.connection.password"><opaque></property>`, `<entry key="db.password"><opaque></entry>` and `machine api.example.com login me password <opaque>` → labels `[]`, SENT by `prepare_state` with the value. The precise detectors read an assignment, a key/value or a quoted name, and an element's text is none of them; the catch-all read quoted literals only. PR3 sends diffs of config files (Maven `settings.xml`, Hibernate and Spring XML, .NET config, a `.netrc` in a project), and this was the commonest config credential shape left. What I missed: every config shape in my probes was a key/value; the finding-36 probe used a `.netrc` canary, and I wrote down that the detector did not know the shape without closing it. | **major** | I / A04:2025 / CWE-200, CWE-312, CWE-798 | `test_egress_credentials.py::test_xml_element_text_and_netrc_passwords_are_refused[×16]` (raw and serialised: 11 XML forms, among them CDATA and a pretty-printed value raw and in a diff; 4 netrc forms; `privKey`), `::test_xml_and_netrc_references_and_prose_pass[×16]`, `::test_marta_priv_key_call_is_refused`; `test_egress_pr3_consumers.py::test_xml_and_netrc_credentials_are_refused_by_prepare_state[×12]`, `…_on_the_serialised_layer[×12]`, `::test_xml_and_netrc_references_are_sent[×7]`, and the same 12 shapes with every precise detector off in `::test_with_no_precise_detector_the_catch_all_still_refuses_the_code_forms`. Before/after: with the round-5 input `credentials.py` in place, 83 tests of the suite fail, all 16 + 12 + 12 leak rows of 47 among them (SENT); on this tree, all refused. | **Fixed** in `credentials.py` by two precise detectors and the catch-all's unquoted reading (46). `_XML_ELEMENT` reads `<tag attrs>value</tag>` and a CDATA value; tag, attributes, value and CDATA are atomic, bounded and stop at `<`, so each `<` costs at most the distance to the next one. The key is the tag or a `name=`/`key=` attribute, judged by its words (`_kv_secret`), so case, a namespace and a dotted key work. The value, with its line breaks and a diff's sign set aside, is judged as a key/value value: `${…}` is a reference, `@x@` (Maven, Ant) and `#{…}` (Spring EL) are skipped, placeholders and whitespace are not a secret. `_NETRC` reads `password <token>` after `machine`/`default` on the line (a bounded window) or on a line of its own; the token is printable ASCII up to a quote or a backslash, judged by `_kv_secret` (`$PW` and `{PW}` are no password). Labels `credential xml element` and `netrc password`. Marta's minor of the same round: `priv` qualifies `key` (`privKey: "<opaque>"` is a key/value, `let privKey = decode("<opaque>")` a catch-all refusal); `pub` does not (the `pubKey` rows are sent). Sweep (46): the netrc rule adds 2 lines on the repo corpus, the finding-36 canary fixture and row 36 quoting it; the element detector adds none. | A passphrase with spaces as element text; a multi-line element cut across two prescreen chunks; `macdef` bodies and a `"`-quoted token in a `.netrc`. |
| 48 | **QG r6 follow-up: a RELATIVE path kept its words, and a hashed asset read as a secret.** Reproduced by the coordinator on the round-6 tree (`credentials.py` 8441184527c9…): the m1 rule dropped only paths starting with `~`, `/`, `./`, `../`, a drive or `$VAR/`, so a Vite/Laravel `public/build/manifest.json` entry (`"resources/js/Pages/Auth/Login.vue": {"file": "assets/Login-<hash>.js", …}`) and a hash manifest keyed by a relative path (`'app/Actions/Auth/X.php' => '<sha>'`) were refused by the catch-all: `Auth` in the path was the secret word, and the hashed asset name (a digit interleaved in its stem) or the hash the secret-looking literal. `_PATHLIKE` in `_NOT_A_SECRET` had the same absolute-only gap. Deny side: one egress per line, never a leak. | minor | — (availability of the advisory, not a disclosure) | `test_egress_credentials.py::test_relative_paths_and_hashed_assets_are_not_credentials[×9]` (the coordinator's 4 fixtures, a manifest entry whose `name` is `auth`, and the controls: an SRI `sha512-` value under a relative key, a lone asset, `<script integrity="sha384-…">`, a composer `reference`), raw and serialised; `::test_a_secret_holding_a_slash_is_not_read_as_a_path[×5]` (every precise detector off, each still refused by the catch-all: base64 with `/` and no extension, `/` inside interleaved segments, one with a dotted tail, `fetch("api/token/v1", "<opaque>")`, `"password": "<opaque>"`). Before: the 4 fixtures refused; after: sent. | **Fixed** in `credentials.py`: `_REL_PATH` is two or more `/`-joined segments whose directories are names as `_CATCH_NAME` reads them (letter runs with at most one digit run, joined by `_`, `.`, `-`), then a file with an extension (`[A-Za-z][A-Za-z0-9]{0,5}`). It is used on both sides: `_file_path` drops its words from the secret-word search (quoted literal and bare token), and `_NOT_A_SECRET` no longer takes it for a secret-looking literal. A base64 run holding `/` fails it (a digit interleaved inside a segment, or no extension), and so does a URL path without an extension (`api/token/v1` keeps its words). Mutants, each killed: relative path words kept (2 failing), the hashed asset read as a secret (1), directories widened to `[\w.-]+` (1, the dotted base64 control), the extension made optional (2). Sweep round 6b (`pr3/bruno-sweep-r5.out`, redacted section; baseline the round-6 tree): repo 601,318 lines 0 newly refused and 1 no longer refused (a comment in `credentials.py`), sample 0 and 0, Laravel boilerplate 0 and 0: none of the three corpora held a relative manifest line the layer refused, so the fixtures carry the proof. | A base64 secret shaped exactly like `name/name.ext` (letter runs, one digit run per segment, a short extension) is taken for a path, and its line's words are dropped; a precise detector still refuses it when a key names it. |
| 49 | **QG r6 majors: a nested `<value>`, a netrc `account`, a secret keyed by a file path, Stripe keys.** Reproduced by Francisca and Marta on the round-6b tree (`credentials.py` 377fda65…): (B1) `<setting name="ApiSecret" serializeAs="String">` then `<value><opaque></value>` on the next line (.NET applicationSettings) and Spring `<property name="password">` then `<value><opaque></value>`, raw and with a diff sign per line; (B2) `machine h login u account <opaque>` (netrc(5) defines `account` as an extra password); (M-B, fix-introduces-defect of the round-6 path-word drop) `"auth/secret.key": "<opaque>"`, `"config/Auth/token.php": "<opaque>"`, `"/etc/secrets/api.token": "<opaque>"` and the PHP `=>` forms; (M-A, operator scoped into this PR) `new Stripe("sk_live_<opaque>")` in a `.ts` diff and `curl … -u sk_live_<opaque>:`. Each: labels `[]`, SENT by `prepare_state`. Minors of the same round, fixed forward: `<passwordPolicy>strict-mode-v2</passwordPolicy>` and `passwordPolicy: strict-mode-v2` refused (m1); 107 of 2000 random 40-character base64 keys with `/` read as names by the catch-all (m2); the three 1 MB timing units cost 21.5 / 15.0 / 8.7 s under `--cov` (m3). | **major** (B1, B2, M-B, M-A); minor (m1-m3) | I / A04:2025 / CWE-200, CWE-312, CWE-798 | `test_egress_credentials.py`: `::test_a_nested_value_child_carries_its_parent_key[×7]` (.NET and Spring raw and as a diff, one line, CRLF with `type=` on the child, the tag naming it with CDATA; raw and serialised), `::test_a_nested_value_without_a_secret_key_or_literal_passes[×5]` (a `username`, `${DB_PASSWORD}`, a self-closed tag, a gap past 64 characters, `passwordPolicy`), `::test_the_nested_value_carry_scans_in_linear_time`, `::test_a_netrc_account_is_refused_as_a_password[×4]`, `::test_a_netrc_account_reference_or_usage_passes[×4]`, `::test_a_policy_about_a_secret_passes[×9]`, `::test_a_secret_named_after_its_policy_is_still_refused[×4]` (`password_reset_token = "<opaque>"` stays refused, decided by execution), `::test_a_secret_keyed_by_a_file_path_is_refused[×7]` and `::…_by_the_catch_all_alone[×7]`, `::test_a_path_keyed_hex_digest_is_the_documented_residual`, `::test_a_slashed_base64_key_is_not_read_as_a_name`, `::test_random_base64_keys_miss_only_the_documented_residual`, `::test_stripe_keys_are_refused_by_the_vendor_vocabulary[×6]`; `test_harness_scanner.py::TestSecrets::test_secret_labels` (5 Stripe rows) and `::test_stripe_lookalikes_are_not_keys[×4]`; `test_egress_pr3_consumers.py::test_round_6_majors_are_refused_by_prepare_state[×7]` (each inside an allowlisted file: a `.cs` and a `.java` test, a `.sh`, a `.ts`, a `.php`) and `::test_round_6_majors_in_an_xml_or_config_file_stop_at_the_path[×7]`. Before (the round-6b `credentials.py` and `harness_scanner.py` copied in, then restored by copy, sha256 checked): 52 of the new rows fail, every leak row SENT and 7 of the 9 policy rows refused; after: all pass. | **Fixed.** (B1) `_XML_CARRY` reads an opening tag (not self-closed) whose attributes are atomic and bounded (512), then a gap of at most 64 whitespace characters, diff signs or serialised `\n`/`\r`/`\t` (atomic), then a `<value>` child, whose text `_XML_ELEMENT` reads; the tag and its `name=`/`key=` values are the carried names, judged by `_kv_secret`. Each `<` costs a constant: linear (the 4x step within 8x + 0.05 s on three adversarial units). (B2) `(?:password\|account)` in both `_NETRC` patterns; the round-7 sweep found CLI usage strings (`account: 'account [info]'`), so a token that is a bracketed option list (`_NETRC_USAGE`) is no password. (M-B) the catch-all reads a line as pieces (`_Piece`: literal or bare token, secret-looking or a path); a path's words are dropped only when its neighbour value (`_hashed_pair`) is a hex digest of 32+, a declared hash, a path or a hashed asset (`name-<hash>.ext`). The round-6b fixtures (Vite manifest, relative hash manifest) stay sent; the round-5 benign row `./app/Auth/Guard.php  <base64>` is the same shape as the leak and moved to the leak side, replaced by a bare path next to a hex digest and a `sha256sum` line. (M-A) `harness_scanner._SECRET_PATTERNS` (the single vendor vocabulary `egress_secret_labels` reads): `Stripe key` = `\b[sr]k_(?:live\|test)_[A-Za-z0-9]{20,}` (gitleaks `stripe-access-token`), `Stripe webhook secret` = `\bwhsec_[A-Za-z0-9]{20,}`. (m1) `policy, reset, rule, rules, hint, expiry, min, max` in `_POINTER_WORDS`, and `_POLICY\|_RESET\|_RULES?\|_HINT\|_EXPIRY\|_MIN\|_MAX` in `_POINTER_SUFFIX`. (m2) `_CATCH_NAME_VALUE`: from 20 characters on, `/` and `+` separate a value's name segments only when it also holds a separator outside the base64 alphabet (`prod/db-password-2024` stays a name); 107 → 39 of 2000, the rest pinned as residual (h). (m3) under coverage (`sys.gettrace()`, `COV_CORE_SOURCE` or a live `coverage.Coverage`) the cap step runs 250 KB → 500 KB with a ceiling of `_CAP_SLOW / 2 = 2.0` CPU seconds, the same bound per character; without coverage it stays 500 KB → 1 MB within 4.0 s. Measured under `--cov=core`: `bare-tokens` 12.7 s, `escapes` 9.5 s, `digits` 5.0 s per test (round 6: 21.5, 15.0, 8.7 s), 13 passed. Sweep round 7 (`pr3/bruno-sweep-r5.out`, round-7 section, redacted; baseline the round-6b tree): repo 601,974 lines 1 newly refused (the source line of a round-7 fixture that holds a digest's fragments) and 4 no longer refused (the m1 policy fixtures); Francisca's sample 0 and 0; her Laravel boilerplate 2 newly refused, both the AWS documentation example key, a base64 literal with `/` (m2, correctly refused). The first B2 draft also refused 2 repo code lines and 2 sample lines, CLI usage strings (`account: 'account [info]'`); `_NETRC_USAGE` sends them, 0 in the final run. | (h) and (i) of 46; a `<value>` more than 64 characters of whitespace after its parent; netrc `macdef`. For a `diff` state, row 50 decides first. |
| 50 | **Structural: the `diff` class is a suffix allowlist (operator decision 2026-09-24, "Ronda 7: majors + allowlist por tipo de ficheiro").** Six review rounds each found one more syntax that binds a secret in a config file (a subscript, a setter, a dotted name, a Ruby block, XML, netrc, a nested value). The detectors cannot prove absence; what leaves the machine as `diff` state must be bounded by what the file IS. | design finding (closes the config-file class for `diff` state) | I / A04:2025 / CWE-200, CWE-538 | `test_egress_pr3_consumers.py`: `::test_a_diff_of_a_file_outside_the_allowlist_never_leaves[×30]` (config: yml, yaml, json, toml, xml, properties, ini, conf, cfg, plist, `.config`; no suffix: `.env`, `.netrc`, `Dockerfile`, `Makefile`, `C:\app\.env`; `deploy/prod.env`; key material: pem, key, crt, p12, jks; lock: `poetry.lock`, `yarn.lock`, `go.sum`; binary: png, so; an empty path, a trailing space; each as a dict and serialised), `::test_a_diff_state_that_names_no_file_is_refused[×8]`, `::test_every_allowlisted_suffix_leaves[×36]` (every suffix of the list once, `.PY` upper case), `::test_every_file_entry_of_a_list_must_be_allowlisted`, `::test_a_git_header_inside_the_diff_names_its_file_too[×5]` (`.env`, a rename to `.yaml`, a rename FROM `.yaml` (the a-side config header `diff --git a/config/app.yaml b/core/a.py`, added in round 8), a quoted octal name ending in `.key`, an unreadable header), `::test_a_git_header_of_source_files_leaves`, `::test_a_path_class_refusal_is_audited_without_the_path`, `::test_other_classes_are_not_gated_by_the_path`, `::test_the_prescreen_skips_config_files_before_diffing_and_records_them`, `::test_the_prescreen_filter_runs_before_git`, `::test_slop_score_prose_files_carry_their_path_and_leave`; the existing ui-in-ts and slop-score tests unchanged and green. | **Added.** The boundary is `privacy.DIFF_SOURCE_SUFFIXES`: `.py .pyi .js .mjs .cjs .ts .tsx .jsx .vue .svelte .php .rb .go .rs .java .kt .swift .c .h .cc .cpp .hpp .cs .sh .bash .zsh .sql .css .scss .less .html .md .mdx .txt .rst .bats` (36). **Two enforcement points.** (1) `prepare_state(…, state_class="diff")` refuses first, before any secret scan, with `DecisionUnavailable("egress-denied:path-class")` and an audit line (`layer: privacy`, finding kind `path-class`, label `pathless` or `suffix-outside-allowlist`, never the path) unless every file the state names ends in an allowlisted suffix (case-insensitive, `\` read as `/`): the dict's `path`, each file entry's `path` in a list (at the top or one list below), and both sides of every `diff --git` header inside a `diff` (an unreadable header refuses). A file entry is a dict holding `path`, `diff`, `content` or `prose`; a diff state naming no file, a JSON string included, is refused (fail-closed: the class must name its file). (2) `qg_prescreen.run_prescreen` partitions the changed files BEFORE diffing (`partition_paths`): a file outside the list is never read by `git diff`, never chunked and listed in the report and `PRESCREEN.json` as `skipped_paths: [{path, reason: "path-class"}]`; the reviewer list (`dispatch_reviewers(compute_tier())`) is computed as before. **Producers.** `quality.diff_state(diff, path)` and `quality.prose_state(prose, path)` now require the path; `qg_prescreen.ask_chunk(…, path=name)`, `slop_check._score_file` passes the prose file's name (`.md`/`.mdx`/`.txt`, all allowlisted, so slop-score is unchanged); `governance.ui_state` already carried it (`.ts`/`.js`/`.mjs`/`.cjs`, all allowlisted). The replay builds the prescreen state with the corpus diff's first `diff --git` path (the corpus row for `.github/workflows/ci.yml` is now refused, as its live counterpart would be skipped) and the slop state with `<case id>.md`. **What stays.** The XML, netrc and every other detector keep running under the allowlist and keep their tests: a source file can hold a config text (an XML string in a `.cs` test, a heredoc in a `.sh`), and row 49's rows prove them there. Mutants, each killed: the allowlist emptied, a pathless state allowed, the prescreen filter off. | Secrets in allowlisted files are still only as safe as the detectors (46-49); a `.txt` or `.md` can hold anything; a config file renamed or copied under a source suffix. |
| 51 | **QG r7 B1: the Decision 2c boundary judged the link NAME while the reader followed the link.** Reproduced by the coordinator (`scratchpad/symrepo`) and Francisca (`r7/francisca-reissue/fx.py`) on the round-7 tree: a committed `config.yaml` and the untracked links `util.py -> config.yaml`, `notes.md -> config.yaml`. `partition_paths` kept `util.py` (its name ends in `.py`), `file_diff` read the YAML through `readable_inside` (which follows links, by design, to refuse a link OUT of the project) and `prepare_state(diff_state(d, "util.py"))` sent it, while the same diff under `config.yaml` was refused `egress-denied:path-class`; slop-score read `notes.md` the same way. Cause: two checks on two different objects. The allowlist looked at the name, `readable_inside` returned the resolved file, and nobody judged the file that was actually read. What I missed in round 7: I pinned links pointing outside the project (finding 36) and never a link inside it to a class the allowlist refuses. | **major** | I / A01:2025 / CWE-59, CWE-200 | `test_egress_pr3_consumers.py`: `::test_the_prescreen_skips_a_link_to_a_config_file_on_record[named, derived-from-git]` (the canary never on the wire, `{path: util.py, reason: path-class}` in `skipped_paths`, and the link to `real.py` still read and sent), `::test_file_diff_never_reads_through_a_link_to_a_config_file` (the second layer, `_untracked_diff`), `::test_the_slop_section_skips_a_link_to_a_config_file_on_record` (`prose_text` returns `("", "path-class")`, one request, for `guide.md -> real.md` only), `::test_a_name_is_judged_by_the_file_it_resolves_to[×5]` (a link to `.yaml`, a dangling link to `.yaml`, a link out of the project, a hard link to `config.yaml`, a hard link to `real.py`), `::test_a_link_to_source_and_a_plain_file_are_still_allowed[×4]` (links to `.py` and `.md`, a plain file, a deleted file), `::test_the_prescreen_records_a_hard_link_as_path_class`. Before the fix (the three consumer edits reverted): 5 of these fail; after: all pass. The round-7 repros re-run against this tree (`scratchpad/bruno-r8/repro-r8.out`): `partition_paths` skips `util.py` as `path-class`, and neither `file_diff` nor `prose_text` returns any of the YAML. | **Fixed** at the boundary, once, and in each consumer. (1) `jev_advisory.resolved_path_allowed(project_dir, name)`: True only when the name passes `privacy.diff_path_allowed`, the name resolves inside the project and outside `.git` (`readable_inside`), the RESOLVED file's name passes the allowlist too, and the file is not a regular file with `st_nlink > 1`; a missing file (a deletion, a dangling link) is judged on both names, since nothing is read. (2) `qg_prescreen.partition_paths(project_dir, changed)` uses it, so a link or a hard link to a refused class lands in `skipped_paths` with reason `path-class` (the ADR reason, not a new one) and is never diffed; `_untracked_diff` checks it again before it reads. (3) `slop_check.prose_text` checks it before either read (the tracked `git diff` path included) and the row says `path-class`. (4) `prepare_state` stays name-based: it has no project directory and cannot resolve a link; its module docstring now says so and names the consumer-side check. **Hard links: refused**, for tracked and untracked files alike (the partition runs before git says which): a hard link is the same inode under two names and neither the name nor `resolve()` shows which one is the "real" file; only `st_nlink > 1` hints it. Price: `git checkout` never creates hard links in a worktree, and pnpm's hard links live in `node_modules`, which git ignores and the derived changed-file list never contains; a false refusal costs one advisory file, on record. A tracked symlink to a config file is now skipped too, where `git diff` would have sent only its target string: conservative, one advisory file. | A link swapped between the check and the read (needs a concurrent writer in the worktree, who could edit the file directly anyway); a hard link on a file system that reports `st_nlink == 1` (some network and FUSE mounts); a config file renamed or copied under a source suffix (row 50's residual); a future consumer that reads from disk and skips `resolved_path_allowed` (`prepare_state` cannot catch it). ui-in-ts sends only the text the agent is about to write (`new_string`, `content`), never a disk read, so a `.ts` link to a config file carries the new text alone: accepted. |
| 52 | **QG r8 M1: changed-file names reached git as PATHSPECS, and a directory passed the boundary.** Found by Francisca (`r8/francisca/fx8.py`, `fx9.py`, `fx10.py`), reproduced by Marta on a fresh repository (`r8/marta/repro.py`) and by me (`scratchpad/psrepo`: a committed `config.yaml` changed, a tracked directory `big.md/inner.yaml`). `partition_paths(repo, [":!zz.py", "big.md", "config.yaml"])` kept `:!zz.py` and `big.md`; `file_diff(repo, base, ":!zz.py")` returned the whole `config.yaml` diff, because git reads `:!zz.py` as "every file but zz.py"; `resolved_path_allowed(repo, "big.md")` was True for a directory. On the wire: the first chunk of that diff was refused by its `diff --git a/config.yaml` header, chunks 2 to 9 carry no header and were SENT with the config content; slop-score (act by default) SENT `prose_text(":!zz.md")` and the tracked directory `big.md` the same way. Cause: three gaps that each let the other two through. The name went to git as a pattern (`qg_prescreen._git`, `evidence_checks._git_tracks` and `_added_lines`, which slop-score reuses); the round-8 guard judged the resolved NAME and link count, never the file type, though its docstring said it judged the file that is read; a continuation chunk named no file, so `prepare_state` judged it by the state's `path` alone. What I missed in round 8: I tested names that are links and never names that are patterns, and I read "the file that is read" as "the name that is read". | **major** | I / A01:2025 / CWE-200, CWE-88 (argument injection into git), CWE-59 | `test_egress_pr3_consumers.py`, fixture `specs` (a committed `config.yaml` with 3000 changed lines, tracked directories `big.md/` and `lib.py/` holding changed YAML, a tracked directory `old.md/` the change deleted, untracked files `:!x.py` and `:!x.md`): `::test_the_prescreen_reads_a_pathspec_name_as_one_file[×7]` (`:!x.py`, `:!x.md`, `big.md`, `lib.py`, `*.py`, `b*.md`, `old.md`; the canary absent from `file_diff` raw AND from every request body of `run_prescreen`, while `+APP = 2` of `app.py` is on the wire), `::test_the_slop_section_reads_a_pathspec_name_as_one_file[×5]` (`:!x.md`, `big.md`, `*.md`, `b*.md`, `old.md`; raw `prose_text` and the `check_slop_score` wire, where `:!x.md`'s own prose is sent), `::test_a_directory_name_is_skipped_on_record`, `::test_a_directory_or_a_fifo_is_refused_on_the_resolved_side`, `::test_magic_and_glob_names_reach_git_literally`, `::test_every_git_call_of_both_readers_is_literal` (every git call from `file_diff` and `prose_text` over the seven names spied: argv `git --literal-pathspecs …` and `GIT_LITERAL_PATHSPECS=1` in its env), `::test_no_reader_calls_subprocess_past_the_literal_helper` (no `subprocess.run(` in `qg_prescreen.py` or `slop_check.py`; `_git_tracks` and `_added_lines` call `literal_git.run`, by AST), `::test_a_diff_covering_more_than_the_name_is_refused_whole`, `::test_the_names_check_alone_refuses_a_directory_in_prose`, `::test_every_chunk_carries_its_file_header_and_is_judged` (a config diff under the name `x.py`: more than one chunk, each at most `MAX_DIFF_CHARS`, each carrying `diff --git a/config.yaml b/config.yaml`, each refused `egress-denied:path-class`); `test_qg_prescreen.py::test_a_60k_diff_is_three_requests_each_under_the_cap` now also asserts that every chunk opens with the carried `diff --git a/big.py b/big.py` line. Round-8 minors: `::test_a_tracked_hard_link_to_config_prose_is_refused_before_git` (Francisca m1) and `::test_a_target_whose_stat_fails_is_refused` (m2). The three repros re-run on this tree: nothing of `config.yaml` or `inner.yaml` in any `file_diff`, chunk or `prose_text`; `big.md` skipped as `path-class`. | **Fixed** in three layers, each mutation-proven on its own. (1) **Literal names.** `core/governance/literal_git.py` is the one git runner of both readers: `git --literal-pathspecs <args>` with `GIT_LITERAL_PATHSPECS=1` in the environment. `qg_prescreen._git` (so `file_diff` and the tracked probe before `_untracked_diff`) and `evidence_checks._git_tracks` / `_added_lines` (so `slop_check.prose_text`, and security-grep with them) go through it. The flag, not only the variable: a git too old to know it exits 129 on every call. The prescreen then sends nothing, because its tracking probe reads only exit 1 as "untracked" and `names_exactly` answers False on any non-zero exit (round 9); slop-score reads the name as untracked and scores the whole file, which is still the one allowlisted regular file the name resolves to. (2) **Regular files only.** `jev_advisory.resolved_path_allowed` refuses an existing target that is not a regular file (`stat.S_ISREG`: a directory, a FIFO, a socket), before the link count; a missing file is still judged on both names. (3) **Every chunk judged.** `file_diff` returns a diff only when `literal_git.names_exactly` (`git diff --relative --name-only -z base -- name`; `--relative` since round 9, so a `project_dir` below the repository root compares relative names) lists exactly `[name]`; `slop_check.prose_text` asks the same when the tracked diff added lines, so a name whose diff covers another file is `path-class`. And `qg_prescreen.carry_git_headers` prefixes every chunk that starts inside a file section with that section's `diff --git` line (its room reserved in `chunk_diff`, so no chunk passes `MAX_DIFF_CHARS`): `prepare_state` now judges each chunk by the file its lines belong to, not by the state's `path`. | A literal pathspec still names a directory's contents; layers 2 and 3 answer that, and a deleted directory (missing, so the names alone admit it) is caught by layer 3 only. A name git prints differently (`./a.py`, `src//a.py`; `-z` keeps every other name raw) fails `names_exactly` and loses its advisory read: fail-closed, on record as a `path-class` row in slop-score, and as no row in the prescreen. Executed in round 9 (`bruno-r9/row52.py`): `file_diff(repo, base, "./a.py")` is `''`, and `run_prescreen` reports `skipped_paths == []` with 0 POSTs; `prose_text(repo, base, "./a.md")` is `('', 'path-class')`, and `_score_file` records `reason: path-class`. An untracked name in a spelling git would not print (`new.py/`, `./new.py`, `x.py/` for a tracked `x.py`) is not read whole by the prescreen (round 9); slop-score still reads it whole, the same allowlisted regular file. A file swapped for a directory between the check and the read (a concurrent writer in the worktree, as in row 51). The typecheck scope (`_added_line_numbers`) shares `_git_tracks` and `_added_lines`, so it reads names literally too; the other git calls in `evidence_checks` (`_diff_base`, `_head_sha`, `_derive_changed_files`, `_tree_state_digest`) take no changed-file name and are unchanged. |

Changes to tests outside this lane: `test_egress_policy.py` used `token: xoxb-…` as its vendor-secret fixture; after finding 33 that text holds a key/value credential too, so 12 fixtures now read `slack xoxb-…` and `test_secret_denies` keeps `token: xoxb-…` to pin the two labels (`["secret", "secret"]`). `test_egress_pr3_consumers.py` sets `qg-prescreen: act` in its temporary config: the site is `shadow` by default since the PR3 replay gate, and a shadow read sends nothing to capture.

### PR2 round-6 carries

- **m1 (timing flake):** `test_serialised_many_match_paste_scans_in_linear_time` now alternates the two sizes 5 times, keeps each minimum and measures process CPU time, not wall time; the sizes are 50 KB and 200 KB (4x the text: linear ≈ 4x the time, quadratic ≈ 16x), with the bound `large ≤ 8 x small + 0.05 s` and the absolute `large ≤ 1.0 s`. Under 32 busy loops on 16 cores (2x CPU load, the condition under which the round-6 report saw the old 3x wall ratio, 100 → 200 KB, fail 2/8): interleaved wall time with a 4x ratio still failed 1/8 (0.068 → 0.339 s); CPU time 50 → 200 KB passed 8/8, ratios 3.85, 4.14, 3.93. A quadratic mutant (bisect replaced by a linear scan of the quote marks) fails it: 0.055 → 0.540 s.
- **m2 (`_VALUE_CAP`):** the code is changed to match the texts, not the reverse: `limit = start + floor + _VALUE_CAP`, and a read that stops at the cap is `capped`, which removes the whole-reference exemption. Why: with `start + max(floor, 256)` a head of 256 or more characters was read with no room past it, so `-u 'bob:$` + 300×`A` + ` tail9!'` read as the reference `$AAA…` and was SENT; and a capped `'$(` + 400×`x` + `) tail9!'` matched `^\$\(.*\)?$` on its head alone. Both now refused. Timed tests re-run green (the 11 adversarial rows, raw and serialised). Differential against the round-5 module on every line of the repository (540,383 lines): the only new labels are the finding 33 key/value and typed-assignment ones, and 0 lines lost a label. The four texts (comment, docstring, 28(g), finding 32) already said "256 past the match"; 28(g) now adds the capped rule.
- **m3 (lower edges):** `test_a_password_flag_after_490_chars_of_arguments_is_refused[mysql, redis-cli, docker, httpie]` (470 to 500 characters of real arguments, raw and serialised) kill `_ARGS{0,16}` (4 of 4). The quoted-extension rows: `test_a_literal_tail_past_a_long_reference_head_is_refused` kills the round-5 read as it shipped (the `start + max(floor, 256)` formula AND no capped rule); with the capped rule in place the formula alone passes it, since a capped read is refused anyway (QG PR3 r1 m4, Francisca). `test_a_reference_of_exactly_the_cap_past_a_one_char_floor_is_read_to_its_end` separates the two formulas: the bare run is `$` (floor 1) and the single-quoted value is a 256-character `$(…)`, read to its closing quote by `start + floor + 256` and capped one character short by `start + max(1, 256)`, which then refuses a reference. `test_a_capped_value_gets_no_reference_exemption` kills the exemption on capped values; `_VALUE_CAP = 8` cannot fail a LEAK row any more, since a shorter read only refuses more, so it is killed on the benign side by `test_a_long_reference_read_to_its_end_is_not_a_secret` (a 203-character `$(…)` reference read to its closing quote).
- m4/m5 (28(f), 28(g) hardening) stay in PR5, not implemented here.

### Mutation proof (PR3)

In-memory mutants (a pytest plugin patches one behaviour; no file is edited, so no restore is needed and the other lanes' concurrent edits are never at risk). Suite per mutant: `test_egress_pr3_consumers.py`, `test_egress_credentials.py`, `test_qg_prescreen.py`, `test_egress_policy.py` (3159 tests; baseline 3159 passed).

| Mutant | Failing | Killing tests (examples) |
|---|---|---|
| 33 key/value detector off | 12 | `test_source_code_credentials_never_reach_the_wire[json, docker-auth, …]`, `test_a_ts_file_embedding_a_secret_is_refused_before_the_post[×2]` |
| 33 annotation group removed | 2 | `…[py-typed]`, `…[ts-typed]` |
| 33 auth header back to the PR2 regex | 3 | `…[headers-json]`, `…[headers-quoted]`, `…[headers-template]` |
| 33 key judged by substring, not by words | 4 | `test_ordinary_code_still_reaches_the_wire[accessorKey…, d3-sankey…, tests pass…]`, `…[docker-auth]` |
| 33 UPPER_SNAKE name guard off | 1 | `…["master_key": "os.environ/ARKA_GATEWAY_KEY"]` |
| 33 placeholder guard off | 1 | `…["api_key": "{api_key}"]` |
| 33 member-access guard off | 1 | `…[password: process.env.DB_PASSWORD,]` |
| 33 number guard off | 1 | `…[max_context_tokens: int = 200_000]` |
| 33 whitespace-is-prose rule off | 1 | `…[passwordHint: "Use at least 12 characters"]` |
| 34 END-line detector off | 1 | `test_a_private_key_split_across_chunks_never_reaches_the_wire` |
| 35 hard line cut restored | 3 | `test_a_token_straddling_the_chunk_cut_never_leaves_in_halves[×2]`, `test_qg_prescreen.py::…_keeps_only_its_head` |
| 35 fixed reserve and `chunk[:limit]` restored | 1 | `test_a_long_file_name_never_cuts_a_chunk_after_the_fact` |
| 36 `_untracked_diff` reads any path | 4 | `test_the_prescreen_never_sends_a_file_outside_the_project[×3]`, `test_an_untracked_symlink_git_lists_is_not_followed` |
| 36 `prose_text` reads any path | 3 | `test_the_slop_section_never_scores_a_file_outside_the_project[×3]` |
| 37 leaf refusal not audited | 5 | `test_a_leaf_secret_refusal_is_audited_without_the_value[×4]`, `test_a_privacy_refusal_rotates_an_oversized_audit_like_any_line` |
| 37 text-layer refusal not audited | 1 | `test_a_text_layer_refusal_on_the_degraded_path_is_audited` |
| PR2-8 rotation off (new deny lines) | 1 | `test_a_privacy_refusal_rotates_an_oversized_audit_like_any_line` |
| m2 round-5 limit formula (see the round-1 re-run below: this row was wrong) | 2 | `test_a_literal_tail_past_a_long_reference_head_is_refused`, `test_a_capped_value_gets_no_reference_exemption` |
| m2 capped exemption restored | 1 | `test_a_capped_value_gets_no_reference_exemption` |
| m3 `_VALUE_CAP = 8` | 1 | `test_a_long_reference_read_to_its_end_is_not_a_secret` |
| m3 `_ARGS{0,16}` | 4 | `test_a_password_flag_after_490_chars_of_arguments_is_refused[×4]` |
| m1 quadratic mark lookup (bisect replaced by a linear scan) | 2 | `test_serialised_many_match_paste_scans_in_linear_time` (0.055 → 0.540 s CPU) and one adversarial timed row |

22 of 22 mutants killed, with one row wrong: re-run by file edit in Quality Gate round 1, the round-5 formula on its own fails neither of the two tests it names (Francisca, m4); it takes the formula AND the capped rule removed together. The table below replaces that row.

Quality Gate round 1 re-run (findings 38-40, m4). Each mutant edits one file of this lane (`credentials.py` or `jev_advisory.py`), runs the suite, and restores the file by copy; `shasum -a 256` equal after every restore (`96508865…` and `7f5a61f3…`). Suite per mutant: `test_egress_pr3_consumers.py`, `test_egress_credentials.py`, `test_qg_prescreen.py`, `test_egress_policy.py`, `test_evidence_checks_slop.py` (baseline 3250 passed; another lane was adding tests to the tree during the runs, so earlier runs counted 3240-3247).

| Mutant | Failing | Killing tests (examples) |
|---|---|---|
| Round-1 `credentials.py` restored whole | 26 | every leak row of 38 and 39 |
| 38 `_kv_value` back to the old rule (`"` → `_DOUBLE`, bare → `_NONE`) | 9 | `…[json-dollar]`, `…[yaml-dollar]`, `test_a_dollar_inside_a_source_code_string_is_data[{"password": …}]` |
| 38 `_NO_SUBSTITUTION = (_SINGLE,)` | 15 | the `$` rows of all three detectors |
| 38 assignment code reading off | 3 | `…[py-dollar]`, `…[password = "…"]`, `…[API_KEY: str = "…"]` |
| 38 header code reading off | 2 | `…[{"Authorization": "Bearer q8Zr$…"}]`, `…[headers.set("Authorization", "Bearer q8Zr$…")]` |
| 38 key/value scan resumes past a quoted value again | 8 | the serialised half of the 8 key/value rows (the run also logged 1 error in `test_evidence_checks_slop.py::test_abstaining_jev_scores_nothing`, not a kill: it passed in 5 re-runs on the restored tree) |
| 38 bare-`$` variable guard off | 2 | `…['password' => $this->password,]` (both benign lists) |
| 38 template slots kept | 1 | ``test_a_whole_reference_in_source_code_is_still_a_reference[token: `${prefix}-${suffix}`]`` |
| 38 dotted `${env.X}` reference removed | 1 | `…[password: "${env.DB_PASSWORD}"]` |
| 39 call-argument form removed | 2 | `…[headers.set("Authorization", "Bearer q8Zr$…")]`, `…[req.Header.Set("Authorization", "<opaque>")]` |
| 39 `_QUOTED_BEARER` off | 3 | `…[bearer-only]`, `…[headers["Authorization"] = …]`, `…[conn.setRequestProperty(AUTH_HEADER, …)]` |
| 39 backtick-opened value read as code | 1 | ``test_a_header_built_from_a_variable_is_not_a_secret[headers: { Authorization: `Bearer ${scheme}${token}` }]`` |
| 39 scheme-word lookahead removed | 1 | `test_a_header_built_from_a_variable_is_not_a_secret['headers.set("Authorization", "Bearer " + token)',]` |
| 39 scheme-word lookahead as `\b` | 1 | `test_an_auth_header_as_a_call_argument_is_refused[curl -H 'Authorization: Token token=…']` |
| 40 round-1 `jev_advisory.py` restored whole | 9 | `test_git_metadata_is_never_sent_in_any_case[.GIT, .Git]`, the slop `.GIT`/`.Git` rows, `test_git_metadata_is_refused_in_any_case[×3]` |
| 40 part test case-sensitive again | 1 | `test_a_nested_git_dir_in_another_case_is_refused` |
| 40 inode comparison off | 1 | `test_the_root_git_dir_is_refused_by_identity_under_another_spelling` |
| m4 round-5 formula alone | 1 | `test_a_reference_of_exactly_the_cap_past_a_one_char_floor_is_read_to_its_end` |
| m4 round-5 formula and no capped rule | 2 | `test_a_literal_tail_past_a_long_reference_head_is_refused`, `test_a_capped_value_gets_no_reference_exemption` |

19 of 19 killed. On APFS the two halves of the finding 40 fix each refuse the root `.GIT` alone; each half is killed by its own row, and the end-to-end rows die only with both removed.

Quality Gate round 2 re-run (finding 41, m7, m8). The same procedure: each mutant edits `credentials.py`, runs the suite, and restores the file by copy; `shasum -a 256` equal after every restore (`455c384a…`, the final tree). Suite per mutant: `test_egress_credentials.py`, `test_egress_pr3_consumers.py`, `test_egress_policy.py`, `test_qg_prescreen.py`, `test_evidence_checks_slop.py`.

| Mutant | Failing | Killing tests (examples) |
|---|---|---|
| Round-2 input `credentials.py` restored whole | 39 | every leak row of 41 at both levels, the m7 and m8 rows |
| 41 prefix never matches in `_ASSIGNMENT` | 11 | `…[SECRET_KEY = b"…"]`, `…[SECRET_KEY = rb"…"]`, `…[password = r"…$…"]`, `…[token = f"{prefix}…"]` |
| 41 prefix never matches in `_KEY_VALUE` | 3 | `…[{"password": b"…"}]`, `…[password: f'{a}…']`, `…[dict-bytes]` |
| 41 `:=` separator removed | 4 | `…[password := "…"]` (×3), `…[go-walrus]` |
| 41 `var`/`const` typed form removed | 3 | `…[var password string = "…$…"]`, `…[const apiKey string = "…"]`, `…[go-var-typed]` |
| 41 keyword-argument reading off | 8 | `…[conn = psycopg2.connect(host=h, password="…$…")]`, `…[db.connect(user="u", password="…$…")]`, `…[conn = connect(password="…$…" if prod else None)]`, `…[kwarg-api-key]` |
| 41 `_CALL_BEFORE` alone off | 1 | `…[conn = connect(password="…$…" if prod else None)]` |
| 41 `_CALL_AFTER` alone off | 1 | `…[    password="…$…",]` (an exploded call) |
| 41 a prefix read as a bare `=` (shell) | 1 | `…[SECRET_KEY=b"…$…"]` |
| 41 an annotation read as a bare `=` (shell) | 5 | `…[password = "…$…"]`, `…[API_KEY: str = "…$…"]`, `…[var password string = "…$…"]`, `…[py-dollar]`, `…[go-var-typed]` |
| 41 f-string slots kept (assignment) | 3 | `test_templates_references_and_placeholders_in_string_forms_pass[token = f"{prefix}{suffix}"]` (both levels), `…[token = f"TOKEN={tok}"]` |
| 41 f-string slots kept (key/value) | 2 | `…["token": f"{head}{tail}"]`, `…["api_token": f"TOKEN={tok}"]` |
| 41 label-run rule off (every run is a value) | 2 | `…[token = f"TOKEN={tok}"]`, `…["api_token": f"TOKEN={tok}"]` |
| m7 `${NAME:-}` reference forms removed | 6 | `…[POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-}"]`, `…["password": "${X:?required}"]` (both levels) |
| m7 `-$OTHER` resume guard off | 1 | `…[password: "${DB_PASSWORD:-$FALLBACK_PW}"]` |
| m8 `%(…)s` placeholder removed | 2 | `…["secret": "%(SECRET)s"]` (both levels) |
| m8 bearer placeholder guard off | 2 | `…[doc: "Bearer token_here_placeholder"]` (both levels) |

17 of 17 killed. In the first run 4 mutants survived: `_CALL_BEFORE` alone, the f-string key/value slots, the label-run rule (my benign row carried a key that names no secret) and "a prefix makes the assignment code". Each one showed a missing row or a redundant term. The rows were added. The prefix and annotation terms were removed, since the separator test already covers them. The two mutants that deleted the prefix group outright crashed on the missing group, so they counted as kills without proving anything. I replaced them with the never-matching prefixes above.

Quality Gate round 3 re-run (findings 42 and 43, m10, m12). The same procedure: each mutant edits `credentials.py`, runs the suite, and restores the file by copy; `shasum -a 256` equal after every restore (`a259b010…`, the tree the mutants ran on). Suite per mutant: `test_egress_credentials.py`, `test_egress_pr3_consumers.py`, `test_egress_policy.py`, `test_qg_prescreen.py`, `test_evidence_checks_slop.py`; the lookbehind mutant ran on the timed tests only, since it turns every scan quadratic.

| Mutant | Failing | Killing tests (examples) |
|---|---|---|
| Round-3 input `credentials.py` restored whole | 44 | every leak row of 42 at both levels, m10, m12, `…[secret-word-run]` |
| 42 quoted-name detector off | 27 | `test_a_secret_named_in_quotes_is_refused[define('AUTH_KEY', …), os.environ["API_TOKEN"] = …, …]`, `…_by_prepare_state[wp-salt, flask-config, …]` |
| 42 subscript separator removed | 11 | `…[os.environ["API_TOKEN"] = …]`, `…[app.config["SECRET_KEY"] = …]`, `…_by_prepare_state[php-config]` |
| 42 comparison forms removed (`=` only) | 1 | `…[if request.headers["X-Api-Key"] == …]` |
| 42 positional `,` removed | 13 | `…[define( 'DB_PASSWORD', … )]`, `…[os.environ.setdefault(…)]`, `…[PASSWORD = os.getenv("PW", …)]` |
| 42 .NET `value=` removed | 3 | `…[<add key="ApiKey" value="…" />]`, `…[dotnet-appsettings]` (both layers) |
| 42 scan resumes past the value | 1 | `…[cfg.get("a", "API_TOKEN", …)]` |
| 42 quoted name not judged by its words | 2 | `test_a_quoted_name_without_a_literal_secret_passes[os.environ["GIT_COMMIT"] = …]`, `…_is_sent[…]` |
| 42 `pw` word removed | 3 | `…[PASSWORD = os.getenv("PW", …)]`, `…[getenv-default]` (both layers) |
| 42 setter detector off | 6 | `…[ds.setPassword(…)]`, `…[builder.password(…)]`, `…[t.set_token(…, ttl=60)]`, `…[setter]` |
| 42 setter verb rule off | 1 | `…[secret = get_secret("prod/db-password-2024")]` |
| 42 setter single-word rule off | 1 | `…[builder.password(…)]` |
| 42 setter lookbehind removed | 3 | `test_adversarial_text_scans_under_a_second[one-identifier, secret-word-run]`, `test_finding_42_detectors_scan_in_linear_time[placeholder-words]` |
| 42 pair detector off | 6 | `…[requests.get(url, auth=("admin", …))]`, `…[auth-tuple]` (both layers) |
| 42 pair: user as a name removed | 1 | `…[s.auth = HTTPBasicAuth(user, …)]` |
| 42 pair: `Basic`/`Digest` alternative removed | 1 | `…[session.mount(a, HTTPDigestAuth("admin", …))]` |
| 42 pair: `auth=` class wrapper removed | 1 | `…[requests.get(u, auth=HTTPProxyAuth("admin", …))]` |
| m10 longest run instead of joined | 1 | `…[token = f"q8Z{a}r2m{b}Xv7"]` |
| m12 bearer placeholder by substring again | 2 | `…[headers[AUTH] = "Bearer …-example-…"]`, `…["Bearer eyJ….dummyX….abc"]` |
| m12 key/value placeholder by substring again | 1 | `…[token: "…-example"]` |
| m12 RFC 2606 address removed | 1 | `…["token": "user@example.com"]` |
| m12 short prefix removed | 2 | `…[api_key: "sk-your-api-key-here"]`, `…["SEQUENZY_API_KEY": "seq_user_your_key_here"]` |
| m12 digits per word removed | 1 | `…[headers[AUTH] = "Bearer access_token_here2"]` |
| 43 non-atomic assignment name restored | 1 | `test_adversarial_text_scans_under_a_second[secret-word-run]` |

24 of 24 killed. In the first run 2 mutants survived: "quoted name not judged by its words" (my benign rows held values too short to be literal, so no row told a secret name from a plain one) and "digits per word" (the benign row `your_token_goes_here1` is already a `_PLACEHOLDER` by its `your_` head). Each got a row (`os.environ["GIT_COMMIT"] = "a1b2c3d4e5f6"`, `"Bearer access_token_here2"`) that only that rule keeps benign. m10: I chose the join, the stricter reading; it closes residual 41(c), and the row above is what only the join refuses. m12 also covered `_KV_PLACEHOLDER`, which had the same substring defect (`token: "<opaque>-example"` SENT, from Francisca's probe set): both now take a value made of placeholder words only. The counts of the first eleven rows were taken before the three rows added for the surviving mutants and for m11; those rows only add kills.

Quality Gate round 4 re-run (findings 44, 45, 46, m13). The same procedure: each mutant edits `credentials.py`, runs the suite (`test_egress_credentials.py`, `test_egress_pr3_consumers.py`, `test_egress_policy.py`, `test_qg_prescreen.py`, `test_evidence_checks_slop.py`), and restores the file by copy; `shasum -a 256` equal after every restore (`6c399ec3…`, the tree the mutants ran on; the final tree `7ab3be3c…` differs from it by the module docstring only). Runner and output: `bruno-r4/mutants.py`, `mutants-final.out` in the session scratchpad.

| Mutant | Failing | Killing tests (examples) |
|---|---|---|
| Round-4 input `credentials.py` restored whole | 59 (consumers, policy, prescreen, slop files); `test_egress_credentials.py` fails to collect (no `CATCH_ALL_LABEL`) | every leak row of 44, 45 and 46 at the prescreen state |
| 44 dot removed from the `_QUOTED_NAME` alphabet | 4 | `…[Config::set("services.stripe.secret", …)]`, `…[System.setProperty("javax.net.ssl.trustStorePassword", …)]` |
| 44 dot removed from the quoted `_KEY_VALUE` alphabet | 3 | `…['stripe.secret' => …]`, `…["db.password": …]`, `…["fs.s3a.secret.key": …]` |
| 44 empty-matching quote group (Francisca's prototype form) | 1 | `test_adversarial_text_scans_under_a_second[dotted]` (quadratic) |
| 45 `\|\|=` removed | 1 | `…[ENV["API_TOKEN"] \|\|= …]` |
| 45 fetch block `) {` removed | 1 | `…[ENV.fetch("API_TOKEN") { … }]` |
| 45 symbol subscript off | 2 | `…[config[:api_key] = …]`, `…[config[:api_key] \|\|= …]` |
| m13 `$` anchor of the placeholder words dropped | 8 | `…[password: "my-…"]`, `…[token: "test_…"]`, `…[os.environ["API_TOKEN"] = "live-…"]` |
| 46 layer off | 142 | every `test_the_catch_all_refuses_…` row, the history rows |
| 46 `_CATCH_MIN` 10 → 8 | 4 | `…_passes_names_patterns_and_references[logger.info("token rotated", extra={"build": "a1b2c3d4"})]`, the history row `token = f"q8Z{a}r2m{b}Xv7"` |
| 46 two-class rule off | 1 | `…[support.auth_phone = "(555)123-4567"]` |
| 46 word list minus `password` | 55 | `…[user.resetPassword(…)]`, `…[keyring.set_password(…)]` |
| 46 word list minus `authorization` | 19 | the history rows of 39 (`headers.set("Authorization", …)`) |
| 46 word read inside the literal | 7 | `…["integrity": "sha512-…Pw…"]`, `…[ds.setPassword(password)]` |
| 46 serialised state read as text | 92 | `test_probe_benign_form_is_sent[ssh h "export API_TOKEN=\"\$API_TOKEN\""-…]` and the serialised benign rows |
| 46 plain-value rule off | 6 | the same `ssh h …` benign row on three paths |
| 46 literal label split off | 3 | the history rows `curl -H 'Authorization: Bearer …'`, `'Authorization: Token token=…'` |
| 46 regex guard off | 1 | `…[GITHUB = re.compile(r"\bghp_[A-Za-z0-9]{20,}")  # token]` |
| 46 alternation guard off | 1 | `…[SECRET_WORDS = re.compile(r"(KEY\|TOKEN\|…)", re.I)]` |
| 46 call guard off | 1 | ``…[Every design token must come through `var(--token-name)`.]`` |
| 46 call guard with a backtracking run (my first draft) | run stopped after 8 min (baseline 45 s); against the r5 linear-time form of the test: fails in 204 s, `not linear: 3.953 s -> 63.440 s` | `test_the_catch_all_alone_scans_a_megabyte_in_linear_time[digits]`; in memory: 0.085 / 0.339 / 1.354 s on 10 / 20 / 40 KB against 0.002 / 0.003 / 0.007 s on this tree |
| 46 range guard with a backtracking run (my first draft) | run stopped after 3 min; against the r5 linear-time form of the test: fails in 14 s, `not linear: 0.279 s -> 4.432 s` | `…[range]`; in memory 0.129 / 0.510 / 2.044 s on 10 / 20 / 40 KB |
| 46 escaped-quote lookbehind removed | `-k "megabyte and escapes"` still running after 90 s (baseline 6 s); against the r5 linear-time form of the test: fails in 88 s, `not linear: 1.707 s -> 27.373 s` | `…[escapes]`, open literal; in memory 0.28 / 1.10 / 4.31 s on 10 / 20 / 40 KB |
| 46 variable-path guard off | 1 | `…[bash "$ARKA_OS/mcps/scripts/apply-mcps.sh" --token-ttl 60]` |
| 46 timestamp guard off | 1 | `…[expires = self.secret_entry("2099-01-01T00:00:00+00:00")]` |
| 46 prose guard off (whitespace, non-ASCII) | 5 | `…[passwordHint: "Use at least 12 characters"]`, the non-ASCII row |
| 46 reference judged before the scheme goes | 1 | `…[-H "Authorization: Bearer $OUTREACH_ACCESS_TOKEN" \]` |
| 46 literal default dropped with its slot | 3 | `…[run(env="${DB_DEFAULT:-…}")  # the db password]`, the history row of 41 |
| 46 pointer-word rule off | 2 | `…["api.key.id": "k1a2b3c4d5"]` (both files) |
| 46 nested quote unwrap off | 1 | `…[hints = ['tokenHint: "created_at"']]` |

30 of 30 killed. In the first run 7 mutants survived: 5 guards whose benign rows were saved by another guard (the alternation row by the pointer word `_SECRET_NAME`, the call row with no secret word outside it, `Bearer $API_KEY` under the length floor, `--token-file` by the pointer word, no row for the unwrap), and the two backtracking guards, because the timed literal was never closed. Each got a row that only its guard saves, and the timed test now scans the literal open and closed.

Quality Gate round 5 re-run (findings 46 and 47, m1, Marta's `priv`). Each mutant edits `credentials.py`, runs `test_egress_credentials.py`, `test_egress_pr3_consumers.py` and `test_egress_policy.py` with the timed tests deselected (they prove linearity, not a row), and is restored by copy with its sha256 checked (`scratchpad/bruno-r5/mutants.py`, output `mutants-final.out`; baseline and restored `8441184527c9…a775`).

| Mutant | Failing | Killing tests (examples) |
|---|---|---|
| 47 element detector off | 12 | `test_xml_element_text_and_netrc_passwords_are_refused[<password>…</password>, <Password>…, <db:password>…, <secret>…]` |
| 47 attribute key off (tag only) | 2 | `…[<property name="hibernate.connection.password">…]`, `…[<entry key="db.password">…]` |
| 47 CDATA branch removed | 1 | `…[<password><![CDATA[…]]></password>]` |
| 47 element line breaks and diff sign kept | 3 | `…[<password>\n    …\n  </password>]`, the same in a diff, `test_the_catch_all_alone_misses_a_pretty_printed_element_value` |
| 47 `@x@` / `#{…}` reference skip off | 3 | `test_xml_and_netrc_references_and_prose_pass[<password>@db.password@</password>]`, `…[#{vault.dbPassword}]` |
| 47 netrc `machine`/`default` rule off | 2 | `…[machine api.example.com login me password …]`, `…[default login me password …]` |
| 47 netrc own-line rule off | 2 | `…[machine …\n  login me\n  password …]`, `…[  password …]` |
| 47 netrc token any non-space | 2 | `…[the machine needs a password reset…]`, `…[fmt = "  password {user_pw}"]` |
| 47 netrc token takes quotes | 1 | `…[fmt = "  password {user_pw}"]` |
| 47 netrc judged by `_opaque` (no placeholder guard) | 2 | `…[… password {netrc_password}]`, `…[fmt = "  password {user_pw}"]` |
| Marta `priv` removed | 6 | `…[privKey: "…"]`, `test_marta_priv_key_call_is_refused`, the history row |
| Marta `pub` added | 3 | `test_xml_and_netrc_references_and_prose_pass[let pubKey = decode(…)]`, `…[pubKey: "…"]` |
| 46 unquoted branch off | 36 | `test_the_catch_all_refuses_a_shape_no_detector_knows[ENV API_TOKEN …, <td>api token</td>…, set the admin password to …]`, the XML and netrc history rows with every detector off |
| 46 delimiters narrowed to whitespace (`<`/`>` dropped) | 16 | `…[<td>api token</td><td>…</td>]`, `test_the_catch_all_alone_refuses_the_review_history[<password>…</password>, <Password>…]` |
| 46 gate off | 2 | `test_the_catch_all_passes_names_patterns_and_references[if (!ACCESS_TOKEN && !process.env.ZOOMINFO_USERNAME) {]`, `…[token rotated in commit 9f2f7db979]` |
| 46 gate: a digit alone | 1 | `…[token rotated in commit 9f2f7db979]` |
| 46 gate: 20+ characters rule removed | 1 | `test_the_catch_all_refuses_a_shape_no_detector_knows[ENV API_TOKEN <32 hex>]` |
| 46 declared-hash guard off | 1 | `…[pkg/auth v1.2.0 h1:…=]` |
| 46 code guard: call or subscript off | 1 | `…[if (!token) cache[q8Zr2mX7] = 1]` |
| 46 code guard: bundler `$1` suffix off | 1 | `…[renderSlot({ token: o }, createElementVNode$1)]` |
| 46 secret word inside a bare value allowed | 1 | `…[the session token is now token_2024_Q3!]` |
| 46 bare path words kept | 1 | `…[./app/Auth/Guard.php  <base64>]` |
| m1 quoted path words kept | 2 | `…['/app/Http/Auth/LoginController.php' => '<hex>',]` (both files) |
| 46 CDATA opener kept | 3 | `test_the_catch_all_alone_refuses_the_review_history[<password><![CDATA[…]]></password>]`, `…[xml-cdata]` (consumers) |
| 46 diff sign kept | 1 | `…[./app/Auth/Guard.php  <base64>]` (serialised, `+./app/…`) |
| 46 backslash not trimmed | 1 | `…[ssh h "export API_TOKEN=\"\$UPSTREAM_2\""]` |
| 46 brackets not trimmed | 1 | `…[renderSlot({ token: o }, createElementVNode$1)]` |

27 of 27 killed. In the first run 3 of 23 survived: the gate off (its first row, `…abcdefghijk1`, was a name to `_CATCH_NAME` already) and the diff-sign and backslash trims (their rows were saved by the code guard and the word-in-value rule, added after them). Each got a row that only its guard saves, and four mutants were added for the two halves of the gate and the netrc token.

Round 7 (findings 49 and 50), `scratchpad/bruno-r7/mut.py` with `mutA.py` and `mutC.py`: each mutant applied as one textual edit, the three test files run (`test_egress_credentials.py`, `test_egress_pr3_consumers.py`, `test_harness_scanner.py`; for C, `test_egress_pr3_consumers.py`, `test_decisions_privacy.py`, `test_qg_prescreen.py`), the file restored by copy and its sha256 compared with the pre-mutation copy (all equal).

| Mutant (round 7) | Failing tests (functions) | Killed by |
|---|---|---|
| 49 nested-value carry off | 9 (2) | `test_a_nested_value_child_carries_its_parent_key`, `test_round_6_majors_are_refused_by_prepare_state[dotnet…, spring…]` |
| 49 `account` out of the `machine` pattern | 3 (2) | `test_a_netrc_account_is_refused_as_a_password`, `…_by_prepare_state[netrc-account]` |
| 49 `account` out of the own-line pattern | 2 (1) | `…[  account …]`, `…[+  account …]` |
| 49 netrc usage guard off | 2 (1) | `test_a_netrc_account_reference_or_usage_passes[account: 'account [info]', …]` |
| 49 path words dropped whatever the value (the round-6 rule) | 16 (3) | `test_a_secret_keyed_by_a_file_path_is_refused`, `…_by_the_catch_all_alone`, `…_by_prepare_state[path-keyed-json, path-keyed-php]` |
| 49 only the value AFTER the path read | 1 (1) | `test_the_catch_all_passes_names_patterns_and_references[<hex>  ./app/Auth/Guard.php]` |
| 49 m2 `_CATCH_NAME_VALUE` replaced by `_CATCH_NAME` | 2 (2) | `test_a_slashed_base64_key_is_not_read_as_a_name`, `test_random_base64_keys_miss_only_the_documented_residual` |
| 49 m1 `policy` out of the pointer words | 3 (2) | `test_a_policy_about_a_secret_passes`, `test_a_nested_value_without_a_secret_key_or_literal_passes[passwordPolicy]` |
| 49 m1 pointer suffixes dropped | 1 (1) | `test_a_policy_about_a_secret_passes[PASSWORD_POLICY=…]` |
| 49 Stripe key pattern off | 11 (3) | `test_stripe_keys_are_refused_by_the_vendor_vocabulary`, `TestSecrets::test_secret_labels`, `…_by_prepare_state[stripe-ts, stripe-curl]` |
| 49 Stripe webhook pattern off | 2 (2) | `…vendor_vocabulary[whsec_…]`, `TestSecrets::test_secret_labels[whsec_…]` |
| 50 allowlist emptied | 268 (44) | every diff-state test on an allowlisted file, `test_every_allowlisted_suffix_leaves`, the slop-score section |
| 50 a pathless diff state allowed | 34 (2) | `test_a_diff_state_that_names_no_file_is_refused[raw, serialised, empty-list]`, `test_a_diff_of_a_file_outside_the_allowlist_never_leaves` (serialised half) |
| 50 prescreen filter off | 2 (2) | `test_the_prescreen_skips_config_files_before_diffing_and_records_them`, `test_the_prescreen_filter_runs_before_git` |

14 of 14 killed.

Round 8 (finding 51 and minor m1), `scratchpad/bruno-r8/mut.py`: each mutant applied as one textual edit, four test files run (`test_egress_pr3_consumers.py`, `test_qg_prescreen.py`, `test_evidence_checks_slop.py`, `test_decisions_privacy.py`), the file restored by copy and its sha256 compared with the pre-run value (`bruno-r8/pre.sha`, `shasum -a 256 -c` → OK for all four sources). Output: `bruno-r8/mutants-r8.out`.

| Mutant (round 8) | Failing tests (functions) | Killed by |
|---|---|---|
| 51 `partition_paths` judges the name only (the round-7 rule) | 3 (2) | `test_the_prescreen_skips_a_link_to_a_config_file_on_record[named, derived-from-git]`, `test_the_prescreen_records_a_hard_link_as_path_class` |
| 51 resolved check removed from `_untracked_diff` | 1 (1) | `test_file_diff_never_reads_through_a_link_to_a_config_file` |
| 51 resolved check removed from `prose_text` | 1 (1) | `test_the_slop_section_skips_a_link_to_a_config_file_on_record` |
| 51 the resolved file's suffix not judged | 6 (4) | `test_a_name_is_judged_by_the_file_it_resolves_to[util.py-config.yaml, gone.py-missing.yaml]` and the three consumer tests |
| 51 hard-link check removed | 3 (2) | `…_resolves_to[hard.py-None, copy.py-None]`, `test_the_prescreen_records_a_hard_link_as_path_class` |
| 51 resolve without the inside check | 1 (1) | `…_resolves_to[up.py-../outside.py]` |
| 51 partition reason `path-class` → another string | 5 (4) | the two prescreen record tests of 51 and the two of 50 (`…_skips_config_files_before_diffing_and_records_them`, `…_filter_runs_before_git`) |
| 51 slop row reason → `no prose to score` | 1 (1) | `test_the_slop_section_skips_a_link_to_a_config_file_on_record` |
| 51 `prose_text` scope → `outside the project` | 1 (1) | the same |
| m1 a-side of a `diff --git` header dropped | 1 (1) | `test_a_git_header_inside_the_diff_names_its_file_too[diff --git a/config/app.yaml b/core/a.py]` (the new row; it survived round 7) |
| m1 b-side of a `diff --git` header dropped | 1 (1) | `…[diff --git a/core/a.py b/config/app.yaml]` |

11 of 11 killed. The first run reported the two m1 mutants as surviving with exit 1: the parser of the script cut a failing test ID at its first space; a manual run showed the kill, the parser was fixed and the whole table re-run.

Round 9 (finding 52 and the round-8 minors m1, m2 of Francisca), `scratchpad/r9/mut.py`: each mutant applied as one textual edit, three test files run (`test_egress_pr3_consumers.py`, `test_qg_prescreen.py`, `test_evidence_checks_slop.py`), the file restored by copy and its sha256 compared with the pre-run value (`r9/pre.sha`, `shasum -a 256 -c` → OK for all four sources). Output: `r9/mutants-r9.out`.

| Mutant (round 9) | Failing tests (functions) | Killed by |
|---|---|---|
| 52 `literal_git.run` without the flag and the variable (plain git) | 7 (3) | `test_the_slop_section_reads_a_pathspec_name_as_one_file[:!x.md, big.md, *.md, b*.md, old.md]`, `test_magic_and_glob_names_reach_git_literally`, `test_every_git_call_of_both_readers_is_literal` |
| 52 `GIT_LITERAL_PATHSPECS` dropped from the env | 1 (1) | `test_every_git_call_of_both_readers_is_literal` |
| 52 `--literal-pathspecs` dropped from the argv | 1 (1) | the same |
| 52 `S_ISREG` guard reverted to the round-8 rule | 2 (2) | `test_a_directory_or_a_fifo_is_refused_on_the_resolved_side`, `test_a_directory_name_is_skipped_on_record` |
| 52 `S_ISREG` guard dropped (link count alone) | 1 (1) | `test_a_directory_or_a_fifo_is_refused_on_the_resolved_side` (the FIFO) |
| 52 `names_exactly` removed from `file_diff` | 4 (2) | `test_the_prescreen_reads_a_pathspec_name_as_one_file[big.md, lib.py, old.md]`, `test_a_diff_covering_more_than_the_name_is_refused_whole` |
| 52 `names_exactly` removed from `prose_text` | 1 (1) | `test_the_names_check_alone_refuses_a_directory_in_prose` |
| 52 `carry_git_headers` removed from `chunk_diff` | 2 (2) | `test_every_chunk_carries_its_file_header_and_is_judged`, `test_a_60k_diff_is_three_requests_each_under_the_cap` |
| r8 m1 resolved guard moved below the tracked branch of `prose_text` | 1 (1) | `test_a_tracked_hard_link_to_config_prose_is_refused_before_git` |
| r8 m2 `except OSError: return False` flipped to True | 1 (1) | `test_a_target_whose_stat_fails_is_refused` |

10 of 10 killed. The plain-git mutant kills no prescreen row: with the names read as patterns, `names_exactly` and the carried headers still refuse every chunk that holds another file, which is the defence in depth working; the literal layer is pinned by its own rows. The first run showed `test_a_60k_diff_is_three_requests_each_under_the_cap` failing under every mutant: its "nothing lost" join did not expect the carried header, a test of round 3 made stale by this fix, not a flake. It now asserts that every chunk opens with `diff --git a/big.py b/big.py` and that the bodies, minus the carried line, rebuild the diff; the table was re-run on the corrected test.

Round-9 fix-forward (the five minors m1 to m5 of Francisca's round-9 review; Quality Gate round 9 APPROVED with minors), `scratchpad/bruno-r9/mut.py`: the same three test files, each mutant applied as a textual edit, the file restored by copy and its sha256 compared with the pre-run value (all equal). Output: `bruno-r9/mutants.out`.

| Mutant (round-9 fix-forward) | Failing tests (functions) | Killed by |
|---|---|---|
| m3 `_split_lines` and `_git_header_lines` back on `str.splitlines` | 9 (1) | `test_a_line_separator_inside_a_content_line_never_forges_a_carried_header[U+000B, U+000C, U+001C, U+001D, U+001E, U+0085, U+2028, U+2029, U+000D]` |
| m3 `_split_lines` alone back on `str.splitlines` | 9 (1) | the same (the filler sweep puts a chunk boundary on the forged fragment) |
| m3 `_git_header_lines` alone back on `str.splitlines` | 9 (1) | the same |
| m4 tracking probe back on `returncode != 0` | 1 (1) | `test_a_git_without_literal_pathspecs_reads_no_diff` (a git shim exiting 129 on `--literal-pathspecs`) |
| m4 git-spelling guard removed from `_untracked_diff` | 4 (1) | `test_a_name_git_would_not_print_is_never_read_whole[x.py/, x.py//, ./new.py, new.py/]` |
| m5 `--relative` dropped from `names_exactly` | 1 (1) | `test_names_exactly_compares_relative_names_from_a_subdirectory` |
| m1 `== [name, ""]` turned into `name in ...` | 1 (1) | `test_a_staged_file_that_replaced_a_directory_is_refused_whole` |
| m2 carried-header reserve line deleted from `chunk_diff` | 3 (1) | `test_a_long_carried_header_keeps_every_chunk_within_the_limit[24000-2800, 24000-1500, 5000-450]` |

8 of 8 killed. `./x.py` is not in the guard's rows: git tracks it (`ls-files` exit 0), so it takes the diff path and `names_exactly` refuses it, the row-52 residual.

### Verification (PR3, this lane)

- `env -u CLAUDE_EFFORT ARKA_BYPASS_DECISIONS=1 arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py tests/python/test_qg_prescreen.py tests/python/test_evidence_checks_slop.py tests/python/test_frontend_gate_jev.py tests/python/test_stop_jev.py -q` → **exit 0**, 3377 passed. Before the fixes on this tree: 6 failed (the two `.ts` secret rows and the four leaf-audit rows).
- Whole `tests/python`: 13288 passed, 21 failed and 1 error, all 22 in `tests/python/watch/` (ffmpeg rejects `-vsync`, the known issue #563), none in a file this lane touched.
- `ruff check core/egress core/governance/qg_prescreen.py core/governance/slop_check.py core/governance/jev_advisory.py tests/python/test_egress_pr3_consumers.py` → **exit 0** (also on `privacy.py` and the three other touched test files: exit 0).
- `uvx codespell core/ installer/ scripts/ tests/ docs/security/` → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep` on the touched files → **exit 0**, PASS.
- Functions ≤ 30 lines (AST): `credentials.py` max 27 (`_quote_context`), `privacy.py` 20 (`prepare_state`), `qg_prescreen.py` 22 (`write_prescreen`), `slop_check.py` 19 (`_score_file`), `jev_advisory.py` 17 (`readable_inside`); tests: `test_egress_credentials.py` 17 (`_interleaved`), `test_egress_pr3_consumers.py` 19 (`stop_env`), `test_qg_prescreen.py` 18, `test_egress_policy.py` 21.

Re-run after Quality Gate round 1 (findings 38-40, m4, the copy fixes):

- `env -u CLAUDE_EFFORT ARKA_BYPASS_DECISIONS=1 ~/.arkaos/bin/arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py tests/python/test_qg_prescreen.py tests/python/test_evidence_checks_slop.py -q -p no:cacheprovider` → **exit 0**, 3412 passed. With the round-1 `credentials.py` in place: 26 failed; with the round-1 `jev_advisory.py`: 9 failed (mutation table above).
- `ruff check core/egress core/governance/jev_advisory.py tests/python/test_egress_credentials.py tests/python/test_egress_pr3_consumers.py tests/python/test_qg_prescreen.py` → **exit 0**.
- `uvx codespell core/ tests/ docs/security/` → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files core/egress/credentials.py,core/governance/jev_advisory.py,tests/python/test_egress_credentials.py,tests/python/test_egress_pr3_consumers.py,tests/python/test_qg_prescreen.py` → **exit 0**, PASS (one fixture line carries `arka:sec-ok(hardcoded-password)`).
- Functions ≤ 30 lines (AST): `credentials.py` 27 (`_quote_context`), `jev_advisory.py` 20 (`readable_inside`); tests: `test_egress_credentials.py` 17 (`_interleaved`), `test_egress_pr3_consumers.py` 19 (`stop_env`), `test_qg_prescreen.py` 18 (`test_a_60k_diff_is_three_requests_each_under_the_cap`).

Re-run after Quality Gate round 2 (finding 41, m7, m8):

- `env -u CLAUDE_EFFORT ARKA_BYPASS_DECISIONS=1 ~/.arkaos/bin/arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py tests/python/test_qg_prescreen.py tests/python/test_evidence_checks_slop.py -q -p no:cacheprovider` → **exit 0**, 3471 passed. With the round-2 input `credentials.py` in place: 39 failed (mutation table above).
- `ruff check core/egress tests/python/test_egress_credentials.py tests/python/test_egress_pr3_consumers.py` → **exit 0**.
- `uvx codespell core/ tests/ docs/security/` → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files core/egress/credentials.py,tests/python/test_egress_credentials.py,tests/python/test_egress_pr3_consumers.py` → **exit 0**, PASS (five fixture lines carry `arka:sec-ok(hardcoded-password)`).
- Functions ≤ 30 lines (AST): `credentials.py` 27 (`_quote_context`); tests: `test_egress_credentials.py` 17 (`_interleaved`), `test_egress_pr3_consumers.py` 19 (`stop_env`).

Re-run after Quality Gate round 3 (findings 42 and 43, m10, m11, m12):

- `env -u CLAUDE_EFFORT ARKA_BYPASS_DECISIONS=1 ~/.arkaos/bin/arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py tests/python/test_qg_prescreen.py tests/python/test_evidence_checks_slop.py -q -p no:cacheprovider` → **exit 0**, 3553 passed. With the round-3 input `credentials.py` in place: 44 failed (mutation table above).
- `ruff check core/egress tests/python/test_egress_credentials.py tests/python/test_egress_pr3_consumers.py` → **exit 0**.
- `uvx codespell core/ tests/ docs/security/` → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files core/egress/credentials.py,tests/python/test_egress_credentials.py,tests/python/test_egress_pr3_consumers.py,docs/security/2026-09-23-jev-decisions-review.md` → **exit 0**, PASS (six fixture lines and two review rows carry `arka:sec-ok(hardcoded-password)`).
- Functions ≤ 30 lines (AST): `credentials.py` 27 (`_quote_context`); tests: `test_egress_credentials.py` 17 (`_interleaved`), `test_egress_pr3_consumers.py` 19 (`stop_env`).
- False-positive sweep: `~/.arkaos/quality-gate/81e2c0d9-…/pr3/bruno-sweep-r3.out` (finding 42).

Re-run after Quality Gate round 4 (findings 44, 45, 46, m13, m14, the :541 note):

- `env -u CLAUDE_EFFORT ARKA_BYPASS_DECISIONS=1 ~/.arkaos/bin/arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py tests/python/test_qg_prescreen.py tests/python/test_evidence_checks_slop.py -q -p no:cacheprovider` → **exit 0**, 3795 passed. With the round-4 input `credentials.py` in place: 59 failed in the four files that collect (mutation table above).
- `ruff check core/egress tests/python/test_egress_credentials.py tests/python/test_egress_pr3_consumers.py` → **exit 0**.
- `uvx codespell core/ tests/ docs/security/` → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files core/egress/credentials.py,tests/python/test_egress_credentials.py,tests/python/test_egress_pr3_consumers.py,docs/security/2026-09-23-jev-decisions-review.md` → **exit 0**, PASS (six fixture lines and one review row carry `arka:sec-ok(hardcoded-password)`).
- Functions ≤ 30 lines (AST): `credentials.py` 30 (`_catch_units`); tests: `test_egress_credentials.py` 17 (`_interleaved`), `test_egress_pr3_consumers.py` 19 (`stop_env`).
- False-positive sweep and threshold table: `~/.arkaos/quality-gate/81e2c0d9-…/pr3/bruno-sweep-r4.out` (findings 44 and 46).

Re-run after Quality Gate round 5 (findings 46 and 47, R5-M2, R5-M3, R5-M4, m1, m2, Marta's `priv`):

- `env -u CLAUDE_EFFORT ARKA_BYPASS_DECISIONS=1 ~/.arkaos/bin/arka-py -m pytest tests/python/test_egress*.py tests/python/test_decisions_privacy*.py tests/python/test_decisions_policy.py tests/python/test_qg_prescreen.py tests/python/test_evidence_checks_slop.py -q -p no:cacheprovider` → **exit 0**, 3915 passed (65.3 s). With the round-5 input `credentials.py` in place (timed megabyte test deselected): 83 failed, 3817 passed.
- `tests/python/test_egress_credentials.py` alone with `--cov=core --cov-report=`, twice → **exit 0** both, 3361 passed, 88.8 s and 89.4 s wall (50.1 s without coverage).
- Linear time: 4x the text within 8x + 0.05 s, 2x at the 1 MB cap within 3x + 0.05 s, and one form at the cap within `_CAP_SLOW = 4.0` CPU seconds; measured worst 0.81 s without coverage and 1.60 s under it (round 4: 0.74 s and 1.45 s).
- `ruff check core/egress tests/python/test_egress_credentials.py tests/python/test_egress_pr3_consumers.py` → **exit 0**.
- `uvx codespell core/ tests/ docs/security/` → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files core/egress/credentials.py,tests/python/test_egress_credentials.py,tests/python/test_egress_pr3_consumers.py,docs/security/2026-09-23-jev-decisions-review.md` → **exit 0**, PASS.
- Functions ≤ 30 lines (AST): `credentials.py` 30 (`_catch_units`, unchanged; `_catch_line` 25, `_catch_bare` 22); tests: `test_egress_credentials.py` 26 (the megabyte test), `test_egress_pr3_consumers.py` 19 (`stop_env`).
- False-positive sweep, gated and ungated: `~/.arkaos/quality-gate/81e2c0d9-…/pr3/bruno-sweep-r5.out` (findings 46 and 47).

Re-run after the round-6 follow-up (finding 48):

- The same pytest command → **exit 0**, 3929 passed (88.3 s); the 4 finding-48 mutants killed (`scratchpad/bruno-r5/mutants-r6.out`, restored by copy, sha256 checked); `ruff`, `uvx codespell core/ tests/ docs/security/` and security-grep on the four files → **exit 0**.
- Evidence hygiene: `pr3/bruno-sweep-r5.out` keeps locations, reasons and counts only; every copied line text was redacted, and the round-6b section prints the secret words on a line, never its text. This review no longer quotes a caught value (the Laravel TOTP seed and test password of row 46 are now `<literal>`).

Re-run after Quality Gate round 6 (findings 49 and 50; operator decision "Ronda 7: majors + allowlist por tipo de ficheiro"):

- The pinned Quality Gate set (`~/.arkaos/quality-gate/81e2c0d9-…/pr3/r1-testcmd.txt`, 44 test files) → **exit 0**, 5406 passed (105.2 s).
- `arka-py -m pytest tests/python/test_harness_scanner.py -q -p no:cacheprovider` (outside the pinned set, touched for the Stripe rows) → **exit 0**, 138 passed.
- `tests/python/test_egress_credentials.py::test_the_catch_all_alone_scans_a_megabyte_in_linear_time` under `--cov=core --cov-report=` → **exit 0**, 13 passed, 36.8 s wall.
- Mutants: 14 of 14 killed (table above), every file restored by copy, sha256 equal before and after.
- `ruff check` on the 13 touched Python files → **exit 0**.
- `uvx codespell` on this review and the 13 touched Python files → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files <the 14 touched files>` → **exit 0**, PASS (seven justified `arka:sec-ok` suppressions, all pre-existing).
- Functions ≤ 30 lines (AST, docstring included): `credentials.py` 29 (`_catch_units`, unchanged; `_catch_line` 17, `_gap_pieces` 17), `privacy.py` 19 (`_leaves`), `qg_prescreen.py` 21 (`write_prescreen`), `harness_scanner.py` 29 (`scan`); tests: `test_egress_credentials.py` 19, `test_egress_pr3_consumers.py` 18, `test_harness_scanner.py` 22 (the pre-existing 31-line `test_unreadable_script_never_erases_the_files_criticals` split with a helper).
- False-positive sweep round 7: `pr3/bruno-sweep-r5.out`, round-7 section (finding 49), locations and reasons only.

Re-run after Quality Gate round 7 (finding 51; minors m1, m2 of Francisca and m1, m2 of Eduardo; operator decision "Ronda 8 com critério fixo: symlink + prosa + 4 minors"):

- The pinned Quality Gate set (`~/.arkaos/quality-gate/81e2c0d9-…/pr3/r1-testcmd.txt`, 44 test files) → **exit 0**, 5421 passed (102.7 s), run after the last source edit.
- Mutants: 11 of 11 killed (round-8 table above), every source restored by copy, `shasum -a 256 -c bruno-r8/pre.sha` → OK for all four.
- Before the fix, with the three consumer edits reverted: 5 of the new finding-51 tests fail (`bruno-r8/before-fix.out`); restored by copy, sha256 equal.
- m2 decision: the `privacy.py` docstring was narrowed to what `_refuse_path_class` checks (own `path`, file entries one list down, both sides of `diff --git` headers under `diff`), with the shapes it does not check and the rule that producers pass the path. Scanning the extra shapes would add a parser with its own misses for states no producer builds; narrowing costs nothing and Francisca's probe (`bruno-r8/fx2-r8.out`) shows the documented set exactly: the two-deep list, the nested dict, `patch`/`content` headers and `diff --cc` are sent; the rest are refused.
- Eduardo m2: row 49's Residual cell no longer names residual (i) twice.
- `ruff check` on the five touched Python files → **exit 0**.
- `uvx codespell` on this review, the ADR and the five touched Python files → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files <the 7 touched files>` → **exit 0**, PASS (five justified `arka:sec-ok` suppressions, all pre-existing).
- Functions ≤ 30 lines (AST, docstring included): `jev_advisory.py` 23 (`resolved_path_allowed`), `qg_prescreen.py` 22 (`write_prescreen`), `slop_check.py` 19 (`prose_text`), `privacy.py` 22 (`prepare_state`); tests: `test_egress_pr3_consumers.py` 19 (`stop_env`).

Re-run after Quality Gate round 8 (finding 52; minors m1, m2 of Francisca and m3 to m6 of Eduardo in this review; operator decision "Ronda 9 com critério fixo: pathspec/diretório + 8 minors"):

- The pinned Quality Gate set (`~/.arkaos/quality-gate/81e2c0d9-…/pr3/r1-testcmd.txt`, 44 test files) → **exit 0**, 5443 passed (105.2 s), run after the last source edit.
- Mutants: 10 of 10 killed (round-9 table above), every source restored by copy, `shasum -a 256 -c r9/pre.sha` → OK for all four. After that run `qg_prescreen.py` had its module docstring rewrapped (ruff E501, no code changed); the pinned set above ran on that final tree.
- The round-8 repros re-run on this tree (`r8/marta/repro.py`, `r8/francisca/fx8.py`, `fx9.py`, `fx10.py`; output `r9/repros-r9.out`, booleans and reasons only): no `file_diff`, chunk or `prose_text` holds config content; `big.md` is skipped as `path-class`; `:!zz.py` and `:!zz.md` send their own text only.
- Eduardo's round-8 minors in this review: row 50 cites `[×5]` and names the a-side case; rows 50 and 51 both say "renamed or copied under a source suffix"; the round-8 mutation table uses the pytest IDs; the round-8 verification header names m1, m2 of Eduardo. His two ADR minors are Gabriel's lane.
- `ruff check` on the seven touched Python files → **exit 0**.
- `uvx codespell` on this review and the seven touched Python files → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files <the 8 touched files>` → **exit 0**, PASS (five justified `arka:sec-ok` suppressions, all pre-existing).
- Functions ≤ 30 lines (AST, docstring included): `literal_git.py` 12 (`names_exactly`), `jev_advisory.py` 26 (`resolved_path_allowed`), `qg_prescreen.py` 21 (`write_prescreen`; `carry_git_headers` 15, `chunk_diff` 16), `slop_check.py` 22 (`prose_text`), `evidence_checks.py` 7 (`_git_tracks`) and 21 (`_added_lines`), the two functions touched; tests: `test_egress_pr3_consumers.py` 24 (the `specs` fixture), `test_qg_prescreen.py` 20.

Re-run after the Quality Gate round-9 fix-forward (minors m1 to m5 of Francisca, Eduardo 3 in this review; Eduardo 2 in the `literal_git.py` docstring):

- The pinned Quality Gate set (`~/.arkaos/quality-gate/81e2c0d9-…/pr3/r1-testcmd.txt`, 44 test files) → **exit 0**, 5464 passed (108.1 s), run after the last source edit (`bruno-r9/pinned.out`).
- Mutants: 8 of 8 killed (round-9 fix-forward table above), every source restored by copy, sha256 equal before and after.
- Francisca's round-9 fixtures on this tree: `fx12.py` 3000 random cases, 0 over-limit chunks (worst ratio 0.9885), and the form-feed case now carries only `diff --git a/ok.py b/ok.py` with every chunk SENT; `fx14.py` (git exiting 129) `file_diff` is empty for `x.py` and `x.md`; `fx16.py` `big.md` exact False, `file_diff` empty, slop `path-class`; `fx11.py` `x.py/` sends nothing.
- `literal_git.py` docstring: "every git call that carries a changed-file name" (Eduardo 2), the two calls outside the helper named, and the old-git outcome stated per reader instead of "every caller reads no diff".
- `ruff check` on the three touched Python files → **exit 0**.
- `uvx codespell` on this review and the three touched Python files → **exit 0**.
- `arka-py -m core.governance.evidence_checks . --checks security-grep --changed-files <the 4 touched files>` → **exit 0**, PASS.
- Functions ≤ 30 lines (AST, docstring included): `qg_prescreen.py` 21 (`write_prescreen`; `_untracked_diff` 20, `file_diff` 16, `_newline_lines` 10, `_split_lines` 13, `chunk_diff` 16), `literal_git.py` 13 (`names_exactly`); tests: `test_egress_pr3_consumers.py` 24 (the `specs` fixture; the new rows at most 17, the `old_git` fixture).

### CI calibration (post-approval)

After approval the `python-tests (3.12)` ubuntu job, the only leg that runs under `--cov=core`, failed four absolute ceilings in `tests/python/test_egress_credentials.py`: `test_adversarial_text_scans_under_a_second[kv-json]` (1.64 s CPU against 1.5), `test_finding_42_detectors_scan_in_linear_time[positional]` (1.63 against 1.5) and `test_the_catch_all_alone_scans_a_megabyte_in_linear_time[escapes]` and `[bare-tokens]` (2.64 s each against 2.0). Python 3.11, 3.13, macOS 3.12 and Windows 3.12 passed, and so did every interleaved ratio test on every leg. The ceilings were measuring the runner: that job ran the same scans about 2.3x slower than the operator's machine (0.8 to 1.64 s, 1.15 to 2.64 s). No production code changed.

- Mechanism: every absolute ceiling (`_SLOW`, `_SHAPE_SLOW`, `_CAP_CEILING`, which is `_CAP_SLOW` or half of it under coverage, and the early stop in `_timed`) is multiplied by a machine factor. The factor comes from a fixed regex workload that does not touch `core.egress` (six `findall` passes over 1.26 MB of generated text, best of 3 in `time.process_time`), measured once per session and divided by its time on the operator's machine, 0.083 s (Apple M4 Max, CPython 3.13, 2026-09-24). Because the workload is independent, a slower scan cannot loosen its own bound.
- Clamp: the factor is at least 1.0, so a fast machine never tightens the historical ceilings, and at most 4.0, so code 4x slower than today still fails on the slowest supported runner. Failure messages print the ceiling and the factor.
- Ratio tests unchanged: 4x the text ≤ 8x the time + 0.05 s, 2x at the cap ≤ 3x + 0.05 s. They remain the linearity proof and carry no factor.
- Evidence: locally the factor is 1.00 with and without coverage (the tracer does not reach the C regex engine). A one-off pytest plugin (outside the repo) that runs the scan 3 times per call reproduces the CI failures at factor 1.0 (1.57, 1.57, 2.36 and 2.49 s); at a forced factor of 2.5 the same four pass; with the scan 15 times per call (the 3x runner times a 5x regression) all four fail again at factor 2.5 (4.79, 4.82, 11.80 and 12.50 s against 3.75 and 5.00). With the factor forced to 1.0 the file behaves exactly as before: every ceiling equals the old constant (1.0, 1.5, 4.0, 2.0) and the whole file passes under `--cov=core`, 3433 tests, exit 0.
- The file passes with and without `--cov=core` (3433 passed, exit 0 each); the pinned Quality Gate set exits 0 with 5464 passed; `ruff check` on the test file exits 0; no function in it is over 30 lines (the largest is 25).
