---
name: strat/scenario-plan
description: >
  Scenario planning with PESTLE: builds 3-5 plausible futures (base,
  optimistic, pessimistic, black swan) and a strategy plus action plan
  for each, so the business is prepared for whichever unfolds. TRIGGER:
  "cenários", "scenario planning", "planeamento de cenários", "futuros
  possíveis", "e se o mercado mudar", "black swan", "/strat scenario".
  SKIP: numeric what-ifs on a financial model ->
  finance/scenario-analysis (quantitative sensitivity on numbers; this is
  qualitative strategic futures); stress-testing a single bet ->
  strat/premortem.
allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch, WebSearch]
---

<!-- arka:kb-first-prefix begin -->
> **KB-first:** query `mcp__obsidian__search_notes` and cite
> `[[wikilinks]]` — or declare the gap — BEFORE any external research.
> Full doctrine: `arka/SKILL.md` (KB-First Research).
<!-- arka:kb-first-prefix end -->

# Scenario Plan — `/strat scenario <context>`

> **Agent:** Lucas (Market Analyst) | **Framework:** Scenario Planning + PESTLE

## What It Does

Scenario planning: 3-5 plausible futures with strategies for each.

## Output

Scenario matrix with base/optimistic/pessimistic/black swan + action plans
