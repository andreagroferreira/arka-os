---
tags:
  - arkaos
  - vector
---
# Vector Store

SQLite-vec backed store for semantic search. Falls back to keyword when sqlite-vec absent.
Indexes markdown chunks with embeddings for sub-100ms retrieval.

See [[Embedder Setup]] and [[KB Architecture]].
